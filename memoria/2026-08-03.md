# Atualização 2026-08-03

## Glossário rápido (pra quem lê sem contexto de chat)

- **AI_HUNTER1.1**: app Streamlit portátil de auditoria fiscal (Qlik/
  NF-e/SPED), usado por auditor(es) pra cruzar movimentação declarada
  (SPED) com movimentação física (XML da nota fiscal eletrônica).
- **Bloco H (SPED)**: bloco do arquivo de escrituração fiscal digital
  (SPED) que declara o Inventário — Estoque Inicial/Final por produto e
  ano. No app vira a tabela `estoque_anual_consolidado` (Estágio 5).
- **XML (NF-e)**: nota fiscal eletrônica em si — vira as tabelas
  `estoque_entradas`/`estoque_saidas` (Estágio 4), a movimentação FÍSICA
  real (compras/vendas), diferente do que foi declarado no SPED.
- **RN1**: identidade contábil EI (Estoque Inicial) + Compras = Vendas +
  EF (Estoque Final), usada pra calcular Divergência/Infração por
  produto — Estágios 7.2 (por valor, só itens com match no Matching/BC3)
  e 7.3/7.3.1/7.3.2 (movimentação física, TODO o XML, com ou sem match).
- **`produto_alvo_fiscalizacao`**: tabela real onde ficam os produtos que
  o auditor efetivamente escolheu fiscalizar (Estágio 7.3.2, botão "💾
  Salvar Grupo de Produto Alvo") — chave única `DESCR_ALVO`, com
  `STATUS` ativo/cancelado (histórico nunca apagado) e os campos do RN1
  (DIVERGENCIA/INFRACAO/TOTAL_DEBITO/TOTAL_CREDITO/PCT_DIVERGENCIA).
  Alimenta o Estágio 10 (Rubrica do Produto Alvo).

## Estágio 7.3.3 — Seleção Consolidada de Alvos (implementado do zero)

Solicitação Técnica detalhada e completa anexada pelo usuário (com
prompt pronto pro agente) — resumo: hoje o auditor só encontra alvos de
fiscalização olhando divergência financeira calculada (7.2/7.3), mas
alguns itens suspeitos (volume físico alto no XML, estoque parado no
Bloco H) nunca aparecem porque não geram divergência. Pedido um painel
que una Entradas (XML)/Saídas (XML)/Estoque (SPED) numa única tabela de
consulta, com filtro e checkbox, pra "cravar" esses produtos direto como
alvo — sem depender do cálculo de RN1.

### Achados que não bateram com a especificação (perguntados antes de codar)

A Solicitação Técnica assumia 3 coisas que o schema real não tinha —
investigado ANTES de escrever qualquer código (ver
[[feedback_anexo_nao_corresponde]] — mesmo princípio: parar e sinalizar
quando o que foi pedido não bate com o que existe de verdade):

1. **Nomes de coluna**: a especificação usava `xprod`/`ucom`/`qcom`/
   `VL_ITEM`/`emit_cnpj`/`dest_cnpj` como se fossem literais — os nomes
   reais são `fatoitemnfe_infnfe_det_prod_xprod`/`_ucom`/`_qcom`/`_vprod`
   e `fatonfe_infnfe_emit_cnpj`/`_dest_cnpj`. Sem impacto de decisão, só
   tradução.
2. **`VL_ITEM` não existe em `estoque_anual_consolidado`** (Estágio 5) —
   só `QUANTIDADE_INICIAL`/`QUANTIDADE_FINAL`; o valor em R$ do Bloco H
   só existe no SPED cru, agregado por (ANO, COD_ITEM), decisão
   documentada desde sempre em `_valores_estoque_hunter()` de NÃO
   estender esse schema.
3. **`produto_alvo_fiscalizacao` não tem campos de Ano/Qtde/Origem** — só
   os campos do RN1 (DIVERGENCIA/INFRACAO/TOTAL_DEBITO/TOTAL_CREDITO).
   "Cravar" um item do 7.3.3 significa criar um alvo SEM RN1 calculado —
   ambiguidade real sobre o que fazer com esses 4 campos.

Perguntado ao usuário via `AskUserQuestion` (4 perguntas, todas com
recomendação):
- **Valor Total do Estoque**: deixar em branco/0 (em vez de tentar
  reconciliar com o SPED cru, granularidade não bate 1:1).
- **Campos RN1 do alvo cravado via 7.3.3**: zerar/nulo + Observação
  registrando a origem (em vez de calcular RN1 na hora — fora do escopo
  original).
- **Semântica do "Cravar"**: SÓ ADITIVO — nunca cancela nada que já
  existia (nem do 7.2, nem de uma rodada anterior do 7.3.3), diferente
  do "💾 Salvar Grupo de Produto Alvo" do 7.3.2, que reconcilia a tela
  inteira (desmarcar cancela) — risco real aqui porque o 7.3.3 mostra um
  recorte/filtro, não o universo todo.
- **Granularidade**: colapsar por descrição (upsert por `DESCR_ALVO`,
  mesma chave de sempre) em vez de granularidade por linha (mudaria o
  schema da tabela real).

### Implementado (`loader.py`)

- `gerar_consolidado_origens_733()`: UNION ALL de Entradas
  (`estoque_entradas`), Saídas (`estoque_saidas`, excluindo autoemissão
  — `fatonfe_infnfe_emit_cnpj != fatonfe_infnfe_dest_cnpj`, mesma regra
  de Vendas usada em todo o app) e Estoque (`estoque_anual_consolidado`,
  só `QUANTIDADE_FINAL IS NOT NULL`). Mesma dedup ET/EP de
  `_valores_por_ano_item()` (`ROW_NUMBER() OVER (PARTITION BY CHV_NFE,
  NITEM ORDER BY PASTA_ORIGEM)`) aplicada em Entradas/Saídas — reaproveita
  o mesmo raciocínio já validado em 2026-07-18, não é lógica nova.
  **Decisão de engenharia não pedida explicitamente**: resultado
  AGREGADO por (ANO, DESCR_PROD, UNID_PROD, ORIGEM) — SUM de Qtde/Valor —
  em vez de uma linha por item físico de XML, que teria 39.686+ linhas
  já SÓ na geraldo (uma linha por NOTA seria centenas de milhares,
  inviável pro `st.data_editor` e sem ganho de informação real pro
  auditor, que decide por produto/ano/origem).
- `persistir_consolidado_origens_733()`/`estagio733_consolidado_ja_
  gerado()`/`consultar_consolidado_origens_733()`: mesmo padrão
  gerar/persistir/consultar de todo outro estágio — tabela cache
  `estagio733_consolidado`.
- `salvar_alvos_selecionados_733(selecionados)`: upsert ADITIVO em
  `produto_alvo_fiscalizacao` — produto NOVO entra com
  `DIVERGENCIA/TOTAL_DEBITO/TOTAL_CREDITO/PCT_DIVERGENCIA=0.0`,
  `INFRACAO=''`, `OBSERVACAO="Selecionado via 7.3.3 (sem cálculo RN1)."`;
  produto que JÁ EXISTIA (do 7.2 ou de uma rodada anterior do 7.3.3) só
  REATIVA (`STATUS='ativo'`, `TS` atualizado) — todos os campos de RN1
  já calculados ficam INTOCADOS, nunca zerados por cima. `total_
  adicionado`/`total_reativado` calculados por diferença de conjunto
  (mesmo padrão da Execução Automática do Estágio 10.1, `memoria/
  2026-07-30.md`).

### Implementado (`interface.py`)

`render_consolidado_origens_733()` — seção "🔍 7.3.3: Seleção Consolidada
(Estoque/XML)" renderizada logo abaixo do painel 7.3.2, dentro da MESMA
página (`render_pagina_rn1_simulada_30()`) — posicionamento pedido
explicitamente na Solicitação Técnica. Botão "Gerar/Regerar Consolidado
(7.3.3)"; filtros de Busca por Descrição + Ano + Origem (`st.selectbox`);
`st.data_editor` compacto (fonte 12px, mesmo truque de CSS por
`.st-key-<container>` já usado no 7.3.2) com checkbox "Selecionar p/
Fiscalização" na 1ª coluna, colunas Ano | Descrição Produto | Unid. |
Qtde | Valor Total | Origem travadas (`disabled`); botão "🎯 Cravar Alvos
Selecionados (7.3.3)" que extrai as linhas marcadas por índice
(`.reindex`, nunca por posição) e chama `salvar_alvos_selecionados_733()`.

### Documentação atualizada

- `DICIONARIO DE CAMPOS.txt`: nova seção "Novos campos - 2026-08-03" com
  `DESCR_PROD`/`UNID_PROD`/`QTDE`/`VALOR_TOTAL` (ver [[feedback_
  dicionario_campos_convencao]] — seção separada, sem dedupe; `ANO` já
  existia).
- `ESTAGIOS_PROJETO.md`: linha do Estágio 7 ganhou "7.3.3 implementado".

### Testado com dado real, backup/restore em toda gravação (ver [[feedback_snapshot_antes_de_testar_upsert]])

- **Geração do consolidado** (leitura, sem risco) nas 3 operações reais,
  direto via `loader` importado da própria pasta `ESSENCIAL/app` de cada
  operação (ver [[feedback_hunter_operacao_dir_banco_path]]): geraldo
  39.686 linhas (25.591 estoque / 7.923 saída / 6.172 entrada), cometa
  3.851 linhas, PB2 639 linhas — sem erros, ~0,14s na geraldo. Conferido
  caso real CERV SKOL LATA 350ML: valores de Compras/Vendas batendo com
  o que já era conhecido de sessões anteriores; Estoque com
  `VALOR_TOTAL` NULL em 100% das linhas, como esperado.
- **`salvar_alvos_selecionados_733()`** testado na geraldo com backup
  completo de `produto_alvo_fiscalizacao` antes (200 linhas — 7 ativos/
  193 cancelados) e restore ao final: selecionadas 3 descrições — 1 já
  ATIVA (CERV SKOL LATA 350ML), 1 CANCELADA (ALCOOL LIQ DA CASA 70º
  1LT), 1 NOVA (nunca salva, `#DES.ENLACE FEM.FRESH 150ML`). Resultado
  batendo exato com o desenho: SKOL preservado intocado (mesma
  Divergência/Total Débito de antes), ALCOOL reativado com RN1
  PRESERVADO (não zerado), produto novo cravado com RN1 zerado +
  Observação correta; `total_adicionado=1`, `total_reativado=1`,
  `200→201` linhas totais, `7→9` ativos. **Idempotência confirmada**: 2ª
  rodada com a mesma seleção devolveu `total_adicionado=0`, `total_
  reativado=0`, sem mudança. Backup restaurado ao final (200 linhas
  exatas de novo).
- **UI real** (Playwright, servidor de TESTE porta 8778, `iniciar_
  sistema` da geraldo — sessão real do usuário na porta 8600 confirmada
  intocada antes e depois, `netstat` + `tasklist`): painel 7.3.3
  renderizando corretamente logo abaixo do 7.3.2 (mesma página),
  status "39.686 linha(s) em `estagio733_consolidado`" (cache já
  persistido pelo teste direto acima), filtros de Busca/Ano/Origem
  funcionando (busca por "#DES.ENLACE FEM.FRESH" reduziu a 1 linha),
  botão "🎯 Cravar Alvos Selecionados" clicado sem nenhuma linha marcada
  devolveu corretamente o aviso "Nenhuma linha marcada" (sem gravar
  nada — reconferido por contagem: 200 antes e depois). Log do servidor
  de teste sem nenhum traceback/erro em nenhuma interação. Interação de
  marcar checkbox via clique de canvas (glide-data-grid) não foi
  finalizada por fragilidade de coordenada de pixel no teste automatizado
  — não é uma lacuna de cobertura real, já que o caminho de gravação
  (`salvar_alvos_selecionados_733`) foi validado de forma MAIS rigorosa
  direto por chamada de função (mesmo código que o botão chama).
  Servidor de teste encerrado ao final (`taskkill`), backup restaurado.

## Ajustes do 7.3.3, mesma sessão: Cód. Produto + remoção do cap de 200 linhas

Pedido curto, encadeado direto depois do painel entregue: **"inclua campo
cod de produto das declarações, traga 'nc' quando não houver. quando
ordenar pelo título, não ficar restrito às 200 primeiras e sim buscar no
todo."**

**Campo `COD_ITEM`** (`loader.gerar_consolidado_origens_733()`): mapeamento
por origem, reaproveitando decisões já documentadas em `_valores_por_
ano_item()` (2026-07-18) — Entradas usa `COD_ITEM_DECLARACAO`
(enriquecido via Matching/BC3, ~99,9% cobertura); Saídas usa
`fatoitemnfe_infnfe_det_prod_cprod` (não `COD_ITEM_DECLARACAO`, 98,8%
nulo ali — quando a auditada é EMITENTE, o próprio código do XML dela já
É a declaração); Estoque usa `COD_ITEM_DECLARACAO` de `estoque_anual_
consolidado` diretamente. Agregado via `STRING_AGG(DISTINCT ..., ', ')`
dentro de cada grupo (mesmo raciocínio de `_mapa_cod_item_por_descr_
alvo()`, raro mais de um código por descrição) — `'nc'` quando nenhuma
linha do grupo tinha código. **Achado real**: a base já tinha valores
como `"nd"`/`"nm"` como código LITERAL em alguns itens (não nulo/vazio) —
esses NÃO viram `'nc'` (só o vazio/nulo genuíno vira), comportamento
correto e esperado, confirmado no teste.

`salvar_alvos_selecionados_733()` também passou a propagar `COD_ITEM`
pra `produto_alvo_fiscalizacao` (antes sempre `""`) — usa o primeiro
valor diferente de `'nc'` entre as linhas selecionadas da mesma
descrição, senão `'nc'`.

**Remoção do cap de 200 linhas** (`interface.py`,
`render_consolidado_origens_733()`): a versão original limitava
`filtrado.head(200)` antes de montar o `st.data_editor` (mesmo padrão do
7.3.2) — como a ordenação por clique no cabeçalho da coluna (recurso
nativo do `st.data_editor`/glide-data-grid) só reordena o que já foi
carregado no frontend, ordenar por título só afetava as 200 primeiras
linhas do filtro, nunca o total. Trocado por `filtrado.copy()` (sem
cap) — decisão viável porque o grid é virtualizado (só renderiza linhas
visíveis na tela). Escopo: só o 7.3.3, o 7.3.2 não foi tocado (não
pedido).

**Gotcha real encontrado no teste**: `estagio733_consolidado` já estava
persistida (cache) de uma rodada anterior — mudar o schema da função
geradora sem reprocessar deixou a tabela cache com o schema VELHO (sem
`COD_ITEM`), gerando `KeyError` ao tentar selecionar a coluna nova.
Resolvido reprocessando (`persistir_consolidado_origens_733()`) antes de
testar — mesmo comportamento de qualquer outra tabela `_agrupado`/cache
deste app quando o gerador muda: sempre precisa "Regerar" depois de uma
mudança de schema, não é bug novo.

**Testado com dado real** (backup/restore de `produto_alvo_fiscalizacao`
de novo, mesmo cuidado): geraldo (39.686 linhas, 0,15% de `'nc'` em
Entradas, 0% em Saídas/Estoque — bate com os ~99,9%/100% de cobertura já
documentados), cometa (3.851 linhas, 3,3% `'nc'` em Entradas) e PB2 (639
linhas, 2,8% `'nc'` em Entradas) — sem erros nas 3. Cravado
"SKOL LATA 350ML SH C/12 NPAL" via função direta: `COD_ITEM=
"07891149200504"` gravado corretamente em `produto_alvo_fiscalizacao`,
idempotente na 2ª rodada. Backup restaurado (200 linhas exatas de novo).
**UI real** (Playwright, servidor de TESTE porta 8779, sessão real do
usuário na 8600 confirmada intocada): coluna "Cod. Produto" renderizando
com valores reais; busca "CERV SKOL LATA 350" reduziu corretamente a 12
linhas; SEM filtro nenhum, as 39.686 linhas carregaram no editor sem
erro (~5,5s incluindo navegação — grid virtualizado aguentou o volume
total, confirmando a decisão de remover o cap). Nenhum traceback no log
do servidor de teste. Servidor de teste encerrado ao final.

Compilado (`ast.parse`) e propagado idêntico nas 4 operações.

## BUG REAL corrigido: ordenar por Valor Total no 7.3.3 ordenava como texto, não número

Print anexado da tabela do 7.3.3 (filtro Origem=saída) mostrando "↓ Valor
Total" ordenado decrescente, mas com "LEITE PO NINHO INSTANTANEO 800G"
(999,60) e "PICOLE DE MORANGO 60G" (996,00) no topo. Reportado: **"porque
mostra leite como maior valor se LAMPADA FLORESCENTE EMPALUX 3U 15W tem
R$ 3.740,88"**.

**Causa raiz**: `render_consolidado_origens_733()` pré-formatava
`VALOR_TOTAL`/`QTDE` como texto BR (`_formatar_moeda_br()`, ex.
`"3.740,88"`) ANTES de montar o `st.data_editor` — mesmo padrão usado em
tabelas só-leitura do app (`st.dataframe`), mas aqui a coluna vira TEXTO
pro grid (glide-data-grid). A ordenação nativa por clique no cabeçalho
(recurso que o usuário já vinha usando desde o pedido anterior de "buscar
no todo") ordena STRING lexicograficamente: `"999,60"` fica acima de
`"3.740,88"` porque `"9" > "3"` como primeiro caractere — nada a ver com
o valor numérico real.

**Corrigido** (`interface.py`, `render_consolidado_origens_733()`):
removida a conversão pra texto; `Qtde`/`Valor Total` continuam como
`float` nativo no DataFrame passado ao `st.data_editor`, com
`column_config={"Qtde": st.column_config.NumberColumn(format="%.2f"),
"Valor Total": st.column_config.NumberColumn(format="%.2f")}` — MESMO
padrão já usado com sucesso em `_render_sumario_unidades_com_aplicar()`
("Valor Minimo"/"Valor Maximo", Estágio 9) pra coluna numérica editável/
ordenável. Perde a formatação BR (vira `3740.88`, ponto decimal, sem
separador de milhar — limitação conhecida do `NumberColumn`, já
documentada em `_formatar_moeda_br()`) — troca aceita porque o que
importa aqui é a ORDENAÇÃO funcionar de verdade, não só a aparência.

**Testado com dado real** (UI real, Playwright, servidor de TESTE porta
8780): busca "LAMPADA FLORESCENTE EMPALUX", clique no cabeçalho "Valor
Total" 2x (ordem ascendente → descendente, mesmo comportamento nativo
do grid) — resultado: "LAMPADA FLORESCENTE EMPALUX 3U 15W" (2021,
saída) no topo com `Valor Total = 3740.88`, batendo EXATO com o valor
reportado pelo usuário. Nenhum erro no log do servidor de teste.
Servidor encerrado ao final.

Compilado (`ast.parse`) e propagado idêntico nas 4 operações.

## 7.3.3 ganha botão/página próprios, separado do 7.3.2

Pedido curto: **"separe 7.3.3 do 7.3.2 criando botão próprio pra ele."**
— reverte o posicionamento original da Solicitação Técnica (7.3.3 logo
abaixo do 7.3.2, na mesma página), que já tinha virado alvo de correção
antes (bug de ordenação) e agora ganha independência total de navegação.

**Implementado** (`interface.py`):
- `render_pagina_rn1_simulada_30()` voltou a chamar só
  `render_rn1_simulada_30()` — removida a chamada a
  `render_consolidado_origens_733()` e o `st.divider()` que a precedia.
- Nova `render_pagina_consolidado_733()` — mesmo padrão de toda outra
  página do menu (`_botao_voltar_menu()` + checagem de
  `dados_carregados` + chamada à função de render) — chama só
  `render_consolidado_origens_733()`. Importante: NÃO depende de nenhum
  outro estágio 7.x já gerado (lê `estoque_entradas`/`estoque_saidas`/
  `estoque_anual_consolidado` direto, Estágios 4/5) — pode ser acessada
  a qualquer momento após a Extração, sem precisar passar por 7.2/7.3/
  7.3.1/7.3.2 primeiro.
- **Menu Principal**: novo botão "🔍 7.3.3: SELEÇÃO CONSOLIDADA
  (ESTOQUE/XML)" — entrou na 2ª linha do menu (que já começava no
  Estágio 8), na primeira posição, ANTES do Estágio 8 (ordem
  cronológica: 7.3.3 é anterior ao Estágio 8). Não entrou na 1ª linha
  (que já tinha 11 colunas — mesma razão que motivou a criação da 2ª
  linha em 2026-07-23, "12 colunas ficavam espremidas").
- `main.py`: novo roteamento `elif pagina == "consolidado_733":
  interface.render_pagina_consolidado_733()`; comentários/docstring de
  cabeçalho atualizados (13→14 grupos de painéis navegáveis).

**Testado com dado real da geraldo** (Playwright, servidor de TESTE
porta 8781): confirmado visualmente o novo botão "🔍 7.3.3: SELEÇÃO
CONSOLIDADA (ESTOQUE/XML)" na 2ª linha do Menu Principal, antes de
"ESTÁGIO 8"; confirmado que a página do 7.3.2 NÃO mostra mais o 7.3.3
embutido (texto "7.3.3: Seleção Consolidada" ausente depois de clicar
"7.3.2: SIMULAÇÃO RN1"); confirmado que a página própria do 7.3.3 (via
o botão novo) carrega o painel completo (39.686 linhas, filtros,
tabela) igual a antes, só que isolada. Nenhum erro no log do servidor
de teste. Servidor de teste encerrado ao final — nada gravado no banco
nesta rodada (só navegação/leitura).

Compilado (`ast.parse`) e propagado idêntico nas 4 operações
(`interface.py` e `main.py`).

## Estágio 7.1 ganha Unidade Relevante (UNID_ALVO) — Enriquecimento da Identidade do Produto

Solicitação Técnica detalhada e completa anexada pelo usuário (com
prompt pronto pro agente) — resumo: `produto_alvo` (Estágio 7.1) já
elege a Descrição Relevante (moda por COD_ITEM entre entradas/saídas/
estoque); o pedido é acrescentar, junto, a Unidade de Medida Relevante
— serve de "unidade de destino" pro Fator Multiplicador (Estágios 9/10)
e de referência pra não somar quantidades em unidades diferentes sem
alerta na RN1 (Estágio 15, ainda não implementado).

**Ambiguidade real encontrada antes de codar** (mesmo princípio de
[[feedback_anexo_nao_corresponde]]/[[feedback_regra_fiscal_e_do_usuario]]
— parar e perguntar quando a especificação admite mais de uma leitura
válida com impacto fiscal real): o texto usa duas frases conflitantes —
"o sistema agora elegerá o PAR vencedor: Descrição e Unidade (maior
contagem estatística COMBINADA)" (sugere agrupar por (COD_ITEM, DESCR,
UNIDADE) junto) vs. "identifique a unidade mais frequente, seguindo o
MESMO RIGOR aplicado às descrições" (sugere moda independente, própria).
A leitura "par combinado" mudaria a fórmula já usada pra eleger
DESCR_ALVO hoje (uma descrição espalhada em 2 unidades poderia perder
pra uma descrição diferente concentrada numa unidade só) — risco real
de regressão silenciosa em TODOS os cálculos que já dependem de
DESCR_ALVO ficar exatamente como está (Estágio 7.2/7.2.1/7.3/7.3.1/
7.3.2/7.3.3, todos usados/validados há semanas). A especificação também
usa 3 nomes diferentes pra mesma coluna nova (`UNID_ALVO`/
`unid_relevante`/`UNID_RELEVANTE`) ao longo do texto.

Perguntado ao usuário via `AskUserQuestion` (2 perguntas):
- **Moda da unidade**: calculada de forma INDEPENDENTE da descrição
  (mesma técnica de sempre, mas contagem PRÓPRIA — não é o par
  combinado) — confirmado; zero mudança na eleição de DESCR_ALVO já
  validada.
- **Nome da coluna**: `UNID_ALVO` — confirmado, mantém o padrão já
  usado na mesma tabela (`COD_ITEM`, `DESCR_ALVO` — sufixo "_ALVO",
  maiúsculo).

**Implementado** (`loader.py`, `montar_produto_alvo()`):
`_COLUNAS_PRODUTO_ALVO` ganhou `UNID_ALVO`; nova constante
`_COLUNA_UNIDADE_POR_TABELA_PRODUTO_ALVO` — mapeia `fatoitemnfe_infnfe_
det_prod_ucom` (Entradas/Saídas) e `UNIDADE` (Estoque, Bloco H), mesmo
mapeamento de campo já usado no consolidado do 7.3.3. A união (UNION
ALL) das 3 fontes passou a trazer também a coluna UNID; a partir dela,
2ª agregação PARALELA e INDEPENDENTE da de DESCR — `GROUP BY (COD_ITEM,
UNID)`, mesma técnica de moda (normaliza código, reagrupa, ordena por
frequência desc + unidade alfabética asc como desempate, `first()` por
grupo) — depois mesclada (`merge how="left"`) no resultado já eleito de
DESCR_ALVO. Linhas com unidade nula/vazia excluídas da contagem; produto
sem nenhuma unidade válida em nenhuma das 3 fontes fica com
`UNID_ALVO=""`. `persistir_produto_alvo()` não precisou de nenhuma
mudança (já persiste `SELECT *` do que `montar_produto_alvo()` devolver).

**Implementado** (`interface.py`): `_COLUNAS_PREVIEW_PRODUTO_ALVO`
ganhou `UNID_ALVO`; caption do painel atualizada pra mencionar a
unidade. `DICIONARIO DE CAMPOS.txt`: nova entrada `UNID_ALVO;Unidade
Relevante`.

**Testado com dado real nas 3 operações** (direto via `loader` de cada
pasta `ESSENCIAL/app`, leitura + persistência — tabela `produto_alvo` é
derivada/determinística, sem dado manual do auditor, mesmo tratamento
de outras tabelas cache, sem precisar de backup/restore): geraldo 6.138
produtos (MESMA contagem de antes — confirma que DESCR_ALVO não mudou
nada), 0% de `UNID_ALVO` vazio; cometa 2.513 produtos, PB2 269 produtos
— ambos também com as MESMAS contagens de sempre e 0% vazio. Conferido
caso real: CERV SKOL LATA 350ML (COD_ITEM `7891149200504`) elegeu
`UNID_ALVO="cx12"` — consistente com o achado de sessões anteriores de
que a maioria das ocorrências físicas desse código vêm etiquetadas
"SKOL LATA 350ML SH C/12 NPAL" (embalagem cx12), não "CERV SKOL LATA
350ML" (unidade solta). Regressão checada: `gerar_cruzamento_valor()`
(Estágio 7.2, consumidor de `produto_alvo`) rodado depois da mudança —
mesmo total de linhas de sempre (17.916), sem erro. **UI real**
(Playwright, servidor de TESTE porta 8782): coluna "Unidade Relevante"
renderizando corretamente ao lado de "Descricao Relevante" no painel
"🏷️ DESCRIÇÃO RELEVANTE", valores batendo com o teste direto (Un/UN/PC/
FD conforme o produto). Nenhum erro no log do servidor de teste.

Compilado (`ast.parse`) e propagado idêntico nas 4 operações.

## Ver também

- [[feedback_anexo_nao_corresponde]] — por que parei pra perguntar antes
  de codar, ao achar 3 pontos onde a Solicitação Técnica não batia com o
  schema real
- [[feedback_confirmar_antes_regenerar_dados]]/[[feedback_snapshot_antes_de_testar_upsert]]
  — por que todo teste de gravação usou backup/restore
- [[feedback_hunter_operacao_dir_banco_path]] — por que os testes diretos
  importaram `loader` da pasta `ESSENCIAL/app` de cada operação
- [[feedback_sync_operacoes]] — por que o código foi replicado idêntico
  nas 4 operações (`geraldo_2020_2024`, `PB_2023_2025`, `cometa_2021_2025`,
  `_MODELO_OPERACAO`)
- [[feedback_dicionario_campos_convencao]] — como os campos novos foram
  adicionados ao dicionário
- `memoria/2026-07-22.md` — histórico completo de `produto_alvo_
  fiscalizacao` (Estágio 7.3.2), a tabela real que o 7.3.3 agora também
  alimenta
