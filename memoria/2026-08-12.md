# Atualização 2026-08-12

> **Nota**: registro reconstruído em 2026-08-13 a partir das mensagens dos
> commits reais do dia (fonte contemporânea confiável) — o arquivo diário não
> foi gerado no próprio dia, gap do mesmo tipo já documentado em sessões
> anteriores (ver `[[feedback-memoria-agents-md-disciplina]]`).

## Resumo Executivo

Dois commits no dia: (1) correção de um falso alerta de cobertura de Estoque
no Estágio 1 e reorganização dos KPIs do painel de Matching (BC3); (2) nova
seção "Totais por Origem" no Estágio 7.3.3 (Seleção Consolidada) — 3 tabelas
lado a lado (Entradas/Saídas/Estoque) agregadas por Descrição, dentro do
mesmo filtro de Busca/Ano/Origem já aplicado na tela principal, com linha de
TOTAL. Ambos implementados e propagados para as 4 operações
(`geraldo_2020_2024`/`PB_2023_2025`/`cometa_2021_2025`/`_MODELO_OPERACAO`).

---

## Funcionalidades Implementadas

- **`fdeff19` — Cobertura de Estoque e KPIs do Matching**:
  - Denominador da checagem de cobertura de Estoque (H010, `loader.
    verificar_cobertura_granular()`) corrigido para exigir exatamente
    `ano_inicial..ano_final` — antes somava +1 ao intervalo (regra que só se
    aplica à Declaração), gerando um falso alerta de "ano faltante" mesmo com
    o período de auditoria já completo.
  - KPIs do painel de Matching (BC3, Estágio "PAINÉIS EM CONSTRUÇÃO") tiveram
    a fonte reduzida e foram divididos em 2 linhas de 7 colunas — a linha
    única de 14 colunas estava truncando rótulos e valores.
- **`c1784ba` — Totais por Origem (Estágio 7.3.3)**:
  - Abaixo da grade consolidada principal, 3 tabelas lado a lado (Descrição/
    Qtde/Valor Total), agregadas só por Descrição (soma todos os anos/
    unidades dentro do filtro já aplicado — Busca/Ano/Origem, os mesmos 3
    filtros da tabela principal), com linha de TOTAL. Fonte reduzida (9px)
    pra caber as 3 colunas lado a lado. Valor Total do Estoque fica em branco
    (`sum(min_count=1)` devolve NaN quando o grupo inteiro é nulo — Bloco H
    não tem valor nessa granularidade, mesma regra já documentada na tabela
    principal do 7.3.3).

## Correções

- Falso alerta de cobertura de Estoque (ver acima) — achado real ao validar
  contra o banco da geraldo, período de auditoria já fechado sendo
  reportado como incompleto.

## Pendência identificada nesta reconstrução (não fazia parte dos commits)

- Uma correção de busca por curinga (`_padrao_busca_curinga()` — usuário
  digitou `"*bol*jutucut*"` esperando `LIKE` estilo Qlik/SQL e o app
  estourava `pyarrow.lib.ArrowInvalid`, já que `"*"` sozinho não é regex
  válida) ficou implementada no working tree nesse mesmo dia (docstring
  datada "2026-08-12") mas **não foi commitada** — permaneceu pendente até
  ser fechada junto com o trabalho de 2026-08-13 (ver `memoria/2026-08-13.md`).

## Arquivos Modificados

- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/interface.py`
- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/loader.py`

## Próximos Passos

- Nenhum além do backlog geral do projeto (Estágio 15 — Fator
  Multiplicador/omissão, ainda não implementado).
