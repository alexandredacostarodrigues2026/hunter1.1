# Atualização 2026-08-20

## Resumo Executivo

Dois blocos de trabalho, ambos no Estágio 10/10.1. **Bloco 1** (manhã):
ajustes pontuais de rótulo nos Critérios de busca ("Produtos Alvos
Salvos"), a pedido do usuário: **"em 10, nos critérios 3 e 4, trocar
nome_prod_decl do alvo por nome_prod_ do alvo"** + 2 pedidos seguintes
no mesmo fio. **Bloco 2** (tarde): feature grande e nova — **Painel de
Revisão da Equalização Automática** — o usuário mandou uma Solicitação
Técnica completa (formato de ticket formal) pedindo uma camada de
"rascunho" entre a Equalização Automática e a Rubrica oficial, pra
nenhuma correspondência entrar sem revisão humana. Trabalho conduzido
via modo de planejamento (pesquisa da arquitetura existente + 2 rodadas
de perguntas de esclarecimento antes de codificar), dado o tamanho e o
risco fiscal da mudança.

**Glossário rápido**: *alvo* = produto que o auditor está fiscalizando
(campo `DESCR_ALVO`); *nome_prod_decl* = nome que a AUDITADA usa na
própria declaração/Matching pra um item (campo `descrição_decl`) —
distinto do nome do alvo, que não vem necessariamente de uma
"declaração" (pode ter sido editado manualmente no Estágio 10, ou
vir da Descrição Relevante do Estágio 7.1); *Rubrica* = conjunto de
itens de Entradas/Saídas/Estoque que o auditor confirma como
pertencentes ao produto alvo, gravado em `cruzamento_confirmado`/
`cruzamento_confirmado_detalhado`; *rascunho/provisório* = proposta
gerada pela automação, ainda NÃO é Rubrica oficial, gravada em
`cruzamento_provisorio`/`cruzamento_provisorio_detalhado` (tabelas
novas, ver Bloco 2); *homologar* = ação do auditor que promove um item
do rascunho pra Rubrica oficial; *FM (Fator Multiplicador)* = conversor
de unidade de embalagem (ex.: "1 caixa = 12 unidades"), ajustado numa
tela separada ("📊 Diagnóstico de Unidades") que este trabalho NÃO
tocou.

## Bloco 1 — Ajustes de rótulo (manhã)

## Correções Realizadas

- **Rótulos dos Critérios 3 e 4** (constantes `CRITERIO_BUSCA3_NOME_
  DECLARACAO_IGUAL` e `CRITERIO_BUSCA4_NOME_XML`, `loader.py`) — lado
  do ALVO deixou de dizer "nome_prod_decl do alvo" e passou a dizer
  "nome_prod do alvo" (tirado o sufixo "_decl", que sugeria vir de uma
  declaração — o nome do alvo não vem necessariamente daí). Lado do
  CANDIDATO manteve "nome_prod_decl do candidato" sem mudança (esse
  sim vem de fato do campo `descrição_decl`):
  - Critério 3: "nome_prod do alvo = nome_prod_decl do candidato"
  - Critério 4: "nome_prod do alvo = nome_prod_xml"

  Como essas duas constantes são COMPARTILHADAS entre as abas Entradas/
  Saídas/Estoque (mesmo padrão já documentado em 2026-08-19), o novo
  rótulo aparece automaticamente nas 3 abas onde cada critério existe
  — Critério 3 (Entradas/Estoque, Saídas não tem) e Critério 4
  (Entradas/Saídas, Estoque não tem).

- **Rótulos dos Critérios 1 e 2** (constantes `CRITERIO_BUSCA1_EAN` e
  `CRITERIO_BUSCA2_MESMO_CODIGO`, `loader.py`), mesmo dia, pedido em
  seguida — padrão INVERTIDO em relação aos Critérios 3/4 acima: no
  Critério 1 (EAN) quem perde o sufixo "_decl" é o lado do ALVO; no
  Critério 2 (código) quem perde é o lado do CANDIDATO:
  - Critério 1: "EAN_prod do alvo = EAN_prod_decl do candidato" (era
    "EAN_prod_decl do alvo = EAN_prod_decl do candidato")
  - Critério 2: "cod_prod_decl do alvo = cod_prod do candidato" (era
    "cod_prod_decl do alvo = cod_prod_decl do candidato")

  Aplicado literalmente conforme pedido do usuário, sem alterar
  nenhuma lógica de comparação/matching — só o texto exibido no
  selectbox "Critério de busca".

- **Rótulo do Critério 5** (constante `CRITERIO_BUSCA5_PESQUISA_
  LIVRE`, `loader.py`), mesmo dia — acrescentado "nome_prod do alvo ="
  antes do texto já existente: "Critério de Busca5: nome_prod do alvo
  = pesquisa livre (sem filtro de código/similaridade)" (era só
  "Critério de Busca5: pesquisa livre (sem filtro de código/
  similaridade)"). Só texto do rótulo — a pesquisa livre continua
  SEM filtro nenhum de fato (nem código, nem nome, nem similaridade);
  o texto novo só nomeia o campo que o auditor usa pra buscar
  manualmente.

- **"=" trocado por "X" em TODOS os 5 rótulos** (mesmo dia, último
  pedido): o sinal de igual entre "do alvo" e "do candidato"/"pesquisa
  livre" virou a letra "X" em todas as 5 constantes `CRITERIO_BUSCA*`.
  Ex.: "EAN_prod do alvo = EAN_prod_decl do candidato" virou "EAN_prod
  do alvo X EAN_prod_decl do candidato". Só o caractere entre os dois
  lados — o "Critério de Busca1:" no início de cada rótulo continua
  com dois-pontos, sem mudança (o "=" trocado é só o que aparecia
  DEPOIS do "do alvo"). `sufixo_criterio` (extrai "1"/"2"/.../"5" do
  texto do rótulo pra montar `key` de widgets únicos por critério, em
  `interface.py`) usa `split(":", 1)` — não foi afetado, já que o
  primeiro ":" continua no mesmo lugar.

### Testes (Bloco 1)

- `python -m py_compile` nos 4 `loader.py` após cada edição — sem
  erros.
- `diff -q` confirmando os 4 arquivos byte-idênticos entre si depois
  de cada propagação (mesmo fluxo de sincronização de 2026-08-19:
  edita em `geraldo_2020_2024`/`PB_2023_2025`, copia pros outros 2).

## Bloco 2 — Painel de Revisão da Equalização Automática (tarde)

Solicitação Técnica completa do usuário (formato de ticket): hoje o
botão "⚡ Equalização Automática" (tela "Cruzamento de Produtos") roda
os critérios automáticos e grava DIRETO na Rubrica oficial — sem
revisão humana — e, em seguida, aplica automaticamente o Fator
Multiplicador (FM) a tudo que confirmou. Pedido: introduzir uma camada
de rascunho, com um painel de revisão pra o auditor aprovar/rejeitar
antes de qualquer coisa virar Rubrica oficial.

**Processo de planejamento** (`EnterPlanMode`, dado o tamanho/risco):
1. Agente de exploração mapeou em detalhe a arquitetura existente:
   `executar_confirmacao_automatica_rubrica()`, `executar_aplicacao_
   automatica_fm()`, `gerar_sumario_unidades_alvo_com_destaque()`,
   `salvar_cruzamento_confirmado()`/`_detalhado()`, a tela manual
   "📊 Diagnóstico de Unidades" (`_render_sumario_unidades_com_
   aplicar()`), a Regra R07, e confirmou que NÃO existe nenhum padrão
   de tabela "rascunho/provisório/draft" em nenhum lugar do app hoje —
   seria uma introdução nova.
2. **Achado-chave da exploração**: "FM Sugerido" hoje é uma
   propriedade calculada por GRUPO DE UNIDADE (UP, ex. "CX12"), não
   por correspondência (código+descrição) — e "Nova Unidade" nunca
   teve sugestão real nenhuma, sempre um placeholder "UNID" editável
   manualmente. Isso criava um conflito real entre o pedido original
   (colunas "FM Sugerido"/"Nova UP" editáveis JUNTO com "Aprovar" na
   mesma grade de correspondência) e a arquitetura existente.
3. 2 rodadas de `AskUserQuestion` resolveram os pontos em aberto:
   - O painel fica na tela **"Cruzamento de Produtos"** (não volta pro
     Estágio 10, apesar do texto original mencionar "Estágio 10" —
     a tela já foi separada em 2026-08-19).
   - O botão **"💾 Homologar" fica DENTRO de cada expander de
     Critério** (não um botão só por aba).
   - **Decisão final, mudando o desenho**: "a revisão na verdade é
     somente para a escolha do candidato. o diagnóstico de unidade
     permanece onde está" — ou seja, a grade do painel NÃO tem FM/Nova
     Unidade; só decide QUAIS correspondências entram na Rubrica. O
     ajuste de FM continua 100% na tela manual já existente, sem
     nenhuma mudança nela — simplificou MUITO o escopo (nenhuma
     reengenharia de cálculo de FM foi necessária).

### Funcionalidades Implementadas

1. **2 tabelas novas** (`loader.py`): `cruzamento_provisorio` (mesmo
   schema de `cruzamento_confirmado` + `SIMILARIDADE_DESCRICAO`, que
   precisa sobreviver até a revisão pra mostrar "SimDescr%" na grade)
   e `cruzamento_provisorio_detalhado` (schema idêntico ao `_detalhado`
   real) — mesmo padrão de toda tabela do app (`CREATE OR REPLACE
   TABLE`, Regra R07, criação lazy no 1º write).
2. **Funções novas** (`loader.py`): `salvar_cruzamento_provisorio()`/
   `_detalhado()` (grava rascunho, sem upsert — sempre regerado do
   zero a cada rodada de automação), `consultar_cruzamento_
   provisorio()`/`_detalhado()` (leitura filtrável, mesmo padrão de
   `consultar_cruzamento_confirmado()`), `limpar_rascunho_
   provisorio(descr_alvo)` (apaga TODO o rascunho de um produto —
   usada no início de cada rodada de automação E pelo botão
   "❌ Cancelar Propostas Automáticas"), `gravar_rubricas_
   revisadas(escolhido, aprovados_df, origem, criterio)` (homologa um
   lote de um critério: promove os aprovados pra Rubrica oficial via
   `salvar_cruzamento_confirmado()`/`_detalhado()` — **funções JÁ
   EXISTENTES, sem nenhuma alteração**, só chamadas em outro momento —
   e descarta do rascunho tanto aprovados quanto reprovados daquele
   critério), `_remover_rascunho_por_criterio()` (helper interno).
3. **`executar_confirmacao_automatica_rubrica()` refatorada**: mesmo
   `plano` de 10 passos (4 Entradas + 3 Saídas + 3 Estoque, sem mudança
   nos critérios), mesmo piso de 60% de similaridade — só troca o
   destino da gravação (`salvar_cruzamento_provisorio()`/`_detalhado()`
   em vez de `salvar_cruzamento_confirmado()`/`_detalhado()`) e chama
   `limpar_rascunho_provisorio(descr_alvo)` no início, pra descartar
   rascunho de rodada anterior.
4. **`executar_aplicacao_automatica_fm()` deixou de ser chamada
   automaticamente** pelo botão ⚡ (função em si NÃO foi alterada, só
   parou de ser invocada nesse fluxo) — consequência direta da decisão
   "o diagnóstico de unidade permanece onde está": como nada fica
   confirmado antes da homologação, não haveria mais o que essa
   automação processasse nesse ponto do fluxo mesmo.
5. **Novo painel `_render_painel_revisao_equalizacao()`**
   (`interface.py`), na tela "Cruzamento de Produtos", logo abaixo do
   bloco do botão ⚡: visibilidade calculada AO VIVO (sem flag de
   `session_state` — só aparece se houver rascunho pendente pro
   produto, sobrevive a reruns/reabertura sozinho). Estrutura: abas
   "📥 Revisar Entradas / 📤 Revisar Saídas / 📦 Revisar Estoque" →
   dentro de cada uma, um `st.expander` colapsado por Critério com
   sugestão (ex.: "Critério de Busca1 — 3 item(ns) pendente(s)") →
   dentro, `st.data_editor` (fonte 8px via `_aplicar_fonte_
   dataframe()`, `row_height=70` pra quebra de texto — mesmo padrão já
   usado na tabela manual de correspondências) com Aprovar (checkbox,
   default True) / Cód. Produto / Descrição XML / SimDescr% — e um
   botão "💾 Homologar" PRÓPRIO dentro de cada expander. Um único botão
   "❌ Cancelar Propostas Automáticas" pro painel inteiro (descarta todo
   o rascunho do produto).
6. Botão ⚡ simplificado: só 1 barra de progresso agora (era 2 —
   confirmação + FM); `help=` do botão atualizado; mensagem de sucesso
   passou de "itens adicionados à Rubrica" pra "propostas geradas pra
   revisão".

### Alterações de Arquitetura

Primeira introdução do padrão "rascunho → revisão → promoção" no app
(não existia NENHUM precedente antes) — a garantia central: a Rubrica
oficial (`cruzamento_confirmado`/`_detalhado`) só muda quando o
auditor clica "💾 Homologar" (ou pelo fluxo manual já existente,
inalterado); a Equalização Automática, sozinha, NUNCA mais escreve lá
direto.

### Testes Realizados (Bloco 2)

- `python -m py_compile` nos 4 pares `loader.py`/`interface.py`.
- **Teste central de governança** (via runtime real da geraldo, dados
  reais, produto CERV SKOL LATA 350ML): snapshot de `cruzamento_
  confirmado`(26 linhas)/`_detalhado`(763 linhas) ANTES de rodar a
  automação; rodou Equalização Automática via `AppTest` — as duas
  tabelas reais continuaram EXATAMENTE 26/763 depois (nem uma linha
  mudou), enquanto `cruzamento_provisorio` recebeu 10 linhas
  (agrupadas) e `_detalhado` 224 (itens individuais) — confirma que a
  automação de fato parou de escrever direto na Rubrica.
- Testou `gravar_rubricas_revisadas()` direto (Python): homologação
  parcial (aprova 1 de 1 correspondência do Critério EAN/Entradas) —
  linha promovida corretamente pra `cruzamento_confirmado` com o
  `CRITERIO` certo, rascunho daquele critério zerado, OUTROS critérios/
  origens do mesmo produto continuaram intocados; homologação com TUDO
  reprovado (`aprovados_df` vazio) — `total_homologado=0`,
  `total_descartado=1`, rascunho igualmente zerado, sem erro.
- Testou `limpar_rascunho_provisorio()` ("❌ Cancelar") — zera as duas
  tabelas de rascunho pro produto, sem tocar na Rubrica real.
- Screenshot real via CDP (headless Edge) confirmando visualmente o
  painel completo — 4 expanders "Critério de Busca1-4" na aba Entradas,
  grade com fonte pequena/Aprovar marcado por padrão, botão Homologar
  dentro do expander, botão Cancelar no fim do painel; achado do
  screenshot: coluna "Aprovar" cortando o texto no cabeçalho (width=70
  insuficiente) — corrigido pra width=85.
- Sanity-check contra o runtime/banco do PB_2023_2025 (menor, sem
  rascunho ainda) — funções novas não quebram com tabela/produto
  inexistente.
- **Toda escrita real feita durante o teste foi revertida** ao final:
  `cruzamento_confirmado`/`_detalhado` restaurados byte-a-byte a partir
  de snapshot CSV tirado antes do teste; `cruzamento_provisorio`/
  `_detalhado` zerados (`limpar_rascunho_provisorio`) — nenhum dado de
  produção ficou alterado por causa dos testes desta sessão.
- Streamlit/launcher de teste (porta 8602) e Edge headless (porta
  9334) encerrados ao final.

### Pendências

- Nenhuma.

### Próximos Passos

- Nenhum solicitado.

## Bloco 3 — remoção das legendas explicativas de Saídas/Estoque

Print do usuário mostrando a legenda explicativa do Critério (ex.:
"Combinações em `estagio8_estoque_agrupado`... EAN vem do Registro
0200... placeholder/vazio nunca conta como igual...") + pedido:
**"retirar isso em todos as tabelas"**. Essas legendas (`st.caption()`
por critério, um `if/elif/elif/else` inteiro antes da busca) já tinham
sido retiradas de Entradas em 2026-08-18 ("retirar o texto") — Saídas e
Estoque continuavam com elas (inconsistência já registrada em
2026-08-19). O badge "🏷️ ... é ST" foi mantido (informação útil,
diferente da legenda técnica).

### Correções Realizadas

- Removido o bloco inteiro de `st.caption()` por critério em
  `_render_cruzamento_saidas()` e `_render_cruzamento_estoque()`
  (`interface.py`) — igualando as 3 abas (Entradas já não tinha desde
  2026-08-18).
- `unid_efetiva = loader.unidade_efetiva_escolhido(escolhido)` removido
  das duas funções (só era usado dentro das legendas apagadas) —
  `loader.unidade_efetiva_escolhido()` continua existindo/usada em
  outros lugares (Relatórios), só as chamadas locais órfãs saíram.
  Docstrings das duas funções atualizadas (não mencionam mais
  `unid_efetiva`).

### Testes Realizados

- `python -m py_compile` nos 4 `interface.py`.
- `AppTest` reexecutado (mesmo script do Bloco 2/EAN) contra o banco
  real da geraldo — Entradas/Saídas/Estoque com Critério 1 (EAN)
  selecionado, 0 exceções, mensagens de sucesso normais.
- `diff -q` confirmando os 4 arquivos idênticos após propagação.

### Pendências

- Nenhuma.

### Próximos Passos

- Nenhum solicitado.
