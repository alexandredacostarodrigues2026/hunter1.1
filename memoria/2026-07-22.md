# Atualização 2026-07-22

## Achado: a lógica de PU/omissão da RN1 original existe — dentro de um Access

Pedido do usuário na mesma sessão: "o 7.3.2 produto será o painel para escolha
do produto alvo para que vc possa implantar o que fi feito em
ANTIGO_geraldo_2020_2024_5. consegue fazer essa ponte?" — entendi (errado, no
primeiro momento) que era sobre portar a lógica de PU (Preço Unitário)/omissão
do Estágio 15 (ver `regra de negócios unificadas/regra negocio_pu_rn1_ei+c=v+ef_1.txt`
e `ESTAGIOS_PROJETO.md` linha do Estágio 15).

Busquei em TODO `ANTIGO_geraldo_2020_2024_5` (.py, .txt, .md, .ipynb, inclusive
`_SNAPSHOTS`) por essa fórmula — zero ocorrências em código Python/texto.
Usuário reagiu com ceticismo ("não há código?") e isso me levou a insistir na
busca — **achado real**: a lógica existe de verdade, mas dentro de
`ANTIGO_geraldo_2020_2024_5/5-HUNTER/HUNTER 4.37_GERALDO2.accdb` (banco Access
de 47MB — a ferramenta "Hunter" ORIGINAL que dá nome ao projeto, anterior à
reescrita em Python). Consegui ler o SQL das queries salvas via COM/DAO
(`win32com.client.Dispatch("DAO.DBEngine.120")`, `db.QueryDefs(nome).SQL` —
pyodbc/ODBC não expõe o SQL de queries Access, só win32com/DAO consegue).

**A query `GRUPO2_E X S (COPIAR)` implementa exatamente a regra do .txt**,
rodando sobre QUANTIDADE (não R$) de um produto tagueado manualmente (form
`TAG_E_S`), por ANO: `EI`/`EF` = quantidade de estoque; `E1`/`S1` = quantidade
cruzada de Compras/Vendas. Condição `(Vendas+EF)−(EI+Compras) > 0` →
**"ENTRADAS SEM NF"** (confirma que a convenção de `INFRACAO` que já
escolhemos no 7.3.1/7.3.2 — `TD<TC` → Entradas sem NF — bate com a ferramenta
original, não foi engano). PU: compras médio se `c>0`, senão Vendas−30%;
Vendas se `v>0`, senão Compras+30% — bate 100% com o texto. Ainda vai além:
calcula ICMS (17%/18%/20% conforme o ano) + Multa (100% do ICMS) — um cálculo
de auto de infração completo que a versão nova nem começou.

**Gap real pra um Estágio 15 de verdade**: tudo isso exige quantidade (não
R$) de EI/Compras/Vendas/EF por produto/ano — `rn1_produto`/`rn1_fisica` só
têm valor. O dado bruto (`fatoitemnfe_infnfe_det_prod_qcom`, Bloco H
`QUANTIDADE_INICIAL`/`QUANTIDADE_FINAL`) já é extraído, mas nunca foi somado
nessa estrutura — nem o app antigo tinha isso pronto por posição (só QT
agregada solta em `ranking.py`/`utils.consolidar_produtos()`).

**Só depois disso o usuário corrigiu o pedido real**: nada de PU/ICMS — é
sobre a MECÂNICA de escolha de produto alvo (`ranking.py`: checkbox "Escolher"
+ botão "Salvar Produto Alvo" + `registrar_produto_eleito()`), aplicada em
cima dos dados do 7.3.2 em vez da tabela de ranking bruta antiga. Ver seção
abaixo pelo que foi de fato implementado. **O achado do Access fica registrado
aqui pra quando o Estágio 15 de verdade for retomado** — não desperdiçar essa
descoberta; a fórmula e as alíquotas por ano já estão decodificadas acima.

## Estágio 7.3.2 — Simulação RN1 (+30%)

Solicitação Técnica: criar um cenário simulado da RN1 por Produto
(Estágio 7.3.1), majorando em 30% os valores que representam "custo"/
"estoque" — Estoque Inicial (EI), Compras e Estoque Final (EF) — pra
testar se uma eventual subvaloração de 30% nessas contas explicaria as
divergências encontradas, ou se o risco fiscal permanece estrutural
mesmo com os valores majorados. Vendas permanece o valor físico real do
XML, sem acréscimo, servindo de âncora de confronto (nome dado pelo
usuário à referência fixa contra a qual os totais majorados são
comparados).

**Ponto de atenção resolvido antes de implementar**: o texto original do
pedido descrevia a regra de `INFRACAO` invertida em relação à já usada em
7.2/7.2.1/7.3/7.3.1 (`TD < TC` → "Saídas sem NF" no texto do pedido, vs.
"Entradas sem NF" no código já validado). Perguntei ao usuário antes de
implementar (ver [[feedback_regra_fiscal_e_do_usuario]]) — confirmado
manter a convenção já existente (provável erro de digitação ao descrever
de novo a mesma regra, não uma mudança fiscal intencional).

**Lógica implementada** (`loader.py`, base: `rn1_produto`, Estágio 7.3.1,
já condensado por Descrição Relevante — não reprocessa entradas/saídas do
zero):
- `gerar_rn1_simulada_30()`: lê `rn1_produto`; `EI_sim = EI × 1.3`,
  `COMPRAS_sim = COMPRAS × 1.3`, `EF_sim = EF × 1.3`; `VENDAS` inalterado;
  `TOTAL_DEBITO = EI_sim + COMPRAS_sim`; `TOTAL_CREDITO = VENDAS + EF_sim`;
  `DIVERGENCIA = |TD − TC|`; `INFRACAO` = "Entradas sem NF" se
  `TD − TC < 0`, senão "Saídas sem NF" (mesma regra do 7.3.1); `PCT_
  DIVERGENCIA = DIVERGENCIA / min(TD,TC) × 100` (proteção 0.00001 se
  mínimo for zero). Ordenado por `DIVERGENCIA` decrescente.
- `persistir_rn1_simulada_30()` / `rn1_simulada_30_ja_gerado()` /
  `consultar_rn1_simulada_30()`: mesmo padrão gerar/persistir/consultar
  de `rn1_produto` — tabela `rn1_simulada_30` no DuckDB da operação.

**Interface** (`interface.py`): 11º botão no Menu Principal, "📈 7.3.2:
SIMULAÇÃO RN1 (+30%)" (`col11`, `st.columns(11)`), `render_pagina_rn1_
simulada_30()` → `render_rn1_simulada_30()`. Painel agrupado por
Descrição Relevante, mesmo padrão de alta densidade (fonte 12px, hide_
index, formatação de milhar/moeda BR, ordenação decrescente por
Divergência) do 7.3.1. Cabeçalhos das colunas majoradas trocados de
"EI (R$)"/"Compras (R$)"/"EF (R$)" para "EI (+30%)"/"Compras (+30%)"/
"EF (+30%)" (só na camada de exibição, via `_preparar_preview_rn1_
simulada_30()` — a tabela persistida usa os mesmos nomes de coluna EI/
COMPRAS/EF do 7.3.1, sem alterar `DICIONARIO DE CAMPOS.txt`, mesma técnica
ad hoc já usada pra "Compras (XML)"/"Vendas (XML)" no 7.3.1).

**Drill-down "explode os anos" por produto** (pedido do usuário na mesma
sessão, depois de ver o painel pronto): ao clicar num produto (ex.:
"REQUEIJÃO ISIS CREMOSO 220GR"), mostra o detalhamento ano a ano — mesmo
padrão do 7.3.1, mas a simulação +30% é recalculada linha a linha por ANO
(`loader.simular_rn1_fisica_30(descr_alvo)`, sobre `rn1_fisica`), não só
exibindo o detalhe real sem simular. Extraída a lógica de simulação para
`loader._aplicar_simulacao_30(df)` (helper compartilhado por
`gerar_rn1_simulada_30()`, nível produto, e `simular_rn1_fisica_30()`,
nível ano) — mesma fórmula, granularidade diferente. Conferido que a soma
de Total Débito/Crédito de todas as linhas anuais bate exatamente com o
total agregado da linha do produto (521,40 / 11.011,67 na geraldo) —
mesmo princípio de coerência TD/TC já usado no drill-down do 7.3.1
(Divergência anual pode não somar à líquida, é esperado, ver
`memoria/2026-07-20.md`).

`main.py`: nova rota `pagina_ativa == "rn1_simulada_30"` →
`interface.render_pagina_rn1_simulada_30()`.

**Sincronização**: implementado primeiro em `_MODELO_OPERACAO` (fonte de
verdade), depois copiado para as 3 operações reais (geraldo_2020_2024,
PB_2023_2025, cometa_2021_2025) — ver [[feedback_sync_operacoes]].
Confirmado por `cmp` que os 4 conjuntos de `loader.py`/`interface.py`/
`main.py` ficaram byte-idênticos antes da cópia (nenhuma customização
por operação foi perdida) e depois da cópia.

**Verificação**: `py_compile` limpo nas 4 pastas com o runtime portátil
de cada uma. Rodado `persistir_rn1_simulada_30()` de verdade na geraldo
(runtime próprio) — gerou 5.730 produtos (mesma contagem de `rn1_
produto`), conferido manualmente 1 produto ("AGUA PURIFIC 20LTS": EI
678,20 → 881,66 = ×1,3 ✓; Total Débito/Crédito/Divergência/Infração/
%Diverg todos batendo com a fórmula). Playwright não estava disponível
nesta sessão para teste de UI ponta-a-ponta (ver [[feedback_
verificacao_streamlit_ui]]); em vez disso, reiniciei o processo
Streamlit real do usuário que já rodava para geraldo na porta 8600 (2x
nesta sessão — 1x pro painel novo, 1x de novo pro drill-down, PID muda a
cada reinício por causa do watcher do Streamlit) e confirmei HTTP 200 sem
traceback no log a cada vez. PB_2023_2025 e cometa_2021_2025 não tinham
sessão Streamlit ativa no momento, nada a reiniciar nelas.

## Correção: clique na linha da tabela não abria o drill-down

Depois de testar na tela, o usuário reportou que clicar numa linha da
tabela do 7.3.2 (ex.: "MINUETO LIMÃO 115G") não fazia nada — o
selectbox "Selecione um produto..." abaixo continuava em "Selecione...".
Causa raiz: `st.dataframe()` sem `on_select`/`selection_mode` não emite
NENHUM evento de clique — a linha só fica destacada/copiável (o ícone de
copiar/lupa que aparece no clique), sem nenhum efeito em session_state.
O drill-down dependia só do selectbox manual, nunca tinha ligação real
com a tabela acima.

**Correção** (`interface.py`, `render_rn1_simulada_30()`): `st.dataframe`
da tabela principal do 7.3.2 ganhou `on_select="rerun"`,
`selection_mode="single-row"` e `key="rn1_simulada_30_selecao_linha"` —
o retorno (`DataframeState`, dict só-leitura com `["selection"]["rows"]`)
dá o índice da linha clicada. Esse índice é mapeado pra `DESCR_ALVO` via
`amostra.iloc[...]` e usado pra setar `st.session_state["drilldown_
rn1_simulada_30"]` (a key do selectbox) ANTES do selectbox ser
instanciado — padrão padrão do Streamlit pra controlar valor de widget
via session_state, sem passar `index=`/`value=` (que conflitaria).
Guarda de idempotência (`rn1_simulada_30_ultimo_clicado`): só força o
valor do selectbox quando a linha clicada MUDA de uma rodada pra outra —
sem isso, qualquer rerun (inclusive o usuário trocando manualmente o
selectbox) reaplicaria o valor da tabela e desfaria a escolha manual.
Testado isoladamente (sem depender de UI real — Playwright continua
indisponível nesta sessão) simulando os 4 cenários possíveis: nenhum
clique / primeiro clique / escolha manual depois preservada enquanto a
linha da tabela não muda / clique numa linha diferente sobrescrevendo —
os 4 bateram.

## Destaque visual: TD/TC/Divergência/%Diverg em vermelho

Pedido do usuário, depois de validar o clique-na-linha: destacar em
vermelho as 4 colunas que resumem o risco fiscal calculado — Total
Débito, Total Crédito, Divergência e % Diverg — nas duas tabelas do
Estágio 7.3.2 (principal e drill-down por ano), pra saltarem aos olhos
no meio das outras colunas monetárias.

`_destacar_vermelho_rn1(df)` (novo, `interface.py`): recebe o DataFrame
já preparado pra exibição (depois de `_preparar_preview_rn1_simulada_30()`
ou `_preparar_preview_rn1_fisica_simulada_30()`, já com os nomes de
coluna amigáveis do dicionário) e devolve `df.style.map(lambda _: "color:
red", subset=["Total Debito (R$)", "Total Credito (R$)", "Divergencia
(R$)", "% Diverg"])` — um `pandas.Styler`, que `st.dataframe()` aceita
diretamente no lugar de um DataFrame puro (suporte nativo do Streamlit,
`streamlit.elements.arrow.marshall_styler`). Aplicado nas duas chamadas
de `st.dataframe()` do painel 7.3.2 — a principal (que também tem
`on_select`/`selection_mode` pro clique-na-linha, sem conflito entre os
dois recursos) e a do drill-down.

Escopo: só o painel 7.3.2 (Simulação RN1 +30%), que era o que estava na
tela quando o usuário pediu — os demais painéis RN1 (7.2, 7.2.1, 7.3,
7.3.1) não têm esse destaque, a menos que peçam depois.

## Fechamento: "atualize tudo" — persistência final nas 3 operações reais

Usuário validou o destaque em vermelho ("perfeito") e pediu "atualize
tudo" — mesmo padrão de fechamento do `memoria/2026-07-21.md`. Rodado
`persistir_rn1_simulada_30()` nas 3 operações reais, cada uma com seu
próprio runtime portátil e `loader` importado da própria pasta
`ESSENCIAL/app` (ver [[feedback_hunter_operacao_dir_banco_path]]).
Nenhuma mudança de lógica de cálculo desde o commit inicial do 7.3.2 —
drill-down e destaque vermelho são só leitura/exibição, não alteram o
que é persistido em `rn1_simulada_30` — mas geraldo já tinha sido
persistida em teste anterior, PB e cometa ainda não tinham nenhuma linha.

**Estado final confirmado, as 3 operações** (`rn1_simulada_30`, contagem
de produtos igual à de `rn1_produto` do fechamento de 2026-07-21):

| Operação | rn1_simulada_30 (produtos) | Divergência total simulada (+30%) |
|---|---|---|
| geraldo | 5.730 | R$ 732.740,87 |
| PB2 | 261 | R$ 3.695.728,91 |
| cometa | 2.054 | R$ 352.374.152,67 |

Sessão do Streamlit real (porta 8600, geraldo) já estava reiniciada com
o código final antes deste fechamento — nenhum reinício adicional
necessário. PB_2023_2025 e cometa_2021_2025 seguem sem sessão Streamlit
ativa nesta máquina.

## Grupo de Produto Alvo (Fiscalização) — 7.3.2 vira tela de escolha

Implementado depois da correção de escopo acima. `loader.py`, nova seção:
tabela `produto_alvo_fiscalizacao` (DESCR_ALVO, TS, STATUS, DIVERGENCIA,
INFRACAO, PCT_DIVERGENCIA, TOTAL_DEBITO, TOTAL_CREDITO, OBSERVACAO) —
não confundir com `produto_alvo` (Estágio 7.1, equalização de nomenclatura,
schema/propósito diferentes).

- `salvar_grupo_produto_alvo_fiscalizacao(selecoes)`: upsert por DESCR_ALVO.
  Recebe um DataFrame com uma linha por produto EXIBIDO na tela no momento do
  clique em "Salvar" (colunas + `SELECIONADO` bool + `OBSERVACAO`). Produtos
  marcados viram/permanecem STATUS='ativo' com valores atualizados; produtos
  que estavam 'ativo' e vieram desmarcados viram STATUS='cancelado' (histórico
  preservado, nunca deleta linha); produtos fora da tela atual (outro filtro)
  ficam intocados — salvar sob um filtro não apaga seleção feita sob outro.
- `consultar_grupo_produto_alvo_fiscalizacao(limite, apenas_ativos=True)`:
  mesmo padrão consultar_* do resto do app.

`interface.py`: `_render_grupo_produto_alvo_fiscalizacao(amostra_raw)`, nova
seção dentro de `render_rn1_simulada_30()`, depois do drill-down por ano.
Usa `st.data_editor` (não `st.dataframe`) com coluna checkbox "Selecionar p/
Fiscalização" + coluna "Observação" editáveis, todas as outras colunas
`disabled=True` — widget diferente do `st.dataframe(on_select=...)` da
tabela principal (que fica só pro clique-linha/drill-down), pra não conflitar:
`st.data_editor` não suporta `on_select` nesta versão do Streamlit (1.58),
mas não precisa — o próprio retorno do `data_editor` já dá o estado do
checkbox editado, sem precisar de evento de seleção. `amostra_raw` é a
mesma fatia (até 200 linhas, já filtrada) da tabela principal, capturada
ANTES da formatação de moeda/percentual (que só existia pra exibição) —
os valores crus são o que vai pra persistência; adicionado `TS`/`STATUS`/
`OBSERVACAO` ao `DICIONARIO DE CAMPOS.txt` (seção nova, 2026-07-22).

Testado com 3 rodadas reais na geraldo (script direto, sem UI — Playwright
segue indisponível): seleciona 2 produtos → filtra diferente, seleciona mais 1
(os 2 anteriores continuam ativos) → desmarca 1 dos 2 originais (vira
'cancelado', mantém `OBSERVACAO` editada, não desaparece da tabela completa).
Os 3 comportamentos bateram exatamente como desenhado. Dados de teste
removidos (`DROP TABLE produto_alvo_fiscalizacao`) antes de propagar — a
tabela real da geraldo não tem nenhuma seleção fictícia. Propagado do
`_MODELO_OPERACAO` pras 3 operações reais, `cmp` confirma identidade.
`ESTAGIOS_PROJETO.md` ganhou a linha do 7.3.2 (estava faltando desde a sessão
anterior). Streamlit da geraldo (porta 8600) reiniciado com o código final.

## COD_ITEM (Cód. Produto da declaração) no 7.3.2

Pedido do usuário: "no 7.3.2 preciso enxergar o cod do produto da
declaração". `rn1_produto`/`rn1_simulada_30` são agrupados por
`DESCR_ALVO` (Descrição Relevante, Estágio 7.1) — não têm `COD_ITEM`.
Confirmei antes de mesclar que a relação é praticamente 1:1 (geraldo:
só 1 de 6.137 Descrições Relevantes tem mais de um `COD_ITEM`
associado, achado real "CATCHUP ODERICH 200GR" com 2 códigos).

`loader.py`: novo helper `_mapa_cod_item_por_descr_alvo()` — agrupa
`produto_alvo` (Estágio 7.1) por `DESCR_ALVO`, concatenando com ", " no
raro caso de mais de um código (evita duplicar linha ao mesclar em
tabelas agrupadas por Descrição). Mesclado em `gerar_rn1_simulada_30()`
(nível produto, persistido em `rn1_simulada_30`) e em
`simular_rn1_fisica_30()` (drill-down por ano, calculado ao vivo).
`_COLUNAS_RN1_PRODUTO` original (Estágio 7.3.1) NÃO mudou — criada
`_COLUNAS_RN1_SIMULADA_30` própria pro 7.3.2, escopo intencionalmente
restrito ao que foi pedido. `COD_ITEM` também entrou em
`_COLUNAS_PRODUTO_ALVO_FISCALIZACAO` (a seleção de Produto Alvo salva o
código junto). `TS`/`STATUS`/`OBSERVACAO` e agora nada novo no
dicionário — `COD_ITEM;Cod. Produto` já existia desde 2026-07-18.

Testado na geraldo: `persistir_rn1_simulada_30()` regenerado (5.730
produtos, mesma contagem de antes — só ganhou a coluna), código exibido
corretamente ("CERV SKOL LATA 350ML" → `7891149200504`, EAN de verdade)
tanto na tabela principal quanto no drill-down por ano. Regenerado
também no PB2 (261) e cometa (2.054). Propagado do `_MODELO_OPERACAO`
pras 3 operações reais, `cmp` confirma identidade. Sessão da geraldo
(porta 8600) reiniciada.

## Unificação: Grupo de Produto Alvo vira a ÚNICA tabela do 7.3.2

Usuário validou a tela do Grupo de Produto Alvo ("ficou ótima") e pediu:
"somente acresscente nela uma caixa para abrir os anos. pode ignorar a
primeira tabela" — ou seja, a tabela editável (checkbox de seleção)
vira a base do drill-down também, e a antiga tabela read-only (com
clique-de-linha via `on_select`/`selection_mode`, destaque vermelho) sai
do painel.

`interface.py`, `render_rn1_simulada_30()`: removido o bloco inteiro da
tabela antiga (evento_tabela/on_select, sync de clique com
session_state, e o antigo selectbox "Selecione um produto..." +
drill-down separado) — o painel agora só filtra por descrição e chama
`_render_grupo_produto_alvo_fiscalizacao(amostra_raw)` direto.

`_render_grupo_produto_alvo_fiscalizacao()`: nova coluna checkbox
"📅 Ver Anos" (`_COLUNA_CHECKBOX_VER_ANOS`) no MESMO `st.data_editor` do
grupo de fiscalização — marcar abre, logo abaixo da tabela, o
detalhamento anual (`loader.simular_rn1_fisica_30()`) de cada produto
marcado (múltiplos produtos podem ficar abertos ao mesmo tempo, um
drill-down por produto marcado, sem exclusividade tipo rádio). Não
precisa de `on_select` — `st.data_editor` não suporta nesta versão do
Streamlit (1.58) mesmo, mas o próprio retorno do editor já dá o estado
do checkbox, então não faz falta. Extração de valores marcados trocada
pra `.reindex(editor_base.index)` em vez de `.to_numpy()` posicional
direto (tanto pro "Ver Anos" quanto pro "Selecionar p/ Fiscalização" e
"Observação", ajuste feito no mesmo commit) — mais seguro contra
qualquer reordenação interna do widget, testado com DataFrame sintético
simulando o retorno do editor.

Removido também o código morto que ficou órfão com a tabela antiga:
`_preparar_preview_rn1_simulada_30()`, `_COLUNAS_PREVIEW_RN1_SIMULADA_30`,
`_destacar_vermelho_rn1()`, `_COLUNAS_DESTAQUE_VERMELHO_RN1` — o destaque
em vermelho (pedido em rodada anterior da mesma sessão) não foi portado
pra tabela nova, já que o usuário não pediu de novo e `st.data_editor`
não aceita `pandas.Styler` (só `st.dataframe` aceita). Se quiser o
destaque de volta, é pedido novo.

Propagado do `_MODELO_OPERACAO` pras 3 operações reais (só `interface.py`
mudou nesta rodada, `loader.py` ficou igual). Sessão da geraldo (porta
8600) reiniciada, HTTP 200 sem erro no log.

## Destaque TD/TC/Divergência na tabela editável — limitação real do st.data_editor

Pedido do usuário: "deixe em vermelho td, td e divergências" (depois
esclarecido: "td e tc" — Total Débito, Total Crédito, Divergência).
Avisei antes de implementar: `st.data_editor` (a tabela do Grupo de
Produto Alvo, que precisa ser editável pros checkboxes) **não aceita
`pandas.Styler`** — só `st.dataframe` (read-only) aceita cor de texto de
verdade, porque a grade é renderizada em canvas (glide-data-grid), sem
elemento DOM por célula pra estilizar via CSS. Perguntei como resolver:
usuário escolheu marcador 🔴 na frente do valor em vez de cor de fonte
literal, pra manter uma tabela só (não voltar a ter 2 tabelas).

`interface.py`: novo helper `_formatar_moeda_br_vermelho(v)` =
`f"🔴 {_formatar_moeda_br(v)}"`. Aplicado via nova constante
`_COLUNAS_DESTAQUE_VERMELHO_GRUPO_ALVO = ("TOTAL_DEBITO", "TOTAL_CREDITO",
"DIVERGENCIA")` nos 3 pontos que mostram esses valores dentro de
`_render_grupo_produto_alvo_fiscalizacao()`: a tabela editável principal,
o drill-down por ano ("Ver Anos") e o expander "Ver grupo completo já
salvo" — os 2 últimos são `st.dataframe` (read-only), então TECNICAMENTE
poderiam ter cor de verdade via Styler, mas usei o mesmo marcador 🔴 nos
3 lugares por consistência visual dentro do mesmo painel.

Testado o helper isoladamente (retorna "🔴 21.517,51" corretamente).
Streamlit da geraldo recarregou sozinho (watcher de arquivo) ao copiar
o `interface.py` novo — sem precisar matar/subir processo manualmente
desta vez. Propagado do `_MODELO_OPERACAO` pras 3 operações reais.

## Ver também

- [[feedback_regra_fiscal_e_do_usuario]] — por que perguntei antes de
  implementar a regra de Infração
- [[feedback_sync_operacoes]] — por que todo arquivo em `ESSENCIAL/app/`
  precisa ir para as 3 operações + `_MODELO_OPERACAO`
- [[feedback_verificacao_streamlit_ui]] — padrão de verificação de UI
  (Playwright indisponível nesta sessão, substituído por checagem de log
  + HTTP)
- `memoria/2026-07-21.md` — estado do Estágio 7.3.1 (RN1 por Produto)
  que serviu de base pra esta simulação
