# Atualização 2026-07-29

## Glossário rápido (pra quem lê sem contexto de chat)

- **AI_HUNTER1.1**: app Streamlit portátil de auditoria fiscal (Qlik/
  NF-e/SPED), usado por auditor(es) pra cruzar movimentação declarada
  (SPED) com movimentação física (XML da nota fiscal eletrônica).
- **"Estágio 10 - Produtos Alvos Salvos"**: painel onde o auditor
  escolhe UM produto alvo e cruza com Entradas/Saídas/Estoque
  (`estagio8_agrupado`/`_saidas_agrupado`/`_estoque_agrupado`) pra
  confirmar quais itens do XML pertencem de fato a esse produto
  ("Rubrica do Produto Alvo") — ver `memoria/2026-07-23.md` a
  `2026-07-28.md` pro histórico completo dessa feature.
- **"Busca de Produtos Correspondentes"**: seção do Estágio 10 com 3
  abas (Entradas/Saídas/Estoque), cada uma com um selectbox "Critério
  de busca" (Critério 1 = mesmo código; Critério 2 = nome de
  declaração igual, só em Entradas/Estoque; Critério 3 = código
  divergente + similaridade ≥ 20%, em Entradas/Saídas) que alimenta
  uma tabela única de candidatos com checkboxes "Salvar"/"Desfazer".
- **`desc_xml`**: descrição bruta do XML do candidato (`fatoitemnfe_
  infnfe_det_prod_xprod`) — em Entradas/Saídas é a descrição real do
  XML; no Estoque (Bloco H, sem XML próprio) é um ALIAS interno do
  mesmo valor de `descrição_decl`, mantido só pra reaproveitar a
  mesma função de salvamento (`salvar_cruzamento_confirmado()`), sem
  aparecer na exibição dessa aba.

## Busca por descrição do XML no topo da tabela de correspondências (Estágio 10)

Print da aba Entradas (Critério 3) anexado, mostrando a tabela de
candidatos sem nenhum filtro de texto. Pedido: **"no topo da tabela,
criar busca por xml para efetuar comparação"**.

Perguntado ao usuário (AskUserQuestion) o escopo: aplicar só na aba
Entradas (do print) ou nas 3 abas (Entradas/Saídas/Estoque, que
compartilham a mesma estrutura de tabela via funções espelhadas); e
qual coluna filtrar (só "Descrição XML" ou também "Cód. Produto
Declaração"). **Confirmado: as 3 abas, filtrando só por "Descrição
XML"**.

**Implementado** (`interface.py`, `_render_cruzamento_entradas()`/
`_render_cruzamento_saidas()`/`_render_cruzamento_estoque()`): novo
`st.text_input("🔎 Buscar por descrição do XML", ...)` inserido logo
depois da coluna "Observação" e antes da montagem de `colunas_
travadas`/`st.data_editor` — filtro client-side (substring, case-
insensitive, `str.contains(..., regex=False)`) sobre `editor_base
["desc_xml"]`, aplicado com a MESMA máscara em `editor_base` E
`editor_exibicao` (mantém os índices alinhados, já que o botão
"Salvar na Rubrica" usa `editor_base.index` como referência pro que
volta do `st.data_editor`). Quando o filtro está ativo, mostra
`st.caption(f"{len(editor_base)} de {len(correspondentes)}
combinação(ões) exibida(s).")` — não muda a mensagem de sucesso "N
combinação(ões) encontrada(s)" logo acima, que continua refletindo o
total do CRITÉRIO (não do filtro de texto).

`sufixo_criterio` (usado nas keys de widget pra evitar estado
"vazado" entre critérios do selectbox) foi movido pra logo depois de
`fn_agrupado, fn_detalhado = criterios[criterio_busca]`, no início da
função — antes só existia perto do `st.data_editor`, tarde demais
pra usar na key do novo `st.text_input`.

**No Estoque**, a coluna "Descricao XML" já era removida da EXIBIÇÃO
(só existe "Descricao Declaracao" — Bloco H não tem XML separado),
mas o filtro funciona igual porque `desc_xml` é alias interno de
`descrição_decl` nessa aba (ver `cruzar_produto_escolhido_estoque()`,
`loader.py`) — testado buscando "965" e batendo com "AGUARDENTE PITU
965ML" mesmo com a coluna oculta.

**Testado com dado real da geraldo** (produto "AGUARDENTE PITU
965ML", já escolhido pra cruzamento) via Playwright contra um
servidor de TESTE isolado (porta 8765, `python -m streamlit run
app/main.py`, runtime portátil da própria operação) — nunca a sessão
real do usuário:
- Entradas, Critério 3 (4 candidatos: "AGUARDENTE PITU 965ML" sem
  LATA + 3 variantes "...LATA..."): busca "LATA" → "3 de 4
  combinação(ões) exibida(s)", exatamente as 3 linhas com "LATA" na
  descrição, a 4ª (sem LATA) corretamente excluída. Busca sem match
  ("XYZNOPE") → "0 de 4", tabela vazia sem erro, resto da página
  (Itens individuais, Diagnóstico de Unidades) continua renderizando
  normal.
- Saídas, Critério 1 (1 candidato só, sem "LATA" na descrição): busca
  "LATA" → "0 de 1 combinação(ões) exibida(s)", correto.
- Estoque, Critério 1 (1 candidato): busca "965" → "1 de 1
  combinação(ões) exibida(s)", correto (confirma o alias `desc_xml`=
  `descrição_decl` funcionando mesmo com a coluna oculta).

Compilado (`ast.parse`, sem erros) e propagado IDÊNTICO nas 4
operações — `geraldo_2020_2024`/`_MODELO_OPERACAO`/`PB_2023_2025`/
`cometa_2021_2025` reconfirmados byte-a-byte iguais (`diff
--strip-trailing-cr`, 0 linhas de diferença) depois da propagação via
script Python (replace de string, já que os 4 `interface.py` eram
idênticos antes da mudança).

**Sessão real do usuário reiniciada**: a porta 8600 tinha uma conexão
ATIVA (PID 33512, iniciado hoje 08:49) com o `interface.py` antigo já
importado em memória — mesmo gotcha de módulo em cache de sempre (ver
[[feedback_verificacao_streamlit_ui]]). Perguntado ao usuário
(AskUserQuestion) se deveria encerrar agora ou deixar pra depois,
apesar da conexão ativa (diferente de outras sessões deste projeto,
que geralmente estavam ociosas) — **confirmado: encerrar agora**.
Processo finalizado (`Stop-Process`), porta 8600 liberada; o usuário
precisa reabrir via `iniciar_sistema.bat` (ou como preferir) pra ver
a busca nova na tela real.

## Critério 4 "pesquisa livre" — Entradas/Saídas/Estoque, gatilhado pela busca de XML

Pergunta exploratória do usuário logo em seguida: **"o que acha de tirar
o limite de 20% para critério3?"**. Resposta: não tirar de vez — o piso
existe porque, sem ele, o Critério 3 devolveria a base inteira ordenada
por similaridade (praticamente toda a 0%); sugeri BAIXAR o valor ou
trocar por "top N" em vez de remover. Réplica do usuário: **"é que pode
ocorrer que um produto com 0% de similaridade seja um candidato. o que
sugere?"** — argumento válido (código já diverge por definição no
Critério 3; a similaridade de descrição pode legitimamente ser 0% pro
produto certo, ex. nomenclaturas muito diferentes). Sugeri então tirar o
piso como FILTRO e deixá-lo só como ORDENAÇÃO (mesmo padrão do Critério
1/2) — usuário confirmou ("s").

**Antes de implementar**, o usuário perguntou: **"não vai impactar para
grande volume de dados?"**. Checagem com dado real ANTES de mexer no
código: `estagio8_agrupado` da geraldo tem **5.091 grupos** em Entradas,
`estagio8_saidas_agrupado` **4.687** em Saídas — como o Critério 3 já
filtra só por código DIVERGENTE (ou seja, quase a base inteira menos o
código do alvo), tirar o piso faria a tabela devolver milhares de linhas
de uma vez, pesado pro `st.data_editor`. Perguntado (AskUserQuestion)
como lidar com o volume — 3 opções (exigir busca antes de mostrar / top
N por similaridade / sem proteção nenhuma).

**Decisão final do usuário, diferente das 3 opções**: **"deixe criterio
3 intacto. crie criterio 4 para entradas e saídas e stoques para
pesquisa livre a fim de comparar com o produto alvo utilizando sua
sugestão 1."** — ou seja, Critério 3 (piso de 20%, FILTRO) fica como
estava, sem nenhuma mudança; em vez disso, um **Critério 4 NOVO**, nas 3
abas (Entradas/Saídas/Estoque — Estoque nunca teve Critério 3, mas ganha
Critério 4 igual), usando a proteção de volume "exigir busca antes de
mostrar" (opção 1 que eu tinha sugerido).

**Implementado** (`loader.py`): nova constante `CRITERIO_BUSCA4_
PESQUISA_LIVRE = "Critério de Busca4: pesquisa livre (sem filtro de
código/similaridade)"`. 6 funções novas, uma dupla (agrupado +
detalhado) por origem — `cruzar_produto_escolhido_entradas_criterio4()`/
`_detalhado()`, `..._saidas_criterio4()`/`_detalhado()`, `..._estoque_
criterio4()`/`_detalhado()` — mesma estrutura das funções de Critério
1/2/3 (mesma exclusão cross-alvo via `_chaves_ja_atribuidas_a_outro_
alvo()`, mesmo cálculo de `SIMILARIDADE_DESCRICAO` só informativo), mas
SEM NENHUM filtro de código (nem igual, nem divergente) — começam do
`agrupado`/`detalhado` INTEIRO (só excluindo o que já pertence a outro
alvo) e ordenam por similaridade desc, qtde_ocorrencias desc. Em
Estoque, mesmo alias `desc_xml = descrição_decl` já usado no Critério
1/2 de lá.

**Implementado** (`interface.py`): registrado o Critério 4 nos 3 dicts
de despacho (`_obter_criterios_cruzamento_entradas()`/`_saidas()`/
`_estoque()`). Nas 3 funções `_render_cruzamento_*()`: novo `elif`
pro caption (explica que é pesquisa livre, sem filtro/piso, e que a
tabela só aparece depois de buscar); banner trocado de `st.success`
("✅ N combinação(ões) encontrada(s)") pra `st.info` ("📚 N grupo(s) na
base... use a busca abaixo") quando o critério é o 4, pra não passar a
falsa impressão de que são candidatos já filtrados; e o GATE de volume
reaproveitando o MESMO campo "🔎 Buscar por descrição do XML" já criado
mais cedo hoje (sem widget duplicado) — quando o critério é o 4 e o
campo está vazio, mostra `st.info("Digite um termo de busca acima...")`
e retorna ANTES de `st.data_editor(...)`, então a base inteira nunca é
renderizada no navegador; com termo preenchido, funciona exatamente
como o filtro de texto já existente (mesma máscara em `editor_base`/
`editor_exibicao`).

**Testado com dado real da geraldo** (via Playwright, servidor de TESTE
isolado porta 8766, produto "REQUEIJAO ISIS CREMOSO 220GR" escolhido):
- Entradas, Critério 4, sem termo de busca: banner "📚 5.084 grupo(s) na
  base..." + tabela OCULTA (só a mensagem "Digite um termo..."),
  confirmando que o gate funciona e a base inteira não é renderizada.
- Entradas, Critério 4, busca "PITU" (produto totalmente diferente do
  alvo, 0% de similaridade esperado): "5 de 5084 combinação(ões)
  exibida(s)", mostrando candidatos como "AGUARDENTE PITU 12X600ML" com
  `SIMILARIDADE_DESCRICAO=0` — confirma na prática que o Critério 4
  encontra candidatos de 0% de similaridade que o Critério 3 NUNCA
  mostraria.
- Saídas, Critério 4, busca "REQUEIJAO": "7 de 4683 combinação(ões)
  exibida(s)", incluindo o próprio alvo (100%) e uma variante "LIGHT"
  (80%) — correto.
- Estoque, Critério 4, sem termo: banner "📚 5.972 grupo(s)..." + tabela
  oculta; com busca "REQUEIJAO": "7 de 5972 combinação(ões) exibida(s)".
- `cruzar_produto_escolhido_entradas_criterio4_detalhado()` chamado
  direto via `loader` (sem UI, sem gravar nada): 16.046 linhas, schema
  correto, sem erro — confirma que o botão "Salvar na Rubrica"
  funcionaria (mesma função `salvar_cruzamento_confirmado_detalhado()`
  de sempre, só muda o `fn_detalhado` de origem); não cliquei "Salvar"
  de verdade pela UI pra não gravar dado de teste na Rubrica real do
  alvo ativo no banco da geraldo.
- Nenhum erro de página (`pageerror`) em nenhum dos testes acima.

Compilado (`ast.parse`) e propagado IDÊNTICO nas 4 operações — cópia
direta dos 2 arquivos (`loader.py`/`interface.py`) da geraldo pras
outras 3 (já confirmados byte-a-byte iguais entre si ANTES desta
mudança, então copy > diff/replace), reconfirmado byte-a-byte igual
depois.

**Sessão real do usuário reiniciada de novo**: a porta 8600 (reaberta
pelo usuário depois do restart anterior, PID novo 41396, iniciado hoje
09:35) tinha o código de ANTES do Critério 4 em memória — mesmo gotcha
de sempre.

## Ver também

- [[feedback_sync_operacoes]] — por que todo arquivo em `ESSENCIAL/app/`
  precisa ir para as 3 operações + `_MODELO_OPERACAO`
- [[feedback_verificacao_streamlit_ui]] — padrão de verificação
  (Playwright + runtime portátil em porta separada) e o gotcha do
  módulo em cache
- `memoria/2026-07-28.md` — histórico completo do Critério 3 (rótulo,
  renumeração, reversão) e da nomenclatura "Estágio 10"
