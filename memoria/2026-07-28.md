# Atualização 2026-07-28

## Glossário rápido (pra quem lê sem contexto de chat)

- **AI_HUNTER1.1**: app Streamlit portátil de auditoria fiscal (Qlik/
  NF-e/SPED), usado por auditor(es) pra cruzar movimentação declarada
  (SPED) com movimentação física (XML da nota fiscal eletrônica).
- **"Estágio 10 - Produtos Alvos Salvos"** (batizado assim 2026-07-28
  — até essa data era chamado só de "Botão 9" nos comentários do
  código, nome que ainda aparece em boa parte da documentação
  histórica anterior a essa data): painel onde o auditor escolhe UM
  produto e cruza com Entradas/Saídas/Estoque pra confirmar quais
  itens do XML pertencem de fato a esse produto ("Rubrica do Produto
  Alvo"). Ver `memoria/2026-07-23.md` a `2026-07-26.md` pro histórico
  completo dessa feature (sob o nome antigo "Botão 9").
- **"Itens individuais (com ID Único)"**: tabela só-leitura dentro do
  Botão 9 (existe nas 3 abas — Entradas/Saídas/Estoque) que lista, item
  a item (chave `idunico`), tudo o que já foi confirmado na Rubrica do
  produto alvo — mostra os campos ORIGINAIS do XML lado a lado com os
  campos "utilizados" (efetivos, após eventual tratamento de FM).
- **`quant_utiliz`**: quantidade EFETIVA de cada item — igual à
  quantidade bruta do XML quando não há tratamento de Fator
  Multiplicador (FM) aplicado, ou já multiplicada pelo FM quando há.
- **`vu_utilizado`**: valor unitário EFETIVO de cada item — mesma ideia,
  já dividido pelo FM (ou pelo preço-base editado no Sumário) quando há
  tratamento.

## KPI de Soma/Média na tabela "Itens individuais"

Pedido direto, com print da tela do Botão 9 anexado (a mesma tela do
CERV SKOL LATA 350ML, 48 itens em Entradas, já documentada em sessões
anteriores): **"crie kpi com soma das quantidades utilizadas e média do
valor unitário utilizado"**.

**Implementado** (`interface.py`): nova função `_render_kpis_itens_
individuais(detalhado)`, chamada nas 3 abas (Entradas/Saídas/Estoque)
logo ANTES do loop que formata `quant_utiliz`/`vu_utilizado` como texto
BR (a formatação em string acontece só na exibição da tabela — o KPI
precisa dos valores ainda numéricos pra somar/tirar média). Renderiza 2
`st.metric()` lado a lado: "Soma Quantidade Utilizada"
(`.sum(skipna=True)`) e "Média Valor Unitário Utilizado"
(`.mean(skipna=True)`), ambos formatados com `_formatar_moeda_br()`
(padrão BR já usado no resto da tabela), com fallback `"-"` se o
resultado vier `NaN` (grupo vazio).

**Testado com dado real da geraldo** (CERV SKOL LATA 350ML, Entradas,
os mesmos 48 itens do print do usuário): soma = 5.676,00, média =
2,54 — validado tanto recalculando manualmente via `loader`/`interface`
direto (sem UI) quanto navegando a UI real com Playwright (servidor de
teste na porta 8601, nunca a sessão do usuário na 8600): os 2 KPIs
aparecem no texto extraído da página exatamente com esses valores,
logo acima de "48 item(ns) individuais gravado(s)", sem nenhum erro de
página (`pageerror` vazio). Compilado (`py_compile`) e propagado nas 4
operações (`geraldo_2020_2024`, `PB_2023_2025`, `cometa_2021_2025`,
`_MODELO_OPERACAO`) — mudança idêntica nas 4, já que `interface.py` é
igual entre operações (só a operação ativa muda em runtime).

**Nota**: a sessão ao vivo do usuário na porta 8600 continuava rodando
durante o teste (não foi tocada) — como o módulo `interface` já estava
importado nesse processo ANTES da edição, ele só vai enxergar os KPIs
novos depois de reiniciado (mesmo gotcha de módulo em cache já
documentado em `feedback_verificacao_streamlit_ui.md`). Avisar o
usuário pra reiniciar via `iniciar_sistema.bat` (ou fechar/reabrir o
processo da porta 8600) pra ver os KPIs na sessão real.

## FM (Fator Multiplicador) não capturava embalagem no formato "N X N ML"

Depois de ver o KPI acima com um produto novo (AGUARDENTE PITU 965ML,
59 itens em Entradas, unidade "CX" sem número embutido), o usuário
perguntou: **"em geraldo, não conseguiu captar caixa com 12?"**.

**Achado real**: `_extrair_fator_multiplicador_xml()` (`loader.py`,
usa `_REGEX_PARTICULA_FM`) só reconhecia os hints `C/N`, `CX N`, `FD N`
e `N UNID` embutidos na descrição do XML — a descrição real desse
produto é `"AGUARDENTE PITU GARRAFEIRA 12X965ML LIQ DV"`, que expressa
a caixa via contagem×volume ("12X965ML" = 12 garrafas de 965ml), um
formato que nenhum dos 4 padrões cobria. Testado direto:
`_extrair_fator_multiplicador_xml("AGUARDENTE PITU GARRAFEIRA
12X965ML LIQ DV")` devolvia `1.0` (fator neutro, nenhum padrão
reconhecido) antes da correção — confirmando que sim, o sistema não
tinha captado a caixa de 12 nesse produto.

**Implementado** (`loader.py`): `_REGEX_PARTICULA_FM` ganhou a
alternativa `\d+\s*X\s*\d+\s*ML` (case-insensitive) — o PRIMEIRO
número do match (antes do "X") é a contagem/fator, o segundo é o
volume da embalagem individual; como a extração de dígitos em
`_extrair_fator_multiplicador_xml()` já pega só o primeiro grupo
numérico do match inteiro, nenhuma lógica extra foi necessária além do
regex. Docstrings de `_extrair_particula_fm()`/`_extrair_fator_
multiplicador_xml()` atualizadas pra citar o novo padrão.

**Testado com dado real da geraldo**: `_extrair_fator_multiplicador_
xml("AGUARDENTE PITU GARRAFEIRA 12X965ML LIQ DV")` agora devolve
`12.0` (`particula="12X965ML"`). Recalculando o Sumário de Unidades
pro produto real "AGUARDENTE PITU 965ML" (59 itens confirmados em
Entradas, unidade "CX", faixa de preço 67,00-90,00) com o regex
corrigido: `fm_sug` passa a sugerir `12.0` automaticamente (antes não
sugeria nada) — só simulado em memória, nada persistido/alterado no
banco. Casos de regressão verificados no mesmo teste: `"CX12"` →
continua `fm=12` (padrão antigo intacto); `"CX15KG"` → continua
`fm=15` (mesmo comportamento "hint informativo, auditor decide" de
antes, já documentado no docstring); `"AGUA PURIFIC 20LTS"` (sem
nenhum padrão) → continua `fm=1` (sem falso positivo). Compilado
(`py_compile`, sem warnings) e propagado idêntico nas 4 operações.

**Nota**: mesmo gotcha de módulo em cache do KPI acima — a sessão ao
vivo do usuário na porta 8600 só vai sugerir FM=12 automaticamente
pra esse tipo de descrição depois de reiniciada.

## Regex de FM estendido de novo — agora cobre peso (G/GR/KG), não só volume (ML/L)

Logo depois de confirmar que o Estágio 9 usava a mesma função corrigida
(pergunta do usuário: "o regex está estendido ao estágio 9?" —
confirmado com dado real, `gerar_curadoria_fm_entradas()` já sugeria
FM=12 pros 5 desc_xml variantes do PITU), o usuário trouxe MAIS casos
reais com print de linhas do Estágio 9 onde `particula` continuava
vazia: **"CLUB SOCIAL SAB. ORIGINAL 6X24GR"**, **"NESTON 3 Cereais
Sachet 12x210g BR"**, **"COCO RALADO UMIDO E ADOÇADO 16 X 50 G"**,
**"LEITE FERMENTADO ISINHO 15 X 110 G"** — pedido: **"reveja o regex
acima"**.

**Causa raiz**: a extensão de 2026-07-28 mais cedo hoje (`\d+\s*X\s*
\d+\s*ML`) só cobria a unidade "ML" (volume) — essas 4 descrições
usam GRAMAS ("G"/"GR"), não mililitros, e 2 delas têm ESPAÇO em volta
do "X" ("16 X 50 G"), diferente do "12X965ML" original sem espaço.
Nenhum dos 2 detalhes (unidade de peso, espaço variável) era coberto.

**Implementado** (`loader.py`): `_REGEX_PARTICULA_FM` generalizado de
`\d+\s*X\s*\d+\s*ML` pra `\d+\s*X\s*\d+\s*(?:GR|KG|ML|G|L)` — cobre
peso (G/GR/KG) e volume (ML/L) numa lista só, `\s*` em volta do "X" já
cobria (e continua cobrindo) tanto "12x210g" (sem espaço) quanto "16 X
50 G" (com espaço) — nenhuma mudança adicional precisou ser feita pra
esse detalhe, o `\s*` já existia desde a versão anterior. Ordem da
lista importa: "GR"/"KG" vêm ANTES de "G" sozinho, senão a alternância
regex pararia no primeiro "G" e devolveria só "6X24G" (faltando o
"R") — sem impacto no FATOR extraído (primeiro número, antes do "X"),
mas o texto do HINT (`particula`) ficaria incompleto.

**Testado** (10 casos, cobrindo os 4 novos + todos os já validados
antes, pra garantir zero regressão): `AGUARDENTE PITU .../12X965ML` →
12; `CLUB SOCIAL 6X24GR` → 6; `NESTON 3 Cereais 12x210g` → 12; `COCO
RALADO 16 X 50 G` → 16; `LEITE FERMENTADO ISINHO 15 X 110 G` → 15;
`CERV SKOL .../CX12` → 12 (padrão antigo intacto); `PT 1,5L MARENI
.../C/04 UN.` → 4; `PT 02L MARENI .../C/04UN` → 4 (os 2 casos "C/04"
do mesmo print que motivou o pedido — já funcionavam corretamente
antes e depois, o "1" visível no print daquelas 2 linhas não era
`fm_sugerido`, e sim outra coluna, provavelmente `qtde_ocorrencias`);
`PRODUTO GENERICO CX15KG` → 15 (regressão check); `AGUA PURIFIC
20LTS` → 1 (sem falso positivo). Todos os 10 bateram o esperado.
Compilado (`py_compile`, sem warnings) e propagado idêntico nas 4
operações.

## FM_ELEITO do Estágio 9 propagado pro Botão 9 (Entradas/Saídas) — com achado real de dado desatualizado

Confirmado com o usuário ("s") o plano do turno anterior: Fator
Multiplicador "deve nascer no estágio 9 de forma 'pré' e só então
propagado a produtos alvos salvos" — implementar em Entradas/Saídas o
MESMO padrão que o Estoque já tinha desde 2026-07-25.

**Implementado** (`loader.py`, `consultar_atributos_estoque_por_
idunico()`): depois de calcular `fm_sugerido` via regex (fallback),
calcula `_valor_unit_grupo = vl_unit_prod.round(0)` (mesma fórmula de
`gerar_curadoria_fm_entradas()`/`_saidas()`) e faz LEFT JOIN contra
`consultar_curadoria_fm()`/`consultar_curadoria_fm_saidas()` (conforme
`origem`) por `(desc_xml, _valor_unit_grupo)` == `(DESC_XML,
VALOR_UNIT_GRUPO)`; onde existe `FM_ELEITO` não-nulo, ele GANHA do
valor do regex. Mesma função (`consultar_atributos_estoque_por_
idunico`) serve Entradas e Saídas, só troca qual tabela de curadoria
consultar. `AppTest` completo na página "Produtos Alvos Salvos"
(3 abas, produto real "AGUARDENTE PITU 965ML" — 60 itens Entradas, 337
Saídas, 4 Estoque) sem exceção. Compilado e propagado idêntico nas 4
operações.

**ACHADO REAL IMPORTANTE — regressão temporária em Entradas na
geraldo**: `fm_entradas_curadoria` já tem **6.593 linhas**, todas
salvas de uma vez em `2026-07-24T21:14:10` (o Estágio 9 sempre grava a
tabela INTEIRA a cada "Salvar", ver `memoria/2026-07-24.md`) — ou
seja, essa é uma decisão "congelada" de ANTES das 2 extensões de regex
de hoje (ML/L e depois GR/KG/G). Comparando o regex de HOJE contra o
`FM_ELEITO` salvo: **2.034 grupos** (de 6.593) têm um FM maior/melhor
pelo regex atual do que o que está congelado — INCLUINDO os mesmos
produtos usados nos exemplos desta sessão (PT 1,5L MARENI C/04→
FM_ELEITO=1 salvo vs regex=4; CLUB SOCIAL 6X24GR→1 vs 6; NESTON
12x210g→1 vs 12; COCO RALADO 16x50g→1 vs 16; LEITE FERMENTADO ISINHO
15x110g→1 vs 15). Testado com dado real (AGUARDENTE PITU 965ML,
Entradas): dos 60 itens, 13 têm `_valor_unit_grupo` que caiu num grupo
com `FM_ELEITO=1` congelado — esses 13 agora mostram `fm_sugerido=1`
na tabela "Itens individuais" do Botão 9, em vez do 12 correto que o
regex de hoje já acertaria sozinho (comportamento de ANTES desta
mudança, mais cedo hoje). `fm_saidas_curadoria` está VAZIA (0 linhas)
— sem esse problema em Saídas, só em Entradas.

**Isso é esperado dado o desenho pedido** (Estágio 9 manda, Botão 9 só
herda) — mas a tabela `fm_entradas_curadoria` PRECISA ser regerada
("Regerar Estágio 9") e salva de novo ("Salvar") pra que o `FM_ELEITO`
reflita as correções de regex de hoje. **NÃO fiz isso ainda** —
requer decisão do usuário antes, já que "Salvar" no Estágio 9
sobrescreve as 6.593 linhas de uma vez (upsert completo por
`DESC_XML`+`VALOR_UNIT_GRUPO`), e uma parte dessas 6.593 linhas pode
conter correções MANUAIS que o auditor já fez em cima do regex antigo
(não dá pra distinguir só pelo dado quais linhas foram editadas à mão
vs. deixadas no valor sugerido automático da época) — regenerar sem
confirmação corre o risco de apagar uma curadoria manual real.

**Decisão do usuário (pergunta feita via AskUserQuestion)**: "Regerar e
salvar tudo agora" — aceitar o risco de sobrescrever eventuais ajustes
manuais em troca de corrigir os 2.034 grupos desatualizados.

**Executado direto via `loader` (não pela UI do Estágio 9)** — achado
importante no processo: a UI de `render_curadoria_fm_entradas()`
(`interface.py`) **pré-popula** "FM Sugerido"/"Nova UP" com o valor
JÁ SALVO em `fm_entradas_curadoria` sempre que a chave (desc_xml+
valor_unit_grupo) já existe (`salvos_por_chave`, proteção deliberada
contra "Regerar" apagar curadoria manual — ver código de 2026-07-24).
Isso significa que clicar "Regerar" + "Salvar" pela TELA NÃO teria
corrigido nada — o editor mostraria de volta os mesmos valores
congelados, só com timestamp novo. Por isso a correção teve que ir
direto no banco:
1. Backup de segurança: `CREATE TABLE fm_entradas_curadoria_
   backup_20260728 AS SELECT * FROM fm_entradas_curadoria` (6.593
   linhas preservadas, tabela ainda existe no banco da geraldo caso
   precise reverter).
2. `loader.persistir_curadoria_fm_entradas()` — regera `estagio9_
   fm_entradas_agrupado` do zero, com `fm_sugerido` calculado pelo
   regex de HOJE pros 6.593 grupos.
3. Montado `selecionadas` (DESC_XML/VALOR_UNIT_GRUPO/FM_ELEITO=
   fm_sugerido fresco/NOVA_UP=nova_up) com `universo_chaves` = todas
   as 6.593 chaves, e chamado `loader.salvar_curadoria_fm()` direto —
   mesma função que o botão "Salvar" da UI chama, só que com o valor
   FRESCO em vez do congelado.

**Resultado confirmado**: `total_salvo=6593, total_removido=0`.
Reconferido: 0 divergências entre `FM_ELEITO` salvo e o regex de hoje
pra TODOS os 6.593 grupos (antes eram 2.034). `AGUARDENTE PITU 965ML`
(Entradas, 60 itens): 59 agora com `fm_sugerido=12` (só 1 item com
`desc_xml` sem o padrão "12X...ML" embutido, `fm=1` corretamente).
Outros exemplos da sessão reconfirmados: CLUB SOCIAL (6/1, conforme a
variante), NESTON (12/6/1, conforme a variante), COCO RALADO (16),
LEITE FERMENTADO ISINHO (15/1, conforme a variante), MARENI (12/30/
4/10/8/1, conforme a variante — cada `desc_xml` distinto dentro da
mesma marca pode ter embalagem diferente, resultado esperado).
`AppTest` completo na página real (3 abas) sem exceção depois da
mudança de dado. `fm_saidas_curadoria` continua vazia (nada a
regenerar lá). Tabela de backup mantida no banco da geraldo (não
removida) como rede de segurança.

## Regex de FM — rodada longa de casos reais + reversão do fallback (decisão final do dia)

Depois de confirmar que o regex já valia pro Estágio 9 (pergunta do
usuário, respondida com dado real), o usuário trouxe uma sequência
rápida de MAIS casos reais com prints (uma leva de interrupções
seguidas, cada uma só com "fm deveria ser N" + a linha da tabela),
forçando várias rodadas de extensão do mesmo regex no mesmo dia:

- **HARPIC PAST ADESIVA 3UNX9G** (Saídas) → esperado 3: "UN" embutido
  ENTRE a contagem e o "X" ("3UN" + "X" + "9G"), padrão novo, diferente
  do "NxN<unidade>" de mais cedo.
- **VANISH SUPER 4X1275G** → esperado 4: já funcionava (peso de 4
  dígitos, confirma que a extensão de peso/volume já cobria).
- **PAPEL ALUMINIO TERMICA 30X7,5M** → esperado 30: 2 problemas novos —
  unidade "M" (metros) não estava na lista, e o SEGUNDO número tinha
  vírgula decimal ("7,5"), que o `\d+` puro não aceitava.
- **PIRULITO BABOOM 20X600GR** → esperado 20: já funcionava (peso com
  "GR", confirma a extensão anterior).
- **FD PAMPERS PANTS XG 16 UN** → esperado 16: padrão TOTALMENTE
  diferente — sem "X" nenhum, só "N UN" solto no fim (unidades, não
  peso/volume) — regex novo `\d+\s*UN\b` (boundary no fim pra não
  confundir com "UNIDADES"/"UN" seguido de mais letras).
- **SABÃO EM PÃO KIUSA MACIEZ 24X 500** → esperado 24: "N X N" SEM
  NENHUMA unidade no fim — precisou tornar a unidade OPCIONAL.
- **BARRA GOLDEN MESCLADO/BRANCO 9X20UN** → esperado 9: "N X N" com
  unidade "UN" no fim (nem peso/volume, nem o "N UN" solto) — expôs que
  tornar a unidade opcional sem incluir "UN"/"UNID" na lista de
  unidades reconhecidas fazia o regex "perder" o match bom (`9X20UN`)
  e cair no fallback `\d+\s*UN\b` isolado mais à direita, extraindo 20
  em vez de 9.

**BUG REAL descoberto no meio do processo — "GUARDANAPO MALU 21X23
50UN"**: ao tornar a unidade opcional (pro caso do SABÃO), o regex
passou a casar com QUALQUER "NxN" sem unidade, incluindo DIMENSÕES
FÍSICAS do produto (não embalagem) que aparecem ANTES da contagem real
na mesma descrição — aqui "21X23" (provavelmente cm de um guardanapo)
casava PRIMEIRO (regex pega o match mais à ESQUERDA), escondendo o
"50UN" real mais adiante, que é a contagem correta.

**Correção estrutural** (não só mais um padrão — arquitetura do
regex): split em 2 regex compiladas —
`_REGEX_PARTICULA_FM_COM_UNIDADE` (exige uma unidade reconhecida no
fim — GR/KG/ML/G/L/M/UNID/UN) tentada PRIMEIRO, só cai pra
`_REGEX_PARTICULA_FM_SEM_UNIDADE` ("N X N" solto, sem nenhuma unidade)
se a primeira não achar NADA na descrição inteira — função
`_buscar_particula_fm()` faz esse fallback em cascata. Isso resolve o
GUARDANAPO (encontra "50UN" via COM_UNIDADE antes de considerar
"21X23"), mas **não resolve 100% o caso residual**: "PANO PIA LIMPAMIL
28X30 AZUL UN" (uma toalha de pia 28x30cm, `up_xml`="UN"/1 unidade)
continua extraindo `fm=28` errado, porque estruturalmente
"28X30" (SEM unidade colada) é IDÊNTICO ao "24X 500" do SABÃO que o
próprio usuário confirmou como correto — não tem como o regex
distinguir "dimensão física" de "contagem de embalagem" só pelo texto
quando não sobra nenhuma unidade pra desambiguar. Documentado como
limitação aceita (o Estágio 9 exige confirmação do auditor antes de
propagar pro Botão 9 — exatamente o checkpoint que existe pra pegar
esse tipo de sugestão errada).

**28 casos de regressão testados ao final** (todos os exemplos reais
desta sessão + os sintéticos de rodadas anteriores) — só o PANO PIA
falha, por design/limitação aceita, documentada acima.

## Reversão do fallback de regex em Entradas/Saídas — "tudo deve nascer do Estágio 9"

No meio da rodada de regex acima, pergunta direta: **"o que foi feito
na entrada está sem propagado para saída e estoque?"**. Resposta:
Saídas SIM (mesma função `consultar_atributos_estoque_por_idunico`,
só muda `origem`); Estoque NÃO — `consultar_atributos_estoque_
estoque_por_idunico()` é uma função SEPARADA que desde 2026-07-25
NUNCA usou o regex como fallback, só lê `FM_ELEITO` de
`fm_estoque_curadoria` (fica `NA` se o grupo não foi curado).

Perguntei (AskUserQuestion) se deveria ADICIONAR um fallback de regex
no Estoque, pra ficar simétrico com Entradas/Saídas. Resposta foi na
direção OPOSTA: **"deve tudo nascer do estágio 9"**, e depois, numa 2ª
pergunta de confirmação: **"o fm somente é utilizado de fato nos
produtos alvos salvos e deve ter como fonte o estágio 9, conforme
[decidido] de cada grupo"** — ou seja: é Entradas/Saídas que deveriam
PERDER o fallback de regex (implementado horas antes nesta mesma
sessão), não o Estoque ganhar um. O modelo correto, confirmado 2 vezes
no mesmo dia, é o que o Estoque já tinha desde o início: Botão 9 é
PURO REFLEXO do que já foi confirmado no Estágio 9, nunca calcula nada
por conta própria.

**Implementado** (`loader.py`, `consultar_atributos_estoque_por_
idunico()`): removida a linha que calculava `fm_sugerido` via
`_extrair_fator_multiplicador_xml()` como baseline — agora
`fm_sugerido` vem SÓ do merge com `FM_ELEITO` (`NA` se a chave
`desc_xml`+`_valor_unit_grupo` não estiver em `fm_entradas_curadoria`/
`fm_saidas_curadoria`). O regex `_extrair_fator_multiplicador_xml()`
continua rodando NORMALMENTE dentro do Estágio 9
(`gerar_curadoria_fm_entradas()`/`_saidas()`), que é o único lugar que
ainda o chama — a mudança é só na leitura por idunico do Botão 9.

**Depois da reversão, uma 2ª regeneração real do Estágio 9**: como o
regex evoluiu MUITO depois da 1ª regeneração de mais cedo hoje
(HARPIC, PAPEL ALUMINIO, PAMPERS, SABÃO, BARRA GOLDEN, GUARDANAPO —
todos os padrões acima), `fm_entradas_curadoria` ficou desatualizada
de novo: comparação mostrou **444 grupos divergentes** entre o
`FM_ELEITO` salvo (regex de horas atrás) e o regex FINAL de agora.
Regenerado de novo pelo mesmo processo já autorizado ("regerar e
salvar tudo agora" já valia pra esta rodada de continuação): backup
extra (`fm_entradas_curadoria_backup_20260728b`), `persistir_
curadoria_fm_entradas()` + `salvar_curadoria_fm()` direto via loader.
Resultado: `total_salvo=6593`, **0 divergências** reconfirmadas contra
o regex final. `AppTest` sem exceção depois da reversão do fallback +
2ª regeneração. Compilado e propagado idêntico nas 4 operações (regex
completo + função `consultar_atributos_estoque_por_idunico()` sem
fallback).

**2 tabelas de backup mantidas no banco da geraldo** (não removidas):
`fm_entradas_curadoria_backup_20260728` (antes da 1ª regeneração) e
`fm_entradas_curadoria_backup_20260728b` (antes da 2ª).

## Ordem dos botões do Menu Principal — Estágio 9 antes de Produtos Alvos Salvos

Pedido direto, com print da 2ª linha do menu anexado: **"coloque o
botão estágio 9 antes de produtos salvos"**.

**Implementado** (`interface.py`, `render_menu_principal()`): a 2ª
linha do menu (`col_estagio8, col_estagio9, col_produtos_alvo_salvos =
st.columns(3)`) tinha as colunas na ordem Estágio 8 / Produtos Alvos
Salvos / Estágio 9 — só trocada a ORDEM de exibição (Estágio 8 /
Estágio 9 / Produtos Alvos Salvos), sem mudar nome de variável de
`session_state`, key de widget nem lógica de navegação de nenhum dos 2
botões.

**Testado**: `AppTest` (`streamlit.testing.v1`) rodando `main.py` com
`dados_carregados=True` — lista de labels de todos os botões do Menu
Principal confirma "⚖️ ESTÁGIO 9: FATOR MULTIPLICADOR (ENTRADAS)" (12ª
posição) antes de "🎯 PRODUTOS ALVOS SALVOS" (13ª), sem exceção
(`at.exception` vazio). Compilado e propagado idêntico nas 4 operações.

## "Botão 9" batizado oficialmente como "Estágio 10 - Produtos Alvos Salvos"

Pergunta exploratória do usuário ("o que é o botão 9?") seguida de
pedido direto: **"batize 'Produtos Alvos Salvos' como 'ESTÁGIO 10 -
Produtos Alvos Salvos', não mais botão 9"**.

**Implementado** (`interface.py`) — só as strings VISÍVEIS ao usuário
foram trocadas, sem tocar em nomes internos (função continua
`render_produtos_alvo_salvos()`/`render_pagina_produtos_alvo_salvos()`,
`session_state["pagina_ativa"]` continua `"produtos_alvo_salvos"`,
keys de widget inalteradas — renomear isso também seria refactor sem
ganho, arriscando quebrar algo à toa):
- Botão do Menu Principal: `"🎯 PRODUTOS ALVOS SALVOS"` →
  `"🎯 ESTÁGIO 10 - PRODUTOS ALVOS SALVOS"`.
- Subtítulo da página: `st.subheader("Produtos Alvos Salvos")` →
  `st.subheader("Estágio 10 - Produtos Alvos Salvos")`.
- Docstring de `render_pagina_produtos_alvo_salvos()` atualizada pra
  registrar o batismo e a data.

**Decisão consciente de escopo**: NÃO reescrevi todo o histórico de
comentários/docstrings que mencionam "Botão 9" espalhados pelo código
(são muitos, documentando decisões de sessões anteriores 2026-07-23 a
2026-07-28) — só a nomenclatura VISÍVEL na tela e o ponto de entrada
principal (`render_pagina_produtos_alvo_salvos()`) foram atualizados,
não a nomenclatura interna. Comentários antigos continuam dizendo
"Botão 9" porque era esse o
nome NA ÉPOCA em que foram escritos — reescrever tudo seria um
refactor grande sem ganho funcional, e esta memória (glossário no
topo) já documenta a equivalência "Botão 9" = "Estágio 10" pra quem
ler a documentação histórica.

**Testado**: `AppTest` completo — label do botão no Menu Principal
confirmado como `"🎯 ESTÁGIO 10 - PRODUTOS ALVOS SALVOS"` (última
posição, depois do Estágio 9); subtítulo da página confirmado como
"Estágio 10 - Produtos Alvos Salvos", sem exceção. Compilado e
propagado idêntico nas 4 operações.

**Seguimento, mesmo dia — "atualize tudo"**: depois de eu explicar que
só tinha trocado o nome VISÍVEL (botão + subtítulo), deixando os
comentários/docstrings antigos com "Botão 9", o usuário pediu pra
atualizar tudo mesmo. Reescrito via script (replace de string simples,
`"Botão 9"` → `"Estágio 10"`) nos 2 arquivos (`interface.py`,
`loader.py`) — 11+19=30 ocorrências por operação, todas dentro de
comentários/docstrings (nenhuma em nome de variável/função/`session_
state`/key de widget, que continuam como estavam). Preservada
DELIBERADAMENTE só 1 ocorrência (a nota histórica escrita mais cedo
hoje em `render_pagina_produtos_alvo_salvos()`: "...antes chamado só
de 'Botão 9'..." — essa PRECISA continuar dizendo "Botão 9", já que é
uma frase sobre o nome ANTIGO; um replace cego a transformaria em
"antes chamado só de 'Estágio 10'", contradizendo a própria frase).
Técnica: marcador temporário (caractere Unicode de área privada,
fora do alfabeto normal) substituindo só essa frase específica antes
do replace geral de "Botão 9"→"Estágio 10", restaurado depois.

`AppTest` completo sem exceção depois do replace nas 4 operações
(mesmo teste de antes, repetido). Compilado e propagado idêntico.

## "FM Sugerido" (Estoque) — substitui a coluna "FM Eleito" em branco

Print da aba Estoque do Estágio 9 anexado, mostrando a coluna `fm_eleito`
sempre `None` (raw, sem tradução no Dicionário de Campos, já que só
existia lá — nunca em Entradas/Saídas). Pedido: **"nos moldes de
entradas e saídas, replique em estoque o fm sugerido em vez de fm
eleito, assim como partículas"**.

**Causa**: a aba Estoque (`gerar_curadoria_fm_estoque()`) nunca calculou
`fm_sugerido` — o comentário original (2026-07-25) justificava isso
dizendo que o Bloco H "não tem ligação nenhuma com Matching/BC3". Essa
justificativa ficou desatualizada no mesmo dia em que foi escrita:
`fm_sugerido` em Entradas/Saídas já não vem mais do
`FATOR_MULTIPLICADOR_SUGERIDO` do Matching desde o revert de
2026-07-25 ("para o fm, nas entradas e saídas não olhe para o sped,
olhe apenas para o xml") — é `_extrair_fator_multiplicador_xml()`,
regex puro sobre a própria descrição, sem NENHUMA dependência de
Matching/BC3. Ou seja, a técnica já em uso funciona igual bem sobre
`descrição_decl` do Bloco H.

**Implementado** (`loader.py`, `gerar_curadoria_fm_estoque()`):
`agrupado["fm_sugerido"] = agrupado["descrição_decl"].apply(_extrair_
fator_multiplicador_xml)`, mesma linha usada em `gerar_curadoria_fm_
entradas()`/`_saidas()`; `_COLUNAS_FM_ESTOQUE_AGRUPADO` ganhou
`"fm_sugerido"` na mesma posição das outras 2 abas (entre `particula` e
`nova_up`).

**Implementado** (`interface.py`, `render_curadoria_fm_estoque()`):
removida a coluna `fm_eleito` inserida manualmente em branco
(`pd.array([pd.NA]*len, dtype="Float64")`) — agora "FM Sugerido" é a
própria coluna calculada, editável (mesmo padrão de Entradas/Saídas: o
auditor ajusta o valor sugerido em vez de partir de um campo vazio);
pré-população a partir de `fm_estoque_curadoria` já salva passou a
escrever em `fm_sugerido` (era `fm_eleito`); `column_config`/`colunas_
travadas`/bloco de "Salvar" trocados de "FM Eleito" para "FM Sugerido"
(dicionário de campos já tinha as 2 entradas — `fm_sugerido` e
`FM_ELEITO` — mapeadas pro mesmo rótulo "FM Sugerido", nenhuma entrada
nova precisou ser criada).

**Testado com dado real da geraldo** (via `loader` direto, read-only,
runtime portátil da própria operação): `gerar_curadoria_fm_estoque()`
sem erros, 5.974 grupos (mesma contagem de antes — só a coluna nova
mudou); os mesmos itens do print do usuário (CREME N 56GR, HIDRAT
MONANGE...) confirmados com `fm_sugerido=1.0` (sem padrão de embalagem
na descrição, comportamento esperado); 459 grupos com `fm_sugerido !=
1` confirmados com valores corretos batendo a partícula extraída (ex.:
"ANT SEPTI BUCAL U.ACTI MIKEI 12X250ML" → `particula="12X250ML"`,
`fm_sugerido=12.0`; "SACO P/LIXO VENEZA 100L C/25X5UN" →
`particula="C/25"`, `fm_sugerido=25.0`). `py_compile` sem warnings nas
4 operações. `__pycache__` de cada operação removido (o schema da
tabela agrupada em memória mudou, evita servir bytecode com o schema
antigo). Sessão ao vivo do usuário na porta 8600 (rodando exatamente a
tela do print) estava com o módulo `loader`/`interface` já importado
ANTES desta edição — mesmo gotcha de módulo em cache já documentado em
`feedback_verificacao_streamlit_ui.md` — confirmado com o usuário via
AskUserQuestion e o processo (PID 41880) encerrado; precisa reabrir via
`ESSENCIAL/iniciar_sistema.bat` pra ver "FM Sugerido" na tela real.
`ESTAGIOS_PROJETO.md` (linha do Estágio 9) atualizado com nota da
mudança. Propagado idêntico nas 4 operações
(`geraldo_2020_2024`/`PB_2023_2025`/`cometa_2021_2025`/
`_MODELO_OPERACAO`).

## BUG REAL corrigido: `KeyError: "['fm_sugerido'] not in index"` ao abrir Estágio 9 (Estoque)

Reportado pelo usuário com print do erro completo, logo depois de reabrir
o app com o código do item anterior — a tela quebrava DEPOIS de mostrar
"5.974 grupo(s)", num `KeyError` ao montar `editor_base`.

**Causa raiz**: `estagio9_fm_estoque_agrupado` é uma tabela CACHE
(derivada, recriada via `CREATE OR REPLACE TABLE` a partir de
`estoque_anual_consolidado` sempre que o botão "Regerar" é clicado) —
diferente de `fm_estoque_curadoria` (decisão MANUAL do auditor,
preservada). Nas 3 operações que já tinham essa tabela gerada
(`geraldo_2020_2024`, `PB_2023_2025`, `cometa_2021_2025`), ela foi
persistida ANTES da mudança de código deste turno, com o schema
ANTIGO (sem a coluna `fm_sugerido`). `render_curadoria_fm_estoque()`
lê essa tabela do banco (não recalcula na hora) e tenta selecionar
`_COLUNAS_PREVIEW_FM_ESTOQUE_AGRUPADO` (que já inclui `fm_sugerido`
desde a mudança de código) — coluna ausente no DataFrame lido, daí o
`KeyError`. `_MODELO_OPERACAO` não tinha a tabela gerada ainda
(ninguém rodou "Gerar" lá), então não sofria o problema.

**Solução**: regenerar a tabela cache — passo normal de migração de
schema já usado neste projeto antes (ver nota de
`tratamento_fm_entradas`/`_saidas`/`_estoque` em sessões anteriores).
Confirmado ANTES de regenerar que não havia curadoria manual salva a
perder: `fm_estoque_curadoria` (decisão do auditor, tabela SEPARADA)
tinha **0 linhas** nas 3 operações — regenerar o cache não apagou
nada. Executado `loader.persistir_curadoria_fm_estoque()` direto
(equivalente a clicar "Regerar Estágio 9 — Curadoria de Fator
Multiplicador (Estoque)" na tela) nas 3 operações afetadas:
`geraldo_2020_2024` (5.974 grupos), `PB_2023_2025` (96),
`cometa_2021_2025` (45) — todas agora com a coluna `fm_sugerido`
presente. Reconfirmado com uma simulação exata do trecho que quebrava
em `interface.py` (`agrupado[_COLUNAS_PREVIEW_FM_ESTOQUE_AGRUPADO]`),
sem `KeyError`.

**Lição pra próxima mudança de schema numa tabela CACHE deste tipo**:
ao adicionar/remover coluna de uma função `gerar_*()` cujo resultado é
persistido via `CREATE OR REPLACE TABLE`, sempre checar (e regenerar
se preciso) as tabelas já materializadas nas operações reais ANTES de
propagar o código — não basta só editar `loader.py`/`interface.py`,
o dado já gravado no DuckDB de cada operação também precisa ser
migrado.

## Rótulo do Critério 3 (Entradas) renomeado — "nome_prod_decl do alvo = nome_prod_xml"

Print da tela "Estágio 10 - Produtos Alvos Salvos" (aba Entradas)
anexado, mostrando o selectbox "Critério de busca" com o rótulo antigo
do Critério 3 ("sem correspondência com declaração"). Pedido: **"mudar
o nome do critério3 para: 'nome_prod_decl do alvo = nome_prod_xml' em
entradas"** — escopo restrito à aba Entradas (não pediu Saídas).

**Contexto que confirma o rótulo novo é correto**: Critério 3 (em
qualquer aba) busca candidatos com código DIVERGENTE do alvo, usando
`SIMILARIDADE_DESCRICAO` como filtro — comparação entre `DESCR_ALVO`
(o "nome_prod_decl do alvo", já que é o nome declarado do alvo) e
`desc_xml` do candidato (o "nome_prod_xml"). O rótulo novo só deixa
explícito o que a comparação sempre fez, seguindo o mesmo padrão "X do
alvo = Y do candidato" já usado nos rótulos do Critério 1/2 — sem
mudança de lógica/função nenhuma.

**Implementado**: como `CRITERIO_BUSCA3_CODIGO_DIVERGENTE` era uma
constante COMPARTILHADA entre `_obter_criterios_cruzamento_entradas()`
e `_obter_criterios_cruzamento_saidas()` (`interface.py`), renomear a
constante em si mudaria as 2 abas — criada uma constante NOVA e
separada só para Entradas (`loader.CRITERIO_BUSCA3_NOME_XML_ENTRADAS =
"Critério de Busca3: nome_prod_decl do alvo = nome_prod_xml"`),
trocada apenas na entrada do dict de Entradas; Saídas continua
usando `CRITERIO_BUSCA3_CODIGO_DIVERGENTE` (rótulo antigo) sem
nenhuma alteração. Confirmado que a constante só é usada como CHAVE de
dict/opção de selectbox em `interface.py` (nunca em comparação de
igualdade explícita tipo `if criterio == ...`, que só existe pra
Critério 1/2 — Critério 3 sempre cai no `else`), então a troca de
texto não quebra nenhum despacho.

**Testado**: `py_compile` sem erros; `interface._obter_criterios_
cruzamento_entradas().keys()` confirmado com o rótulo novo do
Critério 3, `interface._obter_criterios_cruzamento_saidas().keys()`
confirmado INTACTO com o rótulo antigo. `__pycache__` das 3 operações
com Streamlit já rodado removido (schema/constantes mudaram).
Propagado idêntico nas 4 operações.

## Ver também

- [[feedback_sync_operacoes]] — por que todo arquivo em `ESSENCIAL/app/`
  precisa ir para as 3 operações + `_MODELO_OPERACAO`
- [[feedback_verificacao_streamlit_ui]] — padrão de verificação (Playwright
  + runtime portátil em porta separada) e o gotcha do módulo em cache
- `memoria/2026-07-26.md` — histórico completo da tabela "Itens
  individuais" (campos originais vs. utilizados, tratamento de FM)
