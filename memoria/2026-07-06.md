# Atualização 2026-07-06

## Contexto do Sistema (recapitulação + termos novos do dia)

Ver `memoria/2026-07-04.md` para o glossário completo do projeto (HUNTER 1.1, NF-e, SPED/EFD, `C100`/`C170`/`0200`/`0190`, ET/EP, `IND_OPER`/`IND_EMIT`, "Auditada", `dtype=str`/Regra R07, DuckDB) e `memoria/2026-07-05.md` para CFOP, situação da NF-e, segregação, **BC1** (Base Comparativa 1 — dataset `sped_entradas_terceiros`, lado SPED) e **BC2** (Base Comparativa 2 — dataset `nfe_bc2`, lado XML, itens de Emissão de Terceiros).

Termos novos de hoje:

- **Matching (Etapa 1)**: a etapa do sistema que efetivamente cruza a BC2 (o que o fornecedor emitiu, segundo o XML) com a BC1 (o que o cliente auditado declarou ao Fisco, segundo o SPED), item a item, para descobrir se cada compra recebida foi corretamente escriturada.
- **BC3**: o resultado do Matching — uma tabela com uma linha por item da BC2, indicando se e como ele foi encontrado na BC1 (e trazendo a descrição/código do item conforme registrado na declaração, quando encontrado).
- **GTIN/EAN**: o código de barras global e padronizado de um produto (Global Trade Item Number / European Article Number) — em teoria, o mesmo produto tem o mesmo GTIN em qualquer nota fiscal de qualquer fornecedor, então é um identificador mais confiável que o código interno de cada empresa.
- **Levenshtein / similaridade de texto (fuzzy matching)**: técnica que compara duas strings e calcula um "score" (0 a 1) de o quanto elas são parecidas, mesmo que não sejam idênticas — usada aqui para comparar a descrição do produto no XML com a descrição do produto na declaração, já que fornecedor e declarante frequentemente escrevem o nome do mesmo produto de formas ligeiramente diferentes (abreviações, ordem das palavras, etc.).

## Resumo Executivo

Implementado o motor de Matching (Etapa 1): cruza a BC2 (XML) com a BC1 (SPED) para produzir a BC3. Essa é uma mudança de premissa importante em relação ao que se cogitava antes: a ideia original era usar `CHV_NFE + NUM_ITEM` (a chave de acesso da nota + o número sequencial do item) como elo entre as duas bases — mas essa premissa foi **abandonada** porque não há garantia nenhuma de que o item nº 3 no XML do fornecedor seja o mesmo item nº 3 na escrituração do declarante (cada lado pode ordenar/numerar os itens de forma diferente). A nova abordagem usa, em ordem de prioridade: valor financeiro exato, depois código de barras (GTIN) exato, depois similaridade de texto na descrição do produto.

## Funcionalidades Implementadas

- **`scoring.py`** (só existia como esqueleto vazio até hoje): duas funções usando a biblioteca `rapidfuzz` (já estava nas dependências do projeto, usada no cálculo de similaridade de texto). `similaridade(a, b)` devolve um número de 0 a 1 dizendo o quanto duas strings são parecidas. `melhor_similaridade(referencia, candidatos)` compara uma string contra uma lista de candidatas e devolve a mais parecida.
- **`matching.py`** (também só existia como esqueleto vazio): a função principal `executar_matching()` faz o cruzamento em duas etapas (com fallback):
  1. **Match Principal** (`_match_principal`): tenta casar cada item da BC2 com um item da BC1 que tenha a **mesma Chave de Acesso (`CHV_NFE`) e o mesmo Valor do Item (`VL_ITEM`)**, arredondado a 2 casas decimais para evitar diferenças de centavos por arredondamento. É uma correspondência 1-para-1: um item da BC1 só pode ser usado para casar com um único item da BC2 (evita que dois itens do XML "roubem" o mesmo item da declaração).
  2. **Match Secundário** (`_match_secundario`): para os itens que sobraram do passo 1, tenta, **só entre itens da mesma nota (mesma `CHV_NFE`)** que ainda não foram usados: primeiro um match exato pelo código de barras (GTIN/EAN); se não achar, calcula a similaridade de texto entre a descrição do produto no XML e a descrição registrada na declaração (registro `0200` do SPED) — se o score for maior que 0,85 (85% de parecença), considera como match sugerido.
  3. Para o que sobrar depois dos dois passos, o sistema agora distingue **dois motivos diferentes** de não-match (isso foi um ajuste feito no meio do dia, depois de uma pergunta do usuário): `'nd'` (não declarado) quando a chave de acesso inteira nem aparece na BC1 — ou seja, a nota de compra não foi encontrada na declaração de jeito nenhum, o que pode indicar uma compra não declarada; e `'nm'` (não match) quando a nota existe na BC1, mas esse item específico não bateu por nenhum dos três critérios — a nota foi declarada, só não foi possível confirmar qual item específico corresponde.
- Nova função `loader.persistir_bc3()`: roda o `matching.executar_matching()` e grava o resultado na tabela `bc3` do DuckDB. Usa "lazy import" (`import matching` só dentro da função, não no topo do arquivo) para evitar um problema de "importação circular" — como `matching.py` já importa `loader.py` (para ler a BC2 e a BC1), se `loader.py` importasse `matching.py` logo no topo também, os dois arquivos ficariam esperando um pelo outro para carregar e o programa não funcionaria; importar só na hora de usar resolve isso.
- `loader.bc3_ja_gerada()` / `loader.consultar_bc3()`: seguem o mesmo padrão de "verificar o que já está persistido no banco antes de reprocessar" usado em todas as outras telas do sistema até agora.

## Decisões Técnicas

- **Só a camada de dados foi implementada hoje** (funções em `loader.py`/`matching.py`/`scoring.py`, testadas via linha de comando) — sem botão nem tela na interface (`interface.py`/`main.py`) ainda. Isso segue o mesmo padrão já usado para a BC1 e a BC2: primeiro a lógica de dados é implantada e validada, e o botão/tela na interface vem depois, numa mensagem separada, se o usuário pedir.
- O valor (`VL_ITEM`) é convertido para número (`float`) só internamente, na hora de comparar — a coluna continua sendo tratada como texto (`dtype=str`) no restante do sistema, seguindo a Regra Operacional R07.
- `ID_UNICO` (a coluna de identificação única criada em 2026-07-05) continua existindo em ambas as bases, mas **deixou de ser usada como critério de cruzamento** — agora serve só para rastreabilidade (poder localizar de volta a linha original no banco, se precisar).

## Soluções Aplicadas

- Validado contra dados reais da operação `geraldo_2020_2024`: dos 16.408 itens da BC2 (itens de Emissão de Terceiros), 2.529 casaram pelo valor exato (Match Principal), 1.920 casaram pela similaridade de texto (nenhum casou por código de barras nesta operação — os dados de GTIN podem estar ausentes ou divergentes aqui), 846 ficaram como "não declarado" (nota toda ausente da BC1) e 11.113 como "não match" (nota existe, item não confirmado). A soma bate exatamente com o total (2.529+1.920+846+11.113 = 16.408) — nenhum item ficou de fora da contagem.
- Conferência manual de amostras: os matches por similaridade de texto (ex.: "IOGURTE NATURAL PARCIALMENTE DESNATADO ISIS 150 G" com "IOG NATURAL PARCIALMENTE DESNATADO ISIS 150GR", score 0,91) fazem sentido — claramente o mesmo produto escrito de forma abreviada.
- Testado `persistir_bc3()` de ponta a ponta: gravou 16.408 linhas na tabela `bc3` sem erros.

## Problemas Encontrados

- O processamento completo leva cerca de 45 segundos para os ~16 mil itens desta operação (o cálculo de similaridade de texto item a item é a parte mais lenta). Isso é aceitável para uma ação sob demanda (com uma barra de carregamento, por exemplo), mas pode ficar mais lento se uma operação futura tiver muito mais itens — vale otimizar se isso incomodar no futuro (por exemplo, comparando só contra os itens da mesma nota, o que já é feito, mas poderia se beneficiar de uma biblioteca de comparação em lote em vez de calcular um por um).

## Arquivos Modificados/Criados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py` (esqueleto vazio → implementado)
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/scoring.py` (esqueleto vazio → implementado)
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/loader.py` (novas funções `bc3_ja_gerada`, `consultar_bc3`, `persistir_bc3`)
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

## Pendências

- ~~BC3 ainda não tem botão/tela na interface~~ — implementado na 2ª parte do dia (ver abaixo).
- Desempenho do cálculo de similaridade de texto (~45s para 16 mil itens) — otimizar se virar um problema com bases maiores.
- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (2ª parte do dia) — Botão de visualização da BC3

### Resumo

Usuário pediu um botão para visualizar a BC3 (resultado do Matching), seguindo o mesmo padrão já usado nas outras telas do sistema.

### Funcionalidades Implementadas

- `loader.consultar_totais_bc3()`: nova função que conta, direto no banco (sem reprocessar o matching), quantos itens da BC3 caíram em cada tipo de resultado (`PRINCIPAL_VALOR`, `SECUNDARIO_GTIN`, `SECUNDARIO_FUZZY`, `ND`, `NM`) — alimenta os indicadores numéricos da tela.
- `interface.render_bc3()`: nova tela "Matching (Etapa 1) — BC2 × BC1 = BC3", com 5 indicadores numéricos (um para cada tipo de resultado), botão que alterna entre "Gerar Matching (BC3)" e "Regerar Matching (BC3)" (mesmo padrão de todas as outras telas: consulta se já foi gerado antes de mostrar o botão certo) e uma área expansível com prévia dos dados já traduzidos para nomes amigáveis.
- `main.py`: chama a nova tela logo depois do Painel de Monitoramento (a tela de segregação criada em 2026-07-05), dentro do mesmo bloco que só aparece após a carga geral.
- Aproveitando a mudança, a mensagem "Demais módulos (carregamento completo, matching e equalização) ainda não implementados" foi corrigida para remover "matching" da lista, já que essa parte deixou de ser verdade.

### Soluções Aplicadas

- Validado em navegador real (Playwright): sessão nova (sem clicar em nada) já mostra a tela com os 5 indicadores corretos (2.529 / 0 / 1.920 / 846 / 11.113, batendo com os números confirmados de manhã) e a prévia com dados reais.
- Testado também o clique no botão "Regerar Matching (BC3)" — o processamento de ~45 segundos roda até o fim (spinner exibido durante todo o tempo) e a tela atualiza corretamente ao terminar, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/loader.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/main.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Desempenho do cálculo de similaridade de texto (~45s para 16 mil itens) — otimizar se virar um problema com bases maiores.
- ~~Desempenho do cálculo de similaridade de texto~~ — otimizado na 3ª parte do dia (ver abaixo).
- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (3ª parte do dia) — Otimização do Matching para escala de milhões de registros

### Resumo Executivo

Usuário avisou que operações futuras vão ingerir **milhões** de registros, e que os ~45 segundos observados de manhã (para só 16 mil itens) não seria aceitável nessa escala. `matching.py` foi reescrito para ser **totalmente vetorizado** — ou seja, em vez de processar item por item num laço do Python (lento, principalmente por causa do overhead do Pandas em acessar linha por linha com `.loc`/`.iterrows()`), o cálculo passou a usar operações em lote (nativas do Pandas/NumPy/rapidfuzz, implementadas internamente em C, muito mais rápidas).

### O que mudou tecnicamente

- **Match Principal e Match Secundário por GTIN**: antes, cada um desses passos usava um laço item a item, construindo dicionários manualmente e comparando um registro de cada vez. Agora usam **merge** (a operação de "junção" do Pandas, equivalente a um `JOIN` de banco de dados) — mas com um truque para lidar com valores repetidos dentro da mesma nota sem perder a regra "1 para 1" (cada item da declaração só pode casar com um item do XML): cada linha ganha um número de "ordem de ocorrência" dentro do seu grupo (mesma chave de acesso + mesmo valor, ou mesma chave de acesso + mesmo código de barras) — daí o merge casa a 1ª ocorrência de um lado com a 1ª do outro, a 2ª com a 2ª, e assim por diante, tudo de uma vez, sem laço.
- **Match Secundário por similaridade de texto**: antes, para cada item pendente, era feita uma comparação individual contra os candidatos daquela nota (função `.apply()` do Pandas, que tem bastante overhead por chamada). Agora, o sistema agrupa os itens pendentes por nota fiscal (chave de acesso) e, para cada nota, calcula de uma vez só toda a "matriz de parecença" entre os produtos do XML e os produtos da declaração daquela nota, usando uma função do `rapidfuzz` (`process.cdist`) que já faz esse cálculo em lote, em código C (bem mais rápido que chamar a função de similaridade uma vez para cada par manualmente). Dentro de cada nota, os pares com maior parecença são escolhidos primeiro (o que é, inclusive, uma pequena melhoria de qualidade em relação à versão de manhã, que pegava o primeiro candidato acima do limiar, não necessariamente o mais parecido).
- Consequência prática: o laço que ainda roda em Python puro passou a ser **por nota fiscal**, não mais **por item individual** — como cada nota tipicamente tem só um punhado de itens, uma base com milhões de itens tende a ter "só" algumas centenas de milhares de notas distintas, o que reduz bastante a quantidade de iterações do laço lento.

### Soluções Aplicadas

- **Validação de correção**: reexecutado contra os mesmos dados reais de `geraldo_2020_2024` — os números batem quase exatamente com os de manhã (2.529 matches por valor, 846 "não declarado" — idênticos; 1.918 matches por similaridade contra 1.920 antes, uma diferença de 2 itens esperada e correta, porque a nova versão escolhe o par de maior parecença primeiro dentro de cada nota, enquanto a versão de manhã pegava o primeiro candidato que passasse do limiar — pode fazer um item "trocar" de par em casos de empate/proximidade, sem isso ser um erro).
- **Validação de velocidade** (dados reais, 16.408 itens): caiu de ~45 segundos para **~8,4 segundos** — cerca de 5,3 vezes mais rápido.
- **Teste de escala com dados sintéticos** (gerados só para este teste, não são dados reais de nenhum cliente): ~400 mil itens processados em ~19 segundos; **~1 milhão de itens processados em ~64 segundos**. Ou seja, mesmo na escala de milhões de registros que o usuário avisou que viria, o processamento continua na casa de 1 a 2 minutos — perfeitamente aceitável para uma ação sob demanda com barra de carregamento, bem diferente da situação anterior (que, extrapolando o comportamento antigo, provavelmente levaria dezenas de minutos a horas nessa mesma escala).
- Testado em navegador real: clicar em "Regerar Matching (BC3)" com os dados reais agora completa quase instantaneamente (antes era preciso esperar a barra de carregamento por quase 1 minuto).

### Decisões Técnicas

- `scoring.py` (criado de manhã com `similaridade()`/`melhor_similaridade()`) deixou de ser usado por `matching.py` — a nova versão vetorizada chama o `rapidfuzz.process.cdist` diretamente. O arquivo não foi apagado (continua sendo uma função utilitária válida, só não é mais usada neste fluxo específico).

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Se no futuro a escala chegar a dezenas de milhões de itens (não só "milhões"), pode valer a pena otimizar ainda mais o laço por nota do passo de similaridade de texto (por exemplo, processando vários grupos de notas em paralelo) — não foi necessário agora, mas fica registrado como próximo ponto de otimização se pedido.
- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (4ª parte do dia) — Correção: exportação da BC3 limitada a 200 linhas

### Resumo

O usuário testou a tela do Matching (BC3) e percebeu que o botão de exportar para CSV (o ícone de download que o próprio Streamlit desenha no canto da tabela) só trazia 200 linhas — porque esse botão exporta exatamente o que está sendo exibido na tela, e a prévia da tabela é intencionalmente limitada a 200 linhas por questão de desempenho (a tabela completa pode ter milhões de linhas no futuro, não faria sentido desenhar tudo isso na tela). Foi criado um botão de exportação separado, que busca a BC3 inteira do banco de dados só quando o usuário pede.

### Funcionalidades Implementadas

- `loader.consultar_bc3()` ganhou a possibilidade de `limite=None`, que devolve a tabela inteira em vez de uma amostra (antes só aceitava um número fixo de linhas).
- Na tela do Matching (`interface.render_bc3()`), dentro da área expansível, foi adicionado um fluxo de duas etapas: um botão "Preparar exportação completa (CSV)" que busca a BC3 inteira do banco e monta o arquivo CSV (com nomes de coluna amigáveis, conforme o `DICIONARIO DE CAMPOS.txt`) só quando clicado — e, depois de pronto, aparece um segundo botão "Baixar BC3 completa (N linha(s), CSV)" para efetivamente baixar o arquivo. Essa etapa intermediária existe de propósito: montar a exportação completa é uma operação que pode ficar pesada com bases de milhões de linhas, então só deve rodar quando o usuário realmente pede, não toda vez que a tela é redesenhada.

### Soluções Aplicadas

- Testado o fluxo completo em navegador real (Playwright, que consegue capturar downloads de arquivo): clicado em "Preparar exportação completa" e depois em "Baixar BC3 completa", o arquivo baixado tinha exatamente 16.409 linhas (1 cabeçalho + 16.408 linhas de dados) — batendo com o total real da BC3, confirmando que a limitação de 200 linhas não afeta mais a exportação. Conferido também que o CSV usa `;` como separador (padrão do Excel brasileiro), nomes de coluna amigáveis e o marcador de codificação UTF-8 (BOM) que garante que acentos apareçam corretos ao abrir no Excel.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/loader.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (5ª parte do dia) — Suspenso o passo de GTIN/EAN no Matching

### Resumo

Usuário pediu para suspender o passo de comparação por código de barras (GTIN/EAN, campo `COD_BARRA`) do motor de Matching — o fallback agora vai direto do Match Principal (valor) para o Match Secundário (similaridade de texto), sem passar pelo GTIN no meio.

### Funcionalidades Implementadas

- `matching.py`: removida a etapa de match por GTIN dentro de `executar_matching()` — a função auxiliar genérica (`_match_exato_vetorizado()`) continua existindo e sendo usada pelo Match Principal, só deixou de ser chamada uma segunda vez para o GTIN. A constante `_SEM_GTIN` (usada só para filtrar GTIN vazio/"sem GTIN") foi removida por não ter mais uso.
- `loader.consultar_totais_bc3()`: não conta mais `SECUNDARIO_GTIN` como uma categoria própria dos KPIs (mantém uma observação no código de que essa marcação pode aparecer em bases já geradas antes dessa mudança e ainda não regeradas, mas isso não afeta a exibição atual).
- `interface.render_bc3()`: removido o indicador numérico "Match por GTIN" — o painel passou de 5 para 4 indicadores (Match por Valor, Match por Similaridade, Não Declarado, Sem Match). A legenda da tela também foi ajustada para não mencionar mais o GTIN na descrição do fluxo.

### Reavaliação do Tempo de Processamento (pedida explicitamente pelo usuário)

Aqui está um resultado importante, que talvez seja contraintuitivo: **remover uma etapa do processamento não necessariamente deixa o sistema mais rápido** — depende de quantos itens aquela etapa resolvia "de graça" antes de chegar na etapa seguinte, mais cara.

- **Dados reais desta operação** (`geraldo_2020_2024`): tempo ficou praticamente igual (~8,8 segundos, era ~8,4s) — porque o passo de GTIN não estava encontrando nenhum match aqui mesmo antes (os dados de código de barras nesta base não batem entre XML e declaração), então removê-lo não tira nem acrescenta trabalho.
- **Teste sintético de escala** (dados fabricados, ~1 milhão de itens, criados especificamente para simular uma base grande): o tempo **subiu** de ~64 segundos para ~83 segundos. Isso acontece porque, nesse conjunto sintético, o passo de GTIN resolvia cerca de 270 mil itens de forma barata (comparação exata); sem esse passo, esses mesmos 270 mil itens agora precisam passar pelo cálculo de similaridade de texto, que é a etapa mais cara do processo — então, paradoxalmente, tirar uma etapa "grátis" pode aumentar o tempo total, porque sobra mais trabalho para a etapa cara.
- **Conclusão prática**: em bases onde o GTIN realmente bate entre XML e declaração (o que não é o caso desta operação até agora), remover esse passo pode deixar o processamento mais lento, não mais rápido — o principal ganho de mantê-lo suspenso é de **negócio** (evitar falsos positivos de correspondência por código de barras, se for esse o motivo da decisão), não de performance.

### Soluções Aplicadas

- Validado contra dados reais: os números de match continuam batendo (2.529 + 1.918 + 846 + 11.115 = 16.408), sem nenhuma categoria de GTIN aparecendo.
- Validado em navegador real (Playwright): painel mostra os 4 indicadores corretos, legenda atualizada, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/loader.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (6ª parte do dia) — Título da BC1 + exportação completa

### Resumo

Usuário apontou duas coisas na tela "Chaves de entrada de emissão de terceiros (base comparativa1)" (a tela do BC1/SPED): faltava deixar claro no título que esse dado vem da declaração, e a exportação em CSV também estava limitada a 200 linhas ali — o mesmo problema que já tinha sido corrigido na BC3 mais cedo, só que nesta outra tela.

### Funcionalidades Implementadas

- Título alterado de "Chaves de entrada de emissão de terceiros (base comparativa1)" para "**Chaves de entrada de emissão de terceiros da declaração (base comparativa1)**" — deixa explícito que essa base vem do lado SPED/Declaração (em contraste com a BC2, que é do lado XML).
- `loader.consultar_entradas_terceiros()` ganhou a opção `limite=None` (mesma mudança já feita na `consultar_bc3()`), para buscar a tabela `sped_entradas_terceiros` inteira em vez de só uma amostra.
- Na tela (`interface.render_entradas_terceiros()`), foi adicionado o mesmo fluxo de exportação em duas etapas já usado na BC3: botão "Preparar exportação completa (CSV)" que busca a tabela inteira só quando clicado, seguido do botão "Baixar tabela completa (N linha(s), CSV)" para o download de fato. Também foi acrescentada uma nota visível na tela ("Prévia limitada a 200 linhas de N") para deixar claro, desde já, que a tabela mostrada é só uma amostra.

### Soluções Aplicadas

- Testado em navegador real: o título aparece corretamente, e o arquivo baixado pelo novo botão tem 16.101 linhas (1 cabeçalho + 16.100 de dados) — batendo com o total real da tabela `sped_entradas_terceiros`, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/loader.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (7ª parte do dia) — Limiar de similaridade do Match Secundário reduzido para 50%

### Resumo

Usuário pediu para diminuir o limiar de aceitação do "match de nome" (a similaridade de texto entre a descrição do produto no XML e no SPED, explicada no Contexto do dia 2026-07-06) de 85% para 50%. Alterado o valor da constante `LIMIAR_SIMILARIDADE` em `matching.py`, de `0.85` para `0.50`.

### Efeito Observado (importante — registrado para o usuário avaliar)

Com o limiar mais baixo, muito mais itens passam a ser considerados "match": o "Sem Match (nm)" caiu de 11.115 para 3.463, e o "Match por Similaridade" subiu de 1.918 para 9.570 (dados reais desta operação). À primeira vista isso parece uma melhora (menos itens sem correspondência), **mas ao inspecionar amostras dos matches com score próximo de 50%, vários deles são claramente pares de produtos diferentes** que só têm estrutura de texto parecida — por exemplo:

- "KIT VITA CAPILI SH+COND AMID MILHO 310ML" casado com "KIT SHAMPOO+COND.CLINIHAIR LISO EXPERT 250M" (marcas e produtos diferentes).
- "AZEITE OLIVA E.VIRGEM VALE DOS SERTOE..." casado com "AZEITE DE OLIVIA E. V SINHA 250ML" (marcas diferentes).
- "AGUA MINERAL INDAIA C/+ GAS PET 12X500ML" casado com "AGUA MINERAL NATURAL COM GAS SCHIN500ML" (marcas diferentes).

Ou seja, a redução do limiar para 50% aumenta a quantidade de itens "casados", mas às custas de introduzir **falsos positivos** — produtos diferentes sendo apontados como correspondentes só porque o texto da descrição tem uma estrutura parecida (mesma categoria de produto, palavras em comum como "KIT", "AZEITE", "AGUA MINERAL"). Isso foi implementado exatamente como pedido, mas fica registrado esse alerta de qualidade para o usuário decidir se é isso mesmo que quer, ou se prefere um valor intermediário.

### Soluções Aplicadas

- Validado em navegador real (Playwright): "Regerar Matching (BC3)" com o novo limiar roda sem exceções (~11 segundos nos dados reais desta operação).
- Nenhuma outra menção ao valor antigo (0,85) foi encontrada em outros arquivos (`interface.py` não tem esse número fixo em nenhum lugar).

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Usuário pode querer revisar o valor de 50% depois de ver os falsos positivos apontados acima — considerar um valor intermediário (ex.: 70-75%) se quiser equilibrar cobertura e precisão.
- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (8ª parte do dia) — Remove o Match por Valor; Match só em notas com mesma quantidade de itens

### Resumo

Usuário pediu duas mudanças na lógica de Matching: (1) remover completamente a correspondência por valor (`CHV_NFE` + `VL_ITEM`, o "Match Principal" implementado hoje de manhã); (2) só tentar casar os itens de uma nota fiscal quando a quantidade de linhas (itens) do lado XML (BC2) for **igual** à quantidade de linhas do lado da declaração (BC1) para aquela mesma nota — se as quantidades forem diferentes, a nota inteira fica sem tentativa de correspondência item a item.

### Funcionalidades Implementadas

- `matching.py`: removida a função `_match_exato_vetorizado()` (usada só pelo Match por Valor, que não existe mais) e o helper `_arredondar_valor()` — ambos ficaram sem uso.
- Nova função `_chaves_com_mesma_quantidade_de_linhas()`: conta, de forma vetorizada (via `groupby().size()`), quantos itens cada `CHV_NFE` tem na BC2 e na BC1, e devolve o conjunto de chaves onde essas quantidades batem — só essas notas entram na etapa de similaridade de texto.
- `executar_matching()`: agora filtra a BC2 e a BC1 para as notas elegíveis (mesma quantidade de itens) antes de rodar o `_match_fuzzy_por_nota()` — as notas não elegíveis (quantidade diferente) ficam automaticamente como "nm" (sem match), mesmo que a nota esteja declarada.
- `interface.py`: removido o indicador "Match por Valor" do painel — o painel passou de 4 para 3 indicadores (Match por Similaridade, Não Declarado, Sem Match). Legenda da tela atualizada para explicar a nova regra de elegibilidade.
- `loader.consultar_totais_bc3()`: não conta mais `PRINCIPAL_VALOR` como categoria própria dos KPIs (mesma lógica já usada para o GTIN suspenso mais cedo).

### Soluções Aplicadas

- Teste sintético isolado confirmou a regra de elegibilidade: uma nota com quantidades diferentes de itens nos dois lados foi corretamente excluída da comparação, enquanto uma nota com quantidades iguais foi corretamente incluída.
- Validado contra dados reais desta operação: 9.427 matches por similaridade + 846 "não declarado" + 6.135 "sem match" = 16.408 (bate com o total da BC2). Para comparação, antes desta mudança (ainda com Match por Valor e sem a regra de elegibilidade) os números eram 9.570 + 846 + 3.463 — ou seja, a nova regra deixou o resultado mais criterioso (menos itens "forçados" a um match), às custas de mais itens caindo em "sem match" quando a nota tem quantidades de itens diferentes nos dois lados.
- Validado em navegador real (Playwright): painel mostra os 3 indicadores corretos, sem exceções, tanto no clique de "Regerar Matching" quanto numa sessão nova depois.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/loader.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (9ª parte do dia) — Nova hierarquia: similaridade como critério primário + desempate por GTIN/Valor

### Resumo

Usuário trouxe uma reformulação da lógica de Matching: em vez de tentar vários critérios em sequência com fallback (valor → GTIN → similaridade, como era até a parte anterior), agora a **similaridade de descrição passa a ser o critério primário e único portão de entrada** — todo item do SPED com similaridade de texto acima de 60% (dentro da mesma nota) vira candidato. Quando um item do XML tem mais de um candidato acima desse limiar, o sistema usa GTIN/EAN e depois Valor Total só para **desempatar entre os candidatos já pré-aprovados pela similaridade**, não mais como critérios de match independentes.

### Funcionalidades Implementadas

- `matching.py` reescrito: removida a regra de elegibilidade por quantidade de linhas por nota (da parte anterior do dia) — não existe mais esse portão antes da similaridade.
- Nova função `_match_por_nota()` (substitui `_match_fuzzy_por_nota()`): para cada nota, calcula a matriz de similaridade inteira (como antes), mas agora, entre os pares candidatos acima do limiar, aplica um desempate vetorizado com `np.lexsort` em 3 níveis de prioridade — 1º mesmo GTIN/EAN (`COD_BARRA` do SPED vs `cean` do XML), 2º mesmo Valor Total (`VL_ITEM`), 3º maior score de similaridade (desempate final). A atribuição gulosa (que evita usar o mesmo item da declaração duas vezes) passa a seguir essa ordem de prioridade composta, em vez de só o score bruto.
- Limiar de similaridade alterado de 50% (pedido mais cedo hoje) para **60%**, conforme a nova especificação.
- Constante `_SEM_GTIN` reintroduzida (tinha sido removida quando o GTIN foi suspenso como critério independente) — agora usada só para invalidar GTINs vazios/"sem GTIN" na hora do desempate, não como um critério de match à parte.

### Soluções Aplicadas

- Dois testes sintéticos confirmaram a ordem de prioridade do desempate: (1) quando dois candidatos têm similaridade acima do limiar mas só um bate o GTIN, o candidato com GTIN igual vence, mesmo tendo score de similaridade mais baixo que o outro; (2) quando o GTIN não distingue (nenhum dos dois bate, ou nenhum item tem GTIN), o candidato com o mesmo Valor Total vence, de novo mesmo com score mais baixo.
- Validado contra dados reais desta operação: 9.373 matches + 846 "não declarado" + 6.189 "sem match" = 16.408 (bate com o total da BC2).
- Validado em navegador real (Playwright): "Regerar Matching (BC3)" roda até o fim sem exceções.
- Nenhuma alteração necessária em `interface.py`/`loader.py` desta vez — o rótulo interno do tipo de match continua `SECUNDARIO_FUZZY` (o KPI "Match por Similaridade" já refletia corretamente o critério primário, que continua sendo similaridade).

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (10ª parte do dia) — Limiar de similaridade de volta a 50%

### Resumo

Usuário pediu para baixar o limiar de similaridade de 60% (definido na parte anterior, junto com a nova lógica de desempate por GTIN/Valor) para **50%** novamente. Alterada a constante `LIMIAR_SIMILARIDADE` em `matching.py` para `0.50` (e os comentários/docstring que citavam o valor 0,60 por extenso).

### Resultado

- Dados reais desta operação: 11.822 matches + 846 "não declarado" + 3.740 "sem match" = 16.408 (bate com o total da BC2).
- Reconferido o mesmo alerta de qualidade já registrado quando o limiar foi testado em 50% pela primeira vez (antes da lógica de desempate por GTIN/Valor existir): nas amostras com score mais próximo de 50%, ainda aparecem falsos positivos claros (ex.: "KIT VITA CAPILI..." casado com "KIT SHAMPOO+COND.CLINIHAIR..." de marca diferente; "AZEITE OLIVA..." casado com "AZEITE DE OLIVIA..." de marca diferente) — a lógica de desempate por GTIN/Valor só ajuda quando **há mais de um candidato acima do limiar**; quando só existe um candidato (mesmo que fraco, por volta de 50%), ele é aceito diretamente, sem desempate nenhum para filtrar.
- Validado em navegador real (Playwright): "Regerar Matching (BC3)" roda até o fim sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

---

## Atualização (11ª parte do dia) — Matching em dois níveis: Tipo 1 (GTIN) e Tipo 2 (Valor)

### Resumo

Nova reformulação da lógica de Matching: em vez de um único critério com desempates entre si (como estava desde a 9ª parte de hoje), agora existem **dois tipos de match sequenciais, cada um exigindo DUAS condições ao mesmo tempo** (E, não OU):

- **Tipo 1**: dentro da mesma nota (`CHV_NFE`), mesmo GTIN/EAN **e** similaridade de descrição acima de 90%.
- **Tipo 2** (só para o que sobrou do Tipo 1): mesmo Valor Total do item **e** similaridade de descrição acima de 50%.

Antes, a similaridade sozinha já qualificava um item como candidato, e GTIN/Valor só serviam para escolher entre vários candidatos já qualificados. Agora, GTIN (Tipo 1) e Valor (Tipo 2) viraram **exigências obrigatórias**, não apenas critérios de desempate — o que torna o resultado bem mais rigoroso (menos matches, mas com mais confiança em cada um).

### Funcionalidades Implementadas

- `matching.py` reescrito: novas funções `_match_tipo1_por_nota()` e `_match_tipo2_por_nota()`, cada uma combinando a matriz de similaridade (calculada em lote por nota, como antes) com uma segunda condição obrigatória (GTIN igual ou Valor igual, via comparação vetorizada) antes de aceitar um par como candidato. Novo helper `_atribuir_1_para_1()` (fatorado de código repetido) faz a atribuição gulosa (maior score primeiro, sem reaproveitar item da declaração) tanto para o Tipo 1 quanto para o Tipo 2.
- `loader.consultar_totais_bc3()`: passou a contar `TIPO_1`/`TIPO_2` em vez de `SECUNDARIO_FUZZY`.
- `interface.render_bc3()`: painel agora tem 4 indicadores — "Matches Tipo 1", "Matches Tipo 2", "Não Declarado (nd)", "Sem Match (nm)" — e a legenda da tela explica os dois níveis.

### Soluções Aplicadas

- Dois testes sintéticos confirmaram cada tipo isoladamente: um item com GTIN igual e similaridade alta (>90%) casou corretamente no Tipo 1; um item sem GTIN mas com valor igual e similaridade de 83% (>50%) casou corretamente no Tipo 2.
- Validado contra dados reais desta operação: **Tipo 1 = 0** (confirma o que já se sabia desde a 4ª parte do dia — os dados de código de barras desta operação não batem entre XML e declaração), Tipo 2 = 2.236, "não declarado" = 846, "sem match" = 13.326 (soma = 16.408, bate com o total da BC2). Esse resultado é bem mais conservador que o das versões anteriores de hoje (que chegavam a 9-11 mil matches) — esperado, já que agora cada tipo exige duas condições simultâneas em vez de só uma.
- Validado em navegador real (Playwright): painel mostra os 4 indicadores corretos, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/loader.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

## Atualização (12ª parte do dia) — Correção: GTIN do Tipo 1 nunca batia por causa de zero à esquerda

### Resumo Executivo

Depois de entregar o Matching em dois níveis (11ª parte), o usuário perguntou se a comparação de EAN/código de barras (GTIN) tinha realmente sido feita no Tipo 1. Investigando os dados reais para responder com evidência, foi encontrada a causa raiz de o Tipo 1 sempre dar zero matches nesta operação: **não é falta de dado, é diferença de formato**. O SPED (registro 0200, de onde vem o `COD_BARRA` da BC1/declaração) grava o código de barras padronizado em 14 dígitos (padrão GTIN-14), completando com zero à esquerda quando necessário — ex.: `07898034920103`. Já o XML (campo `cean` do produto, renomeado para `COD_BARRA` na BC2) traz o valor cru como o fornecedor declarou, tipicamente em 13 dígitos (GTIN-13) — ex.: `7898034920103`. Como a comparação era feita caractere a caractere (string exata), o mesmo código de barras nunca batia entre os dois lados: `"7898034920103" != "07898034920103"`. Checagem nos dados reais confirmou isso — a BC2 tinha 3.399 GTINs válidos únicos, a BC1 tinha 1.051, mas a interseção exata (antes da correção) era de apenas 1.

### Funcionalidades Implementadas

- Nova função `_normalizar_gtin()` em `matching.py`: normaliza o GTIN removendo zeros à esquerda (`str.lstrip("0")") antes de comparar os dois lados no Tipo 1. Um valor 100% zeros (`"00000000000000"`, o "sem GTIN" usado pelo SPED) vira string vazia após a remoção dos zeros, caindo naturalmente no conjunto de GTIN inválido/em branco já existente (`_SEM_GTIN`), sem precisar de um caso especial.
- `_match_tipo1_por_nota()` passou a chamar `_normalizar_gtin()` nos dois lados (BC1 e BC2) em vez de só normalizar maiúsculas/espaços.

### Soluções Aplicadas

- Diagnóstico com dados reais desta operação: BC2 (XML) mostra GTINs como `7898034920103` (13 dígitos); BC1 (SPED) mostra o mesmo código como `07898034920103` (14 dígitos, zero à esquerda). BC1 também tem 6.876 linhas com o marcador `"00000000000000"` (equivalente ao "SEM GTIN" da BC2), que passou a ser tratado como GTIN inválido depois da normalização (antes não era filtrado, mas também não causava falso positivo, pois nunca aparecia igual do lado da BC2).
- Teste de casos-limite da nova função (`7898034920103`, `07898034920103`, `00000000000000`, `SEM GTIN`, string vazia, `"0"`, `007898034920103`) confirmou que zero à esquerda é removido corretamente nos dois lados e que todos os marcadores de "sem GTIN" caem como inválidos.
- Validado contra dados reais completos desta operação, antes x depois da correção:
  - Antes: Tipo 1 = 0, Tipo 2 = 2.236, não declarado = 846, sem match = 13.326.
  - Depois: **Tipo 1 = 337**, Tipo 2 = 2.210, não declarado = 846, sem match = 13.015 (soma = 16.408, confere com o total da BC2). Os 337 itens que antes caíam no Tipo 2 (por bater só o valor) agora são corretamente identificados como Tipo 1 (bateram pelo código de barras, critério mais confiável que valor).
- Validado em navegador real (Playwright, porta descartável 8711): painel "Matching (Etapa 1)" mostra "Matches Tipo 1 = 337" após clicar em "Regerar Matching (BC3)", sem exceções na tela.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

## Atualização (13ª parte do dia) — Tipo 2 passa a exigir só similaridade > 60% (sem Valor Total)

### Resumo Executivo

Usuário pediu para simplificar a regra do Tipo 2 (fallback do Matching): em vez de exigir Valor Total (`VL_ITEM`) idêntico **e** similaridade de descrição > 50% (como ficou na 11ª parte do dia), a condição passou a ser exclusivamente **similaridade de descrição > 60%**, dentro da mesma `CHV_NFE` — a exigência de valor foi removida por completo. Pediu também para garantir que o cálculo de similaridade continuasse passando pelo módulo `scoring.py` (que já existia desde a implementação original do Matching, mas tinha ficado sem uso desde a vetorização da 3ª parte do dia, quando `matching.py` passou a chamar `rapidfuzz.process.cdist` diretamente).

### Funcionalidades Implementadas

- Nova função `scoring.matriz_similaridade(descricoes_a, descricoes_b)`: recebe duas listas de descrições e devolve a matriz de similaridade em lote (mesma chamada `rapidfuzz.process.cdist` que antes ficava direto em `matching.py`, só que agora centralizada no módulo de scoring). Mantém o desempenho vetorizado (nenhum laço item a item foi reintroduzido).
- `matching.py`: `_matriz_similaridade()` agora delega para `scoring.matriz_similaridade()` em vez de chamar `rapidfuzz` diretamente — `matching.py` deixou de importar `rapidfuzz` (só usa via `scoring`).
- `_match_tipo2_por_nota()`: removida a comparação de `VL_ITEM` (variáveis `val_bc2`/`val_bc1`/`mask_valor`) — a máscara de aceitação agora é só `matriz > LIMIAR_TIPO2`.
- Constante `LIMIAR_TIPO2` alterada de `0,50` para `0,60`.
- `interface.render_bc3()`: legenda da tela atualizada de "Tipo 2 (fallback) = mesmo Valor Total + similaridade > 50%" para "Tipo 2 (fallback) = similaridade > 60% (sem exigência de Valor Total)".

### Soluções Aplicadas

- Teste sintético: dois itens na mesma nota, com descrições parecidas mas **valores propositalmente muito diferentes** (ex.: XML R$100,00 x declaração R$999,99) — confirmado que o Tipo 2 casa mesmo assim, só pela similaridade de texto, validando que a exigência de valor foi mesmo removida.
- Validado contra dados reais desta operação: Tipo 1 (inalterado, = 337) + Tipo 2 = **9.030** (era 2.210) + não declarado = 846 + sem match = **6.195** (era 13.015) = 16.408. Tempo de execução: ~12s.
- **Alerta de qualidade (reportado ao usuário)**: inspecionando manualmente os matches de Tipo 2 com score mais baixo (perto do novo limiar de 60%), foram encontrados pares claramente incorretos — ex.: "SAB. LIQ. INTIMO CIA NATUREZA 210ML" casou com "COLONIA CIA NATUREZA 300ML SILVER" (produtos diferentes, mesma marca); "N DEO AERO 150ML PROTECT CARE FEMININO" casou com "DES AER NIVEA INVISIBLE POWER 150ML" (marca diferente). Isso é esperado: sem o filtro de valor como segunda condição, a similaridade de texto sozinha aceita pares que têm estrutura de frase parecida mas são produtos distintos — o mesmo padrão de risco já visto em iterações anteriores do dia com limiares baixos (7ª e 10ª partes).
- Validado em navegador real (Playwright, porta descartável 8712): painel mostra a legenda e os KPIs atualizados corretamente, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/scoring.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.
- Risco de falso positivo no Tipo 2 (ver acima) — sinalizado ao usuário, aguardando decisão se quer ajustar o limiar ou reintroduzir algum critério de desempate.

## Atualização (14ª parte do dia) — Tipo 2 volta a exigir mesmo Valor Total

### Resumo Executivo

Logo depois de remover a exigência de Valor Total do Tipo 2 (13ª parte), o usuário pediu para reintroduzi-la — provavelmente reagindo ao alerta de falsos positivos reportado (produtos diferentes da mesma marca casando só por similaridade de texto). Tipo 2 volta a exigir, dentro da mesma `CHV_NFE`, **mesmo Valor Total (VL_ITEM idêntico) E similaridade de descrição > 0,60** (o limiar de 60% definido na parte anterior foi mantido — só a condição de valor foi reintroduzida).

### Funcionalidades Implementadas

- `_match_tipo2_por_nota()`: reintroduzida a comparação de `VL_ITEM` (arredondado a 2 casas decimais) como condição obrigatória, junto com a similaridade — igual à lógica da 11ª parte do dia, só que agora com o limiar em 0,60 em vez de 0,50.
- `interface.render_bc3()`: legenda atualizada de volta para "Tipo 2 (fallback) = mesmo Valor Total + similaridade > 60%".

### Soluções Aplicadas

- Teste sintético: dois itens na mesma nota, um com valor diferente do lado da declaração (não deveria casar) e outro com valor igual (deveria casar) — confirmado que só o item de valor igual casou.
- Validado contra dados reais desta operação: Tipo 1 (inalterado) = 337, Tipo 2 = **1.825** (era 9.030 sem o filtro de valor, e 2.210 na versão com limiar 50%+valor da 12ª parte — a diferença para aquela versão é só o limiar mais alto, 60% em vez de 50%), não declarado = 846, sem match = 13.400. Soma = 16.408, confere.
- Validado em navegador real (Playwright, porta descartável 8713): painel mostra a legenda e os KPIs corretos, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

## Atualização (15ª parte do dia) — Limiar do Tipo 2 reduzido para 40%

### Resumo Executivo

Usuário pediu para baixar o limiar de similaridade do Tipo 2 (fallback) de 60% para **40%**. A exigência de mesmo Valor Total (VL_ITEM idêntico), reintroduzida na parte anterior, foi mantida — só o limiar de similaridade mudou. Alterada a constante `LIMIAR_TIPO2` em `matching.py` de `0.60` para `0.40`, e a legenda em `interface.render_bc3()` atualizada de "similaridade > 60%" para "similaridade > 40%".

### Soluções Aplicadas

- Validado contra dados reais desta operação: Tipo 1 (inalterado) = 337, Tipo 2 = **2.383** (era 1.825 com limiar 60%), não declarado = 846, sem match = 12.842. Soma = 16.408, confere.
- Inspeção manual dos matches Tipo 2 com score mais próximo do novo limiar (40-41%): como a exigência de Valor Total continua ativa, os pares nesse patamar tendem a ser produtos genuinamente parecidos com nomes bem abreviados/diferentes nos dois lados (ex.: "PIC 60G MARENI COCO" x "PICOLE DE COCO 60G") — mas também apareceram casos ambíguos onde a marca parece diferente entre XML e declaração (ex.: "GRANOLA TRADIC C/CASTANHAS ALLNUTRI" x "GRANOLA KOBBER TRADICIONAL", mesmo valor) — risco de falso positivo menor do que quando o Valor Total não era exigido (13ª parte), mas ainda existe quando dois produtos distintos de marcas diferentes coincidem no preço.
- Validado em navegador real (Playwright, porta descartável 8714): painel mostra a legenda e os KPIs corretos, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.
- Risco residual de falso positivo no Tipo 2 quando dois produtos de marcas diferentes coincidem em Valor Total (ver acima) — sinalizado ao usuário.

## Atualização (16ª parte do dia) — Limiar do Tipo 2 de volta a 60%

### Resumo Executivo

Usuário pediu para voltar o limiar de similaridade do Tipo 2 para **60%** (era 40%, definido na parte anterior). Alterada a constante `LIMIAR_TIPO2` em `matching.py` de volta para `0.60`, e a legenda em `interface.render_bc3()` ajustada de "similaridade > 40%" para "similaridade > 60%" — a exigência de mesmo Valor Total (VL_ITEM) permanece.

### Soluções Aplicadas

- Validado contra dados reais desta operação: Tipo 1 (inalterado) = 337, Tipo 2 = **1.825** (idêntico ao resultado da 14ª parte do dia, quando o limiar era 60% pela primeira vez — confirma que a mudança é reversível e determinística), não declarado = 846, sem match = 13.400.
- Validado em navegador real (Playwright, porta descartável 8715): painel mostra a legenda e os KPIs corretos, sem exceções.

### Arquivos Modificados

- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/matching.py`
- `OPERACOES/geraldo_2020_2024/ESSENCIAL/app/interface.py`
- Sincronizado (idêntico) em `PB2` e `_MODELO_OPERACAO`.

### Pendências

- Mesma pendência de sempre: as 241 duplicatas em `nfe_entradas` (notas autoemitidas, emitente=destinatário) seguem sem tratamento.
- Mesma pendência de sempre: corrigir o rótulo de `fatoitemnfe_infnfe_det_nitem` no `DICIONARIO DE CAMPOS.txt`, se o usuário quiser nomes mais precisos.

## Próximos Passos

- Aguardando os próximos passos que o usuário disse que traria (mensagem do fim do dia anterior, 2026-07-05).
