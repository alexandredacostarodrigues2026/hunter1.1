# Atualização 2026-08-14

## Resumo Executivo

Refinamento da busca por curinga (`_padrao_busca_curinga()`, criada em
2026-08-12) pedido via Solicitação Técnica: suporte a âncoras de
início/fim (`^`/`$`) e busca por interseção — múltiplos termos em
qualquer ordem, via Positive Lookahead — aproximando a sintaxe de
`LIKE` do SQL/Qlik Sense. Duas ambiguidades reais da especificação
foram esclarecidas com o usuário antes de implementar (ver Decisões
Técnicas). Testado isoladamente (contra os exemplos exatos da
Solicitação Técnica) e ao vivo na geraldo_2020_2024 com dados reais,
propagado pras outras 3 operações.

---

## Funcionalidades Implementadas

- **`_padrao_busca_curinga()` (interface.py) refeita** com 5 regras, em
  ordem:
  1. **Sem nenhum `*`**: comportamento ORIGINAL preservado — substring
     simples ("contém"), sem âncora nenhuma (decisão confirmada com o
     usuário — ver Decisões Técnicas).
  2. Não começa com `*` → prefixa `^` (só casa quem COMEÇA com o 1º
     fragmento). Ex.: `mac*` → `^mac.*`.
  3. Não termina com `*` → sufixa `$` (só casa quem TERMINA com o
     último fragmento). Ex.: `*mac` → `.*mac$`.
  4. Lado com `*` na borda vira `.*` solto (sem âncora) naquele lado.
     Ex.: `*mac*` → `.*mac.*` (idêntico ao comportamento anterior).
  5. **Interseção (qualquer ordem)**: só quando o texto COMEÇA com `*`
     E tem mais de um fragmento não-vazio — monta um Positive Lookahead
     por fragmento em vez de exigir ordem linear. Ex.: `*mor*mac*` →
     `^(?=.*mor)(?=.*mac).*$` — acha "PICOLE DE MORANGO" e "MORANGO
     PICOLE" com o mesmo termo de busca. Fragmentos vazios (bordas ou
     `**` consecutivo) são ignorados.
  Todo fragmento continua escapado com `re.escape()` — sem regressão na
  proteção contra caracteres especiais em descrições de produto.
- **Legenda explicativa** (`st.caption`) adicionada só no Estágio 7.3.3,
  abaixo do campo "Buscar por Descrição" — pedido explícito do usuário
  de escopo restrito por ora ("por enquanto somente no 7.3.3. vamos
  expandir posteriormente"). A melhoria de regex em si já vale pras 7
  telas que chamam a função (mesmo código compartilhado), só a legenda
  ficou restrita.

## Decisões Técnicas (esclarecidas via AskUserQuestion antes de implementar)

- **Texto sem `*` mantém "contém"**: a leitura literal da Solicitação
  Técnica ("se não começar com `*`, prefixa `^`; se não terminar com
  `*`, sufixa `$`") faria uma busca simples como `mac` (sem asterisco
  nenhum) virar `^mac$` — match EXATO, quebrando o comportamento atual
  de "contém" já em uso em 7 telas há semanas. Usuário confirmou manter
  "contém" quando não há asterisco algum — as regras de âncora só
  entram em ação quando há pelo menos um `*` no texto.
- **"10.2" esclarecido como fora de escopo por ora**: a Solicitação
  Técnica citava "7.2 e 10.2" como exemplos de outras telas que usam a
  função. Checagem no código mostrou que o Estágio 10.2 ("⚖️ Cruzamento
  Final do Produto") não tem NENHUM campo de busca por descrição hoje —
  as telas com "🔎 Buscar por descrição do XML" ficam nas abas
  Entradas/Saídas/Estoque do Estágio 10 (Rubrica), mas usam busca
  literal simples (`regex=False`), não `_padrao_busca_curinga()`.
  Usuário respondeu "por enquanto somente no 7.3.3. vamos expandir
  posteriormente" — não migrou essas 3 telas da Rubrica pra sintaxe
  curinga nesta sessão.
- **Função mantida em `interface.py`** (não movida pra `loader.py`,
  apesar do cabeçalho da Solicitação Técnica citar "loader.py") — a
  função já vivia em `interface.py` desde 2026-08-12, é usada só ali
  (filtragem client-side sobre DataFrame já carregado em memória, não
  consulta SQL), e mover exigiria wiring de import sem ganho real.
  Sinalizado no resumo ao usuário, não uma decisão unilateral silenciosa.

## Testes Realizados

- **Isolado** (script direto, sem UI): os 5 exemplos literais da
  Solicitação Técnica batem exatamente com a regex gerada (`mac*` →
  `^mac.*`, `*mac` → `.*mac$`, `*mac*` → `.*mac.*`, `*mor*mac*` →
  `^(?=.*mor)(?=.*mac).*$`, `mac` sem asterisco → `mac` puro). Testes
  funcionais adicionais (`re.search` real contra strings de exemplo)
  confirmaram os 4 comportamentos (inicia/termina/contém/interseção) e
  que o caso sem asterisco continua achando substring em qualquer
  posição.
- **UI ao vivo** (Playwright, geraldo real, Estágio 7.3.3, termo "leite"/
  variações): `leite` (sem `*`) → 1.144 linhas; `leite*` (inicia) → 287;
  `*integral` (termina) → 8; `*po*` (contém) → 3.617; `*leite*po*`
  (ordem normal) → 99; `*po*leite*` (ordem invertida) → 99 — as duas
  ordens de busca acharam exatamente o mesmo total, confirmando que a
  interseção por Lookahead ignora a ordem dos termos como esperado.
  Zero exceções JS.
- **Bug encontrado e corrigido no próprio teste**: a 1ª versão da
  legenda usava `st.caption("...'mac*'...")` sem escapar os asteriscos
  — Streamlit renderiza `st.caption` como Markdown, então `*mac*` virava
  itálico e os asteriscos literais SUMIAM da tela (`'mac*'` aparecia
  como `'mac'`, sem o asterisco visível) — o oposto do que a legenda
  deveria ensinar. Corrigido escapando com `\*` (r-string) em todos os
  4 exemplos da legenda; reconferido na tela — asteriscos aparecem
  literalmente agora.

## Arquivos Modificados

- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/interface.py`

`loader.py` não foi alterado — ver Decisões Técnicas (função mantida em
`interface.py`).

## Pendências

- Nenhuma pendência de código conhecida.
- Legenda e sintaxe curinga completa ainda restritas ao 7.3.3 — as
  outras 6 telas que chamam `_padrao_busca_curinga()` (7.2, 7.2.1, 7.3,
  7.3.1, 7.3.2, 11) já ganharam a capacidade de âncora/interseção
  silenciosamente (mesma função compartilhada), mas sem a legenda
  explicando — usuário sinalizou que vai pedir a expansão depois.
- As 3 telas de "Buscar por descrição do XML" do Estágio 10 (Rubrica)
  continuam com busca literal simples (`regex=False`), fora do escopo
  desta sessão.

## Próximos Passos

- Aguardar pedido do usuário pra expandir a legenda (e, se for o caso,
  migrar a Rubrica do Estágio 10 pra sintaxe curinga também).

---

# Parte 2 — Seletor de produto nos Relatórios Analíticos (Estágio 12)

## Resumo Executivo

Solicitação Técnica "SELEÇÃO DE ITEM PARA RELATÓRIOS ANALÍTICOS":
antes, os 3 relatórios analíticos do Estágio 12 (12.2 Itens Cruzados,
12.3 MC Quantidades, 12.4 MC Preços Unitários) usavam obrigatoriamente
o produto escolhido GLOBALMENTE pra cruzamento no Estágio 10
(`produto_cruzamento_escolhido`) — o auditor precisava voltar no
Estágio 10 e trocar o produto em cruzamento só pra ver o relatório de
outro item. Agora a própria aba de Relatórios tem um seletor local, que
lista qualquer produto com item confirmado na Rubrica, sem afetar a
escolha global. Investigação prévia (agente Explore) mostrou que as
funções de geração de dados/PDF já aceitavam `escolhido` como parâmetro
— só faltava a interface deixar escolher outro. Implementado, testado
(lógica isolada + UI ao vivo, produto diferente do global), propagado
pras 4 operações.

## Funcionalidades Implementadas

- **`loader._enriquecer_escolhido(escolhido)`**: extraído de dentro de
  `consultar_produto_cruzamento_escolhido()` (refatoração pura, sem
  mudança de comportamento pra quem já chamava essa função) — adiciona
  IS_ST/DESCR_EDITADA/UNID_ALVO/UNID_EDITADA a um dict com DESCR_ALVO/
  COD_ITEM, via lookup ao vivo em `produto_alvo_fiscalizacao`.
- **`loader.consultar_produtos_disponiveis_relatorios_12()`**: `SELECT
  DISTINCT DESCR_ALVO, COD_ITEM FROM cruzamento_confirmado_detalhado`,
  ordenado por DESCR_ALVO — lista produtos com pelo menos 1 confirmação
  na Rubrica (não só o escolhido globalmente). Regra R07 aplicada.
- **`loader.montar_escolhido_local(descr_alvo, cod_item)`**: monta um
  dict `escolhido` no MESMO formato do global (via `_enriquecer_
  escolhido()`), mas pra QUALQUER produto — TS fica vazio (não é uma
  escolha persistida, só a seleção atual do combo). Não lê nem grava
  `produto_cruzamento_escolhido` — escolher no seletor local não afeta
  o Estágio 10, e vice-versa.
- **`interface._selecionar_produto_relatorio_12()`** (novo): `st.
  selectbox("🔎 Selecione o produto para o relatório analítico", ...)`
  com key de session_state ÚNICA e COMPARTILHADA pelos 3 relatórios
  analíticos (`relatorios12_produto_selectbox`) — trocar entre 12.2/
  12.3/12.4 não reseta a escolha (importante: 12.2 e 12.3 comparam pelo
  MESMO LOC entre si, trocar de produto no meio da conferência
  quebraria essa comparação). Pré-seleciona o produto escolhido
  GLOBALMENTE quando ele estiver na lista disponível — preserva o
  comportamento de hoje como padrão, o auditor só precisa trocar se
  quiser ver outro produto. `st.info` orientando se não há nenhum
  produto com confirmação na Rubrica ainda.
- **`render_pagina_relatorios()` refatorada**: RELATÓRIO FINAL (12.1)
  continua sem seletor (visão consolidada de todos os produtos, sem
  mudança); os 3 analíticos passam por `_selecionar_produto_relatorio_
  12()` antes de despachar pra `_render_relatorio_itens_cruzados(
  escolhido)`/`_render_relatorio_mc_quantidades(escolhido)`/`_render_
  relatorio_mc_pu(escolhido)` — as 3 funções passaram a RECEBER
  `escolhido` como parâmetro em vez de buscar `loader.consultar_
  produto_cruzamento_escolhido()` internamente (a checagem "nenhum
  produto escolhido" migrou pro seletor compartilhado, não é mais
  responsabilidade de cada função individual).

## Testes Realizados

- **Isolado** (script direto contra o banco real da geraldo):
  `consultar_produtos_disponiveis_relatorios_12()` achou 6 produtos com
  confirmação na Rubrica; `montar_escolhido_local()` pro produto
  globalmente escolhido devolveu exatamente os mesmos campos
  enriquecidos que `consultar_produto_cruzamento_escolhido()` (prova de
  que a refatoração não mudou o comportamento existente); testado
  também com um produto DIFERENTE do global ("CERV SKOL LATA 350ML") —
  as 3 funções de dados (itens cruzados 197 linhas, MC quantidades 197,
  MC PU 143) e os 3 PDFs geraram normalmente.
- **UI ao vivo** (Playwright, geraldo real): seletor de produto AUSENTE
  em "RELATÓRIO FINAL" (0 ocorrências) — confirma que o 12.1 não ganhou
  seletor, como pedido; seletor PRESENTE nos 3 analíticos; abrindo
  "RELATÓRIO ITENS CRUZADOS" pela 1ª vez, o seletor já vem com "AGUA
  PURIFIC 20LTS" pré-selecionado (o produto globalmente escolhido no
  momento do teste) — confirma o default; trocando manualmente pra
  "CERV SKOL LATA 350ML", o texto "Item Cruzado:" atualizou
  IMEDIATAMENTE pro novo produto (mesma tela, sem precisar recarregar);
  trocando o relatório em cima pra "RELATÓRIO MC QUANTIDADES" e depois
  "RELATÓRIO MC PREÇOS UNITÁRIOS", o produto selecionado CONTINUOU
  sendo "CERV SKOL LATA 350ML" nos dois — confirma que a key
  compartilhada de session_state preserva a escolha entre os 3
  relatórios. Zero exceções JS em todo o fluxo.

## Arquivos Modificados

- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/loader.py`
- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/interface.py`

## Pendências

- Nenhuma pendência de código conhecida.

## Próximos Passos

- Nenhum identificado além do backlog geral do projeto.

---

# Parte 3 — Painel 1: Procedimentos Iniciais (unificação Extração + Matching BC3)

## Resumo Executivo

Solicitação Técnica "PAINEL 1 — PROCEDIMENTOS INICIAIS": unificar o
Estágio 1 (Extração) e o Estágio 2 (Matching BC3) — carregar os dados
(XML+SPED) agora dispara automaticamente o Matching (BC3) na mesma
tela, exibindo de imediato os KPIs de taxa de match (D1-D6/A1-A5/ND/NM)
com quantidade e percentual sobre o total real da BC2. O botão do Menu
Principal foi renomeado pra "🛠️ 1: PROCEDIMENTOS INICIAIS". Investigação
prévia (agente Explore) mapeou a sequência exata de chamadas hoje; o
painel de Matching manual (Estágio 2) foi mantido intacto, sem nenhuma
mudança de comportamento, só ganhou uma execução automática adicional
gatilhada pela carga. Testado com uma recarga REAL e completa da base
da geraldo (não simulação) — fluxo inteiro (NF-e → SPED → Matching)
rodou sem erros e o painel de KPIs apareceu corretamente. Propagado
pras 4 operações.

## Funcionalidades Implementadas

- **`loader.consultar_total_bc2()`** (nova): `COUNT(*) FROM nfe_bc2` —
  denominador real das porcentagens. `loader.consultar_totais_bc3()`
  foi mantida INTOCADA de propósito (ainda devolve só contagens brutas,
  `dict[str,int]`) — evita qualquer risco de quebrar `render_bc3()`
  (Estágio 2), que já consome essa função hoje; o cálculo de % fica
  local à nova tela (`interface._render_resultado_matching_inicial()`).
  Confirmado com dado real: `sum(consultar_totais_bc3().values())` =
  `consultar_total_bc2()` = 16.395 na geraldo (todo item da BC2 vira
  exatamente 1 linha na bc3, sem sobra nem perda, por construção do
  motor de matching) — serve como conferência de integridade também.
- **`render_carga_operacao()` (interface.py) estendida**: depois que
  `ok_nfe and ok_sped` (mesma condição de sempre), antes do
  `st.session_state["dados_carregados"] = True` + `st.rerun()`, agora
  roda `loader.persistir_bc3()` dentro de um `st.spinner(...)` (mesmo
  texto/padrão que `render_bc3()` já usa — `persistir_bc3()` não expõe
  progresso granular internamente, só 1 callback no fim, então não dá
  pra ter uma barra `st.progress()` de verdade, só o spinner). Erro no
  Matching NÃO invalida a carga (`dados_carregados` já foi marcado True
  antes) — fica registrado em `session_state["erro_bc3_automatico"]`
  (padrão "flag consumida com `.pop()`, não reaparece nos reruns
  seguintes", já usado em outras partes do app) e exibido como
  `st.error()` no topo da própria função na próxima renderização,
  orientando a gerar manualmente em "🧩 MATCHING (BC3)" se precisar.
- **`interface._render_resultado_matching_inicial()`** (nova): mesmo
  formato visual de `render_bc3()` (2 linhas de `st.columns(7)`, CSS
  escopado por `st.container(key=...)` pra fonte reduzida — mesmo
  padrão já usado em vários painéis de KPI do app), mas cada `st.metric`
  ganha um `delta` com a % sobre a BC2 (`delta_color="off"`, texto
  cinza — sem semântica de "aumento/queda", só um percentual estático).
  Efeito colateral cosmético observado e aceito: Streamlit desenha uma
  setinha cinza (↑) antes da % mesmo com `delta_color="off"` (a cor fica
  neutra, mas o ícone de seta não some sozinho quando o texto do delta
  não começa com "-") — não é um bug funcional, só um detalhe visual;
  não corrigido nesta sessão.
- **`render_pagina_extracao()` estendida**: depois de `render_entidade_
  auditada()`, se `bc3_ja_gerada()`, chama `_render_resultado_matching_
  inicial()` — cobre tanto o fluxo automático (carga acabou de rodar) 
  quanto reabrir a página depois (BC3 já persistida de antes).
- **Botão do Menu Principal renomeado**: `"📥 EXTRAÇÃO"` →
  `"🛠️ 1: PROCEDIMENTOS INICIAIS"` — `key`/`pagina_ativa` internos
  (`btn_menu_extracao`/`"extracao"`) mantidos sem alteração (só o texto
  visível mudou, evita ter que caçar toda referência interna). As 15
  mensagens `st.info('Carregue os dados primeiro em "📥 EXTRAÇÃO".')`
  espalhadas pelo app (uma por página que exige `dados_carregados`)
  foram atualizadas em bloco pro novo rótulo, mais 2 outras menções
  pontuais (Estágio 10.2 e Auditoria de Divergência) — evita instruir o
  auditor a procurar um botão que não existe mais com esse texto.
- **Painel de Matching manual (Estágio 2) preservado sem NENHUMA
  mudança** — continua existindo, com seu próprio botão "Gerar/Regerar
  Matching (BC3)", pro auditor rodar de novo manualmente se precisar
  (ex.: depois de corrigir algo na BC1/BC2 sem recarregar tudo).

## Testes Realizados

- **Isolado**: `consultar_total_bc2()` e `consultar_totais_bc3()`
  testados contra o banco real da geraldo — soma das 13 categorias
  bate exatamente com o total da BC2 (16.395=16.395); percentuais
  calculados manualmente conferem com a tela (D2 57,9%, D6 15,4%, etc.).
- **UI ao vivo, ponta a ponta, RECARGA REAL** (não simulação): clique em
  "🛠️ 1: PROCEDIMENTOS INICIAIS" → "Carregar novamente" na base real da
  geraldo — reprocessou NF-e (9 passos) + SPED (4 passos) + Matching
  (BC3) automaticamente, tudo numa única interação do auditor. Painel
  "🧬 Resultado do Matching Inicial (BC3)" apareceu com os 14 KPIs
  corretos (D1=344, D2=9.490, ..., Taxa de Match 91,7%), zero exceções
  JS. Confirmado por timestamp do arquivo `hunter.duckdb` (modificado
  no exato minuto do teste) que a recarga realmente rodou, não serviu
  dado em cache.
  Verificado adicionalmente: a mudança de rótulo do menu não quebrou
  nenhuma outra tela (Estágio 2/Matching manual continua 100%
  funcional, testado separadamente — botão "Regerar Matching" e os
  KPIs originais de `render_bc3()` intactos).

## Arquivos Modificados

- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/loader.py`
- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/interface.py`

## Pendências

- Nenhuma pendência de código conhecida (o detalhe cosmético da seta
  cinza, sinalizado na Parte 3, deixou de existir — a grade de 14
  métricas que tinha os `delta` foi removida na Parte 4).

## Próximos Passos

- Nenhum identificado além do backlog geral do projeto.

---

# Parte 4 — Simplificação do KPI do Matching Inicial (só Taxa de Match)

## Resumo Executivo

Pedido do usuário logo após a Parte 3: simplificar o painel de
resultado do Matching dentro de "🛠️ 1: PROCEDIMENTOS INICIAIS" — trocar
a grade de 14 métricas (D1-D6/A1-A5/ND/NM, uma por linha) por UM único
KPI (Taxa de Match), com uma barra de progresso visual (`st.progress`)
e uma legenda explicando o que essa taxa representa: "taxa de inclusão
de código de produto no dado do fornecedor". O detalhamento por tipo
continua disponível, sem nenhuma mudança, no painel manual "🧩 MATCHING
(BC3)" (Estágio 2) — só o painel novo (visão rápida) foi simplificado.

## Funcionalidades Implementadas

- **`_render_resultado_matching_inicial()` reescrita**: removida a
  grade de `st.columns(7)` × 2 com CSS escopado; agora é só
  `st.metric("Taxa de Match", ...)` + `st.progress(taxa_match/100)` +
  `st.caption(...)`. Cálculo de `taxa_match` inalterado (mesma fórmula
  de antes: soma de D1-D6/A1-A5 dividida pelo total da BC2).
- **Legenda explicativa** (pedido explícito do usuário, para deixar
  claro o significado da métrica): "Taxa de inclusão de código de
  produto no dado do fornecedor: a BC2 (XML de Entradas de Terceiros)
  chega do fornecedor SEM o código de produto da auditada — o Matching
  (BC3) tenta incluir esse código casando cada item contra a Declaração
  da auditada (BC1). A Taxa de Match é o percentual da BC2 que
  conseguiu esse código incluído." — com um link textual de volta pro
  detalhamento por tipo em "🧩 MATCHING (BC3)", pra quem precisar do
  diagnóstico fino.
- **Correção de data**: os docstrings escritos na Parte 3 citavam
  "2026-08-15"/"2026-08-16" sem confirmação — corrigidos pra
  "2026-08-14" (última data confirmada por system-reminder nesta
  sessão), evitando um registro histórico incorreto no próprio código.
- **`loader.py` não precisou de alteração nesta parte** — `consultar_
  totais_bc3()`/`consultar_total_bc2()` (Parte 3) já forneciam tudo que
  o cálculo de `taxa_match` precisa; a simplificação foi só de exibição.

## Testes Realizados

- **UI ao vivo** (Playwright, geraldo real, banco já com BC3 gerada da
  Parte 3 — não precisou recarregar a base inteira de novo): "Matches
  D1" (e as outras 13 métricas antigas) sumiram da tela (0 ocorrências);
  "Taxa de Match" continua presente; screenshot confirma o layout final
  — `st.metric` grande (91,7%) + barra de progresso preenchida + legenda
  completa explicando o significado + referência ao painel de Matching
  manual para detalhamento. Zero exceções JS.

## Arquivos Modificados

- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/interface.py`
- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,_MODELO_OPERACAO}/ESSENCIAL/app/loader.py`
  (só a correção de data no docstring de `consultar_total_bc2()`, sem
  mudança de lógica)

## Pendências

- Nenhuma pendência de código conhecida.

## Próximos Passos

- Nenhum identificado além do backlog geral do projeto.
