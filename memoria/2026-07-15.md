# Atualização 2026-07-15

## Resumo Executivo

Quatro frentes no dia. (1) Usuário pediu pra gerar as regras de data para
ET/EP: localizado o texto original (`regra de negócios unificadas/
DIRETRIZES DA APLICAÇÃO.txt`, rotulado "modelo antigo"), comparado com a
hierarquia `DATA_ELEITA` do Estágio 4 e, após confirmação direta do
usuário, documentado que a hierarquia implementada (não o modelo antigo)
é a regra final. (2) Usuário trouxe uma "Solicitação Técnica" formal pra
preservar a data de emissão original do XML (`dhEmi`) como campo de
auditoria paralelo à `DATA_ELEITA` — implementado `DATA_ORIGINAL`/
`ANO_ORIGINAL`. (3) Usuário trouxe uma segunda "Solicitação Técnica"
pedindo pra "simplificar" rótulos de origem da data que, na descrição
dele, já existiam de forma detalhada (`'1-DT_E_S'`, `'2-DT_FIN'`) —
verifiquei o código real e **essa coluna não existia em nenhuma forma**
antes de hoje; sinalizei a divergência ao usuário antes de mexer (mesmo
princípio de "anexo não corresponde ao descrito"), ele confirmou pra criar
do zero já simplificada. Implementado `DATA_ELEITA_ORIGEM`
(`'declaração'`/`'xml'`), regenerado nas 3 operações reais + `_MODELO_
OPERACAO` a pedido explícito do usuário. (4) Usuário trouxe uma terceira
"Solicitação Técnica" pra um "novo" botão/painel "AUDITORIA1: COMPARAÇÃO
ENTRADAS-SAÍDAS-ESTOQUES" — pesquisa mostrou que **quase tudo já existia**
(`loader.auditar_divergencia_entradas()`, os 5 indicadores, o botão
"Investigar Chaves Divergentes", o caminho do Excel, o critério
CHV_NFE+contagem); só faltava um botão de navegação dedicado (o painel
vivia escondido no fim de "TABELAS ENTRADAS/SAÍDAS/ESTOQUES"). Implementei
só a parte que faltava — novo botão + página `"auditoria1"` — sem tocar na
lógica de negócio já correta. Todas as mudanças de código sincronizadas
nas 4 operações, testadas via backend real (geraldo, regenerado múltiplas
vezes) e via UI (Playwright, porta descartável 8601).

## Funcionalidades Implementadas

### `DATA_ORIGINAL`/`ANO_ORIGINAL` (Estágio 4)

- `loader._aplicar_data_eleita()`: além de `DATA_ELEITA`/`ANO_ELEITO`,
  agora também cria `DATA_ORIGINAL`/`ANO_ORIGINAL` a partir de `dhEmi`
  (`fatonfe_infnfe_ide_dhemi`, campo já existente no XML) — reaproveita
  `_candidato_data_ano()` (já validava/extraia esse mesmo campo como 4ª
  prioridade da hierarquia). Sempre o mesmo valor pros dois cenários
  (ET/EP), nunca tocado pela lógica de prioridade — é dado "cru" do XML,
  não sofre a hierarquia SPED > XML.
- `loader.persistir_estoque_entradas_saidas()`: `DATA_ORIGINAL`/
  `ANO_ORIGINAL` entraram na lista de colunas forçadas a string (Regra
  R07), junto de `DATA_ELEITA`/`ANO_ELEITO` (nunca `NULL` de verdade,
  sempre `""` na pior hipótese).
- `interface._COLUNAS_PREVIEW_ESTOQUE_ENTRADAS_SAIDAS`: painel "Estágio 4
  — Entradas e Saídas Enriquecidas" ganhou `DATA_ORIGINAL`/`ANO_ORIGINAL`
  ao lado de `DATA_ELEITA`/`ANO_ELEITO` na prévia.
- `DICIONARIO DE CAMPOS.txt`: nova seção `2026-07-15` —
  `DATA_ELEITA;Data Eleita (Hunter)`, `ANO_ELEITO;Ano Eleito` (não tinham
  entrada até então, mostravam o nome técnico cru na UI),
  `DATA_ORIGINAL;Data Emissao (Original)`, `ANO_ORIGINAL;Ano Original`.
- `docs/estagios/04_cronologia_ano_eleito.md`: novo passo "2b. Data
  original" descrevendo o campo, mais nota na "Regra original de datas
  ET/EP" já escrita mais cedo no dia.

### `DATA_ELEITA_ORIGEM` (Estágio 4)

- `loader._aplicar_hierarquia_data()`: mudou de assinatura — agora
  devolve `(valor, ano, origem)` em vez de só `(valor, ano)`. `origem` é
  montada com o mesmo padrão `combine_first` das outras duas, usando um
  dicionário `_ORIGEM_POR_COLUNA` que mapeia cada coluna candidata da
  hierarquia pro rótulo simplificado: `DT_E_S`/`DT_FIN` → `'declaração'`;
  `dhSaiEnt`/`dhEmi` → `'xml'`.
- `loader._aplicar_data_eleita()`: cria `DATA_ELEITA_ORIGEM` combinando
  `origem_a`/`origem_b` pela mesma máscara `mask_cenario_a` já usada pra
  `DATA_ELEITA`/`ANO_ELEITO`.
- `loader.persistir_estoque_entradas_saidas()`: `DATA_ELEITA_ORIGEM`
  entrou na lista de colunas forçadas a string (Regra R07).
- `interface._COLUNAS_PREVIEW_ESTOQUE_ENTRADAS_SAIDAS` e
  `DICIONARIO DE CAMPOS.txt` (`DATA_ELEITA_ORIGEM;Origem da Data`).
- `docs/estagios/04_cronologia_ano_eleito.md`: novo passo "2a. Origem
  simplificada", nota nos totais reais (14.896/4.537 em `estoque_entradas`,
  157/92.284 em `estoque_saidas`) e atualização da lista R07.

### Botão "AUDITORIA1: COMPARAÇÃO ENTRADAS-SAÍDAS-ESTOQUES" (Estágio 6)

- `interface.render_menu_principal()`: 5º botão (`st.columns(5)`, era 4),
  posicionado após "TABELAS ENTRADAS / SAÍDAS / ESTOQUES", seta
  `pagina_ativa="auditoria1"`.
- Nova `interface.render_pagina_auditoria1()`: botão de retorno + (se
  `dados_carregados`) `render_auditoria_divergencia_entradas()` — reusa a
  função já existente (2026-07-13), sem duplicar lógica.
- `render_auditoria_divergencia_entradas()` saiu do fim de
  `render_pagina_construcao()` (só ela chamava antes).
- `main.py`: novo `elif pagina == "auditoria1"`, docstring/comentário
  atualizados de "4 grupos" pra "5 grupos".
- `docs/estagios/06_menu_navegacao.md`: nova seção "AUDITORIA1 ganhou
  botão próprio (2026-07-15)", seção antiga "Decisão de agrupamento"
  marcada como histórico/superada.

## Correções Realizadas

- `render_auditoria_divergencia_entradas()`: quando a operação não tem o
  Excel de referência, antes retornava silenciosamente (`return` sem
  desenhar nada) — fazia sentido quando o painel vivia embutido no meio de
  outros (silêncio = menos poluição visual), mas numa página dedicada
  ("AUDITORIA1") isso deixaria a tela em branco, parecendo quebrada.
  Trocado por um `st.info()` explicando que o estudo só se aplica a quem
  tem esse Excel (hoje, só geraldo). Validado ao vivo: testei tanto o
  caminho com Excel (geraldo, 5 KPIs + drill-down funcionando) quanto sem
  (PB_2023_2025, mensagem amigável em vez de página em branco).

## Alterações de Banco de Dados

- `estoque_entradas`/`estoque_saidas` ganham 3 colunas novas
  (`DATA_ORIGINAL`, `ANO_ORIGINAL`, `DATA_ELEITA_ORIGEM`). Regeneradas e
  validadas nas 3 operações reais, a pedido do usuário ("regenere as duas
  e a modelo"):
  - `geraldo_2020_2024`: 19.433/92.441 — `DATA_ELEITA_ORIGEM`
    14.896 `'declaração'`/4.537 `'xml'` (entradas), 157/92.284 (saídas).
  - `PB_2023_2025`: 4.564/11.362 — 1.673 `'declaração'`/2.891 `'xml'`
    (entradas, bate com o `COD_ITEM_DECLARACAO` real já documentado),
    4/11.358 (saídas).
  - `cometa_2021_2025`: 10.802/179.349 — 8.312 `'declaração'`/2.490
    `'xml'` (entradas), 119/179.230 (saídas).
  - `_MODELO_OPERACAO`: rodou 0/0 (esperado — template em branco, sem
    XML/SPED carregado). Efeito colateral: criou `ESSENCIAL/banco/
    hunter.duckdb` vazio pela primeira vez (antes não existia arquivo de
    banco nenhum ali) — inofensivo, mas registrado pra não confundir
    numa sessão futura.

## Arquivos Criados

- `memoria/2026-07-15.md` (este arquivo)

## Arquivos Modificados

- `docs/estagios/04_cronologia_ano_eleito.md` — seção "Regra original de
  datas ET/EP (histórico)" (manhã) + passos "2a. Origem simplificada" e
  "2b. Data original" (tarde) + R07 atualizada + nota nos totais reais.
- `docs/estagios/06_menu_navegacao.md` — nova seção "AUDITORIA1 ganhou
  botão próprio (2026-07-15)" + seção "Decisão de agrupamento" marcada
  como histórico + "Como funciona" atualizado (5 botões/páginas).
- `DICIONARIO DE CAMPOS.txt` — seção `2026-07-15` (4 campos).
- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,
  _MODELO_OPERACAO}/ESSENCIAL/app/loader.py` —
  `_aplicar_hierarquia_data()` (mudou de assinatura, 2→3 valores de
  retorno), `_aplicar_data_eleita()`, `montar_estoque_entradas()`/
  `montar_estoque_saidas()`, `persistir_estoque_entradas_saidas()`,
  `consultar_estoque_entradas_saidas()` (docstrings + lógica de
  `DATA_ORIGINAL`/`ANO_ORIGINAL`/`DATA_ELEITA_ORIGEM`). Nenhuma mudança em
  `auditar_divergencia_entradas()` — já satisfazia a solicitação técnica.
- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,
  _MODELO_OPERACAO}/ESSENCIAL/app/interface.py` —
  `_COLUNAS_PREVIEW_ESTOQUE_ENTRADAS_SAIDAS` + caption/docstring de
  `render_estoque_entradas_saidas()` + `render_menu_principal()` (5º
  botão) + nova `render_pagina_auditoria1()` + `render_pagina_construcao()`
  (perdeu a chamada de `render_auditoria_divergencia_entradas()`) +
  `render_auditoria_divergencia_entradas()` (aviso amigável sem Excel).
- `OPERACOES/{geraldo_2020_2024,PB_2023_2025,cometa_2021_2025,
  _MODELO_OPERACAO}/ESSENCIAL/app/main.py` — novo `elif
  pagina == "auditoria1"`, docstring/comentário atualizados.

## Problemas Encontrados

- **Divergência identificada entre texto original e implementação**
  (frente 1): a hierarquia `DATA_ELEITA` implementada pro Cenário A (ET)
  faz o oposto do modelo antigo de `DIRETRIZES DA APLICAÇÃO.txt` —
  prioriza `DT_E_S`/`DT_FIN` (SPED) sobre o XML incondicionalmente, sem
  checagem de divergência VTN/VTP/quantidade nem de declaração
  extemporânea. **Resolvido na mesma sessão**: usuário confirmou (após
  selecionar a tabela da hierarquia no editor e escrever "deve ser isso")
  que a hierarquia implementada é a regra final — o texto original fica
  só como histórico.
- **Estado inesperado do repositório notado durante a sessão**: `git
  status` mostrou `OPERACOES/PB2`/`OPERACOES/cometa` como deletados (não
  commitados) e `OPERACOES/PB_2023_2025`/`OPERACOES/cometa_2021_2025`
  como pastas novas não rastreadas — rename feito fora desta sessão
  (provavelmente pelo usuário diretamente, sem `git mv`). Sinalizado ao
  usuário, não mexido. As mudanças de `DATA_ORIGINAL`/`ANO_ORIGINAL`
  foram aplicadas nos nomes de pasta *atuais* (`PB_2023_2025`/
  `cometa_2021_2025`), não nos antigos.
- **Premissa falsa na 2ª Solicitação Técnica**: o pedido descrevia
  `_aplicar_hierarquia_data` como já tendo rótulos detalhados de origem
  (`'1-DT_E_S'`, `'2-DT_FIN'` etc.) a serem "simplificados". Grep completo
  em `loader.py`/`interface.py` das 4 operações, nos docs do Estágio 4 e
  no `DICIONARIO DE CAMPOS.txt` confirmou que **nenhuma coluna de origem
  existia antes de hoje**, em forma alguma. Parado antes de implementar
  (mesmo princípio de "conteúdo não corresponde ao descrito" — ver
  `feedback_anexo_nao_corresponde` na memória do Claude) e perguntado ao
  usuário como proceder; ele confirmou "criar do zero observando a
  nomenclatura do Estágio 4" — implementado assim, sem versão detalhada
  intermediária.
- **3ª Solicitação Técnica quase toda já implementada**: o pedido "AUDITORIA1"
  descrevia como trabalho novo várias coisas que já existiam de verdade —
  `loader.auditar_divergencia_entradas()` já usava `estoque_entradas`
  (Estágio 4) como base do Hunter (não tabela bruta de XML, como o pedido
  presumia precisar "garantir"), já cruzava só por `CHV_NFE`+contagem
  (nunca por código de produto), já lia o Excel pelo caminho exato pedido
  (`_OPERACAO_DIR.glob("TABELA ENTRADAS*.xlsx")`, que pra geraldo já
  resolve pro caminho literal da solicitação) e já tinha os 5 indicadores
  + botão de drill-down. Diferente da 2ª solicitação (premissa 100% falsa),
  aqui a pesquisa (`Explore` agent) confirmou que só faltava mesmo a parte
  de navegação — não precisei parar/perguntar de novo, já que não havia
  ambiguidade sobre o que construir, só transparência sobre o que já
  existia (registrado no resumo executivo e na resposta ao usuário).

## Soluções Aplicadas

- Confirmação direta do usuário resolveu a dúvida sobre qual hierarquia
  de datas prevalece (frente 1).
- `DATA_ORIGINAL`/`ANO_ORIGINAL` implementado reaproveitando
  `_candidato_data_ano()` já existente (sem duplicar lógica de validação
  de formato ISO 8601) — só chamado de novo com a coluna `dhEmi` fora da
  hierarquia de prioridade.

## Decisões Técnicas

- `DATA_ELEITA`/`ANO_ELEITO` ganharam entrada no `DICIONARIO DE
  CAMPOS.txt` nesta sessão (não tinham antes) — aproveitado o pedido do
  usuário, que já especificava os rótulos exatos ("Data Eleita (Hunter)"/
  "Ano Eleito") na solicitação técnica.
- Hierarquia `DATA_ELEITA` (SPED antes do XML pro ET) confirmada pelo
  usuário como regra definitiva, superando o modelo antigo de
  `DIRETRIZES DA APLICAÇÃO.txt`. Nenhuma mudança em
  `loader._aplicar_data_eleita()` na lógica de prioridade — só adição de
  campo novo e paralelo.
- `DATA_ELEITA_ORIGEM` implementado com só 2 valores possíveis
  (`'declaração'`/`'xml'`), sem versão intermediária com rótulos por
  coluna (`'DT_E_S'`/`'DT_FIN'`/`'dhSaiEnt'`/`'dhEmi'` individuais) —
  decisão explícita do usuário na solicitação técnica, mesmo a coluna
  nunca tendo existido antes.
- "AUDITORIA1" implementado como reposicionamento de navegação, não
  reimplementação — `render_auditoria_divergencia_entradas()` reusada tal
  qual, sem duplicar/reescrever a função. Único ajuste de comportamento
  foi o `st.info()` no caso sem Excel (ver "Correções Realizadas"), que
  não muda nenhum cálculo, só a experiência de quem acessa a página vazia.

## Pendências

- ~~Regenerar `estoque_entradas`/`estoque_saidas` em `PB_2023_2025` e
  `cometa_2021_2025`~~ — **resolvido**: usuário pediu explicitamente
  ("regenere as duas e a modelo"), rodado nas 3 operações + `_MODELO_
  OPERACAO`, ver "Alterações de Banco de Dados".
- Esclarecer com o usuário a causa do rename
  `PB2`→`PB_2023_2025`/`cometa`→`cometa_2021_2025` fora do controle de
  versão, e se deve ser commitado.

## Próximos Passos

- Nenhum próximo passo específico deste tópico — as 3 operações reais e o
  modelo já estão com as colunas novas, e o botão "AUDITORIA1" está no ar
  e testado (com e sem Excel de referência).
