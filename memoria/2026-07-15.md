# Atualização 2026-07-15

## Resumo Executivo

Sessão curta: usuário pediu pra gerar as regras de data para ET/EP.
Localizado o texto original (`regra de negócios unificadas/DIRETRIZES DA
APLICAÇÃO.txt`, rotulado "modelo antigo") e comparado item a item com a
hierarquia `DATA_ELEITA` já implementada no Estágio 4
(`docs/estagios/04_cronologia_ano_eleito.md`). Confirmado com o usuário
(via pergunta direta) que o destino do texto era dentro desse próprio
arquivo, não um novo arquivo solto na raiz. Em seguida, o usuário
selecionou a tabela da hierarquia atual no editor e confirmou (segunda
pergunta direta) que essa hierarquia — não o modelo antigo — é a regra
final: fecha a pendência de VTN/VTP/quantidade/extemporaneidade, que fica
só como registro histórico.

## Funcionalidades Implementadas

Nenhuma — sessão só de documentação, sem mudança de código.

## Correções Realizadas

Nenhuma.

## Alterações de Arquitetura / Banco de Dados / Infraestrutura

Nenhuma.

## Arquivos Criados

- `memoria/2026-07-15.md` (este arquivo)

## Arquivos Modificados

- `docs/estagios/04_cronologia_ano_eleito.md` — nova seção "Regra original
  de datas ET/EP (histórico) — superada pela hierarquia implementada",
  logo antes de "Divergências de dados da bc3". Transcreve o texto
  original de `DIRETRIZES DA APLICAÇÃO.txt` (ET: XML seria a base, SPED
  seria exceção condicional a divergência de VTN/VTP/quantidade ou
  declaração extemporânea; EP: XML seria a base, "regramento das datas"
  não detalhado), documenta a divergência com a implementação e registra a
  confirmação do usuário de que a hierarquia atual é a regra final.

## Problemas Encontrados

- **Divergência identificada entre texto original e implementação**: a
  hierarquia `DATA_ELEITA` implementada pro Cenário A (ET) faz o oposto do
  modelo antigo — prioriza `DT_E_S`/`DT_FIN` (SPED) sobre o XML
  incondicionalmente, sem nenhuma checagem de divergência VTN/VTP/
  quantidade nem de declaração extemporânea (as duas condições que, na
  regra original, seriam o motivo pra *não* usar a data declarada). Pro
  Cenário B (EP), a implementação bate com o modelo antigo, mas por
  coincidência estrutural (não existe BC1 de saídas nesta base, então
  `DATA_ELEITA` sempre cai pro XML) — não por lógica desenhada pra isso.
  **Resolvido ainda na mesma sessão**: usuário confirmou (segunda pergunta
  direta, após selecionar a tabela da hierarquia no editor e escrever
  "deve ser isso") que a hierarquia implementada é a regra final —
  `DIRETRIZES DA APLICAÇÃO.txt` fica só como histórico, sem checagem de
  VTN/VTP/quantidade/extemporaneidade a implementar.

## Soluções Aplicadas

Nenhuma mudança de código — o pedido era documentar a regra original e a
comparação com a implementação. A dúvida sobre qual das duas prevalece foi
resolvida por confirmação direta do usuário (hierarquia implementada
vence), documentada na mesma seção do doc.

## Decisões Técnicas

- Seção adicionada dentro de `docs/estagios/04_cronologia_ano_eleito.md`
  (confirmado com o usuário via pergunta direta) em vez de um novo arquivo
  solto na raiz no estilo `r_*.txt` — o usuário apontou que o assunto "está
  no Estágio 4".
- Hierarquia `DATA_ELEITA` (SPED antes do XML pro ET) confirmada pelo
  usuário como regra definitiva, superando o modelo antigo de
  `DIRETRIZES DA APLICAÇÃO.txt`. Nenhuma mudança em
  `loader._aplicar_data_eleita()` necessária.

## Pendências

Nenhuma pendência aberta sobre datas ET/EP — fechada nesta sessão.

## Próximos Passos

Nenhum próximo passo específico deste tópico; ver pendências gerais de
outras sessões (`memoria/2026-07-14.md`).
