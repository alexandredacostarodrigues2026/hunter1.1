# Estágios do Projeto (AI_HUNTER1.1)

Índice dos grandes estágios do fluxo Hunter, do dado bruto (XML/SPED) até o
cruzamento final. Cada estágio tem seu detalhamento em `docs/estagios/`.
Fonte original da lista: `estágios.txt` (raiz).

| # | Estágio | Status | Detalhe |
|---|---|---|---|
| 1 | Extração | ✅ Implementado (XML: nfe_entradas/saidas + analise/situacao ET-EP; SPED: sped_itens/produtos/unidades; BC2/BC1; Período de Auditoria) | [docs/estagios/01_extracao.md](docs/estagios/01_extracao.md) |
| 2 | Criação BC3 — busca de código de produto do auditado para produtos ET do fornecedor | ✅ Implementado | [docs/estagios/02_criacao_bc3.md](docs/estagios/02_criacao_bc3.md) |
| 3 | Criação das entradas, saídas e estoques (movimentação) para fins de cruzamento | ✅ Implementado (xml_entradas_real/xml_saidas_real; prévia enriquecida com COD_ITEM_DECLARACAO/FATOR_MULTIPLICADOR_SUGERIDO da bc3 desde 2026-07-14) | [docs/estagios/03_fluxos_fisicos.md](docs/estagios/03_fluxos_fisicos.md) |
| 4 | Implantação das regras das datas nas ET e nas EP | ✅ Implementado (DATA_ELEITA/ANO_ELEITO, tabelas estoque_entradas/estoque_saidas — **ainda é movimentação, não estoque**, apesar do nome; ganhou primeiro painel próprio na UI em 2026-07-14, dentro de "Tabelas Entradas / Saídas / Estoques") | [docs/estagios/04_cronologia_ano_eleito.md](docs/estagios/04_cronologia_ano_eleito.md) |
| 5 | Geração da Tabela de Estoque (consolidação do inventário declarado, Bloco H) | ✅ Implementado (estoque_anual_consolidado — foco só em consolidação; **é aqui que o "estoque" passa a existir de fato**; regra de continuidade corrigida em 2026-07-17, desvio de 1 ano; Auditoria de Divergência de Estoque vs. Excel de referência implementada e fechando 100% nas 3 operações reais desde 2026-07-18 — restrita ao Período de Auditoria configurado, ver AUDITORIA1 no Estágio 6; cálculo de divergência/RN1 propriamente dito — cruzando contra a movimentação do Estágio 4 — fica pra uma etapa futura, Estágio 15) | [docs/estagios/05_tabela_estoque.md](docs/estagios/05_tabela_estoque.md) |
| 6 | VAMOS ORGANIZAR (Menu de Navegação) — reorganiza a tela única em 4 grupos navegáveis: "Extração" (Período/Carga/Entidade Auditada), "Matching (BC3)" (Estágio 2 — motor D1-D6/A1-A5, com BC1 como subcomponente num expander), "Segregados" (CFOPs Não Autorizados/Notas Não Autorizadas — não entram no cômputo do cruzamento) e "Tabelas Entradas / Saídas / Estoques" (Estágios 3-5 + Auditoria; renomeado de "Painéis em Construção" em 2026-07-14) | 🚧 Em andamento | [docs/estagios/06_menu_navegacao.md](docs/estagios/06_menu_navegacao.md) |
| 7 | Escolha do Produto Alvo — seleciona, entre os produtos movimentados/declarados, quais serão efetivamente auditados | 🚧 Em andamento (7.1, 7.2, 7.2.1, 7.3, 7.3.1 e 7.3.2 implementados) | sem doc própria ainda |
| 7.1 | Fixação da Descrição Relevante — elege a descrição mais frequente (moda) por COD_ITEM entre entradas, saídas (Estágio 4) e estoque (Estágio 5), padronizando o nome "oficial" do produto pra relatórios e seleção de itens pra auditoria física | ✅ Implementado (`produto_alvo`; botão de 6º nível próprio no Menu Principal — "🏷️ DESCRIÇÃO RELEVANTE" — em vez de sub-painel dentro de "Tabelas Entradas/Saídas/Estoques" como a nota de numeração original previa; código normalizado ANTES da moda desde 2026-07-19 — achado real: código curto colidindo entre 4 paddings diferentes elegia a descrição errada; testado nas 3 operações reais: geraldo 6.138 produtos, PB2 269, cometa 2.513) | sem doc própria ainda; ver `interface.render_pagina_descricao_relevante()`/`loader.montar_produto_alvo()` |
| 7.2 | Cruzamento por Valor — aplica EI+Compras=Vendas+EF por (Ano, Produto) em R$, identidade pela Descrição Relevante (7.1); Compras/Estoque pela visão da auditada, Vendas pela visão física do XML; indicadores de risco (Divergência absoluta, Infração, % Diverg) ordenados por maior divergência | ✅ Implementado (`cruzamento_valor`; botão de 7º nível próprio no Menu Principal — "📉 7.2: CRUZAMENTO POR VALOR"; Vendas corrigida em 2026-07-18 pra usar `cprod` do XML + excluir autoemissão — ver `docs/estagios/06_menu_navegacao.md`; validado contra referência de produção do usuário — "FRALDA NENE BABY 3" fecha 100% nos 4 anos; direção de `INFRACAO` (2026-07-19) confirmada contra a RN1 já documentada; teto ">1000%" e formatação BR na tela desde 2026-07-19; testado nas 3 operações reais) | sem doc própria ainda; ver `interface.render_pagina_cruzamento_valor()`/`loader.gerar_cruzamento_valor()` |
| 7.2.1 | Cruzamento por Produto — condensa o 7.2 (uma linha por Ano+Produto) numa linha por Descrição Relevante, somando os valores financeiros e recalculando Infração/% Diverg/Divergência sobre os totais acumulados; drill-down pro detalhamento anual de um produto selecionado | ✅ Implementado (`cruzamento_produto`; botão de 8º nível próprio no Menu Principal — "📊 7.2.1: CRUZAMENTO POR PRODUTO"; direção de `INFRACAO` (2026-07-19) mantida igual ao 7.2; **Divergência é o total LÍQUIDO acumulado** (`\|∑TD-∑TC\|`, corrigido 2026-07-20 — antes era soma das divergências anuais, inflava bem além do que TD/TC totais mostravam na mesma linha, achado real no produto "CHOC SONHO DE VALSA 1KG"); testado ao vivo na geraldo — 5.721 produtos condensados) | sem doc própria ainda; ver `interface.render_pagina_cruzamento_produto()`/`loader.gerar_cruzamento_produto()` |
| 7.3 | RN1 — Movimentação Física (XML) — mesma identidade EI+Compras=Vendas+EF do 7.2, mas Compras soma o valor TOTAL do XML de entradas (Estágio 4), inclusive itens sem match no Matching/BC3 (o 7.2 só soma itens COM match) — agregada por (Ano, Descrição Relevante) em vez de (Ano, COD_ITEM), Descrição Relevante (7.1) como elo entre movimentação e inventário | ✅ Implementado (`rn1_fisica`; botão de 9º nível próprio no Menu Principal — "🔥 7.3: RN1 — MOVIMENTAÇÃO FÍSICA (XML)"; Compras via nova `loader._compras_entradas_sem_filtro_bc3()` (sem o filtro `COD_ITEM_DECLARACAO IS NOT NULL` do 7.2); itens sem match nenhum viram uma linha POR (Ano, descrição bruta do XML), prefixada `"(SEM VÍNCULO) "` (`loader.PREFIXO_RN1_SEM_VINCULO`) — não um total cego, achado real na cometa: 52 descrições distintas sem match, R$2,25 milhões; Vendas/EI/EF reaproveitados de `cruzamento_valor` (sem o mesmo problema de cobertura); **não é o Estágio 15** — sem a lógica de PU (preço unitário)/tie-break do texto original da RN1, que continua reservada; testado nas 3 operações reais — geraldo R$1.230.187,38 de divergência acumulada/9 descrições sem vínculo (R$1.307,07), PB2 R$5.412.013,80/12 descrições (R$339.992,00), cometa R$323.652.947,55/50 descrições (R$2.252.713,86)) | sem doc própria ainda; ver `interface.render_pagina_rn1_fisica()`/`loader.gerar_rn1_fisica()` |
| 7.3.1 | RN1 por Produto — condensa o 7.3 (uma linha por Ano+Descrição Relevante) numa linha por Descrição Relevante, somando todos os anos e recalculando Infração/% Diverg/Divergência sobre os totais acumulados; drill-down pro detalhamento anual (mirror do 7.2→7.2.1, agora sobre o 7.3) | ✅ Implementado (`rn1_produto`; botão de 10º nível próprio no Menu Principal — "📊 7.3.1: RN1 POR PRODUTO"; NÃO é redundante com o 7.2.1 — os números divergem sempre que houver Compras sem vínculo no Matching, incluídas normalmente como linhas de produto "(SEM VÍNCULO) ..."; **Divergência é o total LÍQUIDO acumulado** (corrigido 2026-07-20, mesmo dia — mesma correção do 7.2.1); testado nas 3 operações reais, incluindo o caso exato reportado pelo usuário via print da UI) | sem doc própria ainda; ver `interface.render_pagina_rn1_produto()`/`loader.gerar_rn1_produto()` |
| 7.3.2 | Simulação RN1 (+30%) e Grupo de Produto Alvo (Fiscalização) — majora EI/Compras/EF do 7.3.1 em 30% (Vendas como âncora real, sem acréscimo) pra testar se uma subvaloração de 30% explicaria as divergências; o mesmo painel também é a tela de escolha de quais produtos entram no grupo efetivamente fiscalizado (checkbox por produto + botão "Salvar Grupo de Produto Alvo") | ✅ Implementado (`rn1_simulada_30` + `produto_alvo_fiscalizacao`; botão de 11º nível próprio no Menu Principal — "📈 7.3.2: SIMULAÇÃO RN1 (+30%)"; drill-down por ano com clique direto na linha da tabela (`on_select`/`selection_mode`); TD/TC/Divergência/%Diverg destacados em vermelho; grupo de fiscalização é mecânica NOVA — mirror do `ranking.py`/`registrar_produto_eleito()` do app antigo `ANTIGO_geraldo_2020_2024_5`, mas sobre os dados de divergência do 7.3.2 em vez da tabela de ranking bruta; upsert por Descrição Relevante preserva seleções feitas sob outros filtros, cancelamento vira histórico (STATUS) em vez de deletar; testado nas 3 operações reais) | sem doc própria ainda; ver `interface.render_pagina_rn1_simulada_30()`/`loader.gerar_rn1_simulada_30()`/`loader.salvar_grupo_produto_alvo_fiscalizacao()` |
| 8 | Resumo de Entradas — duas visões de referência sobre `estoque_entradas` (Estágio 4) pra conferir a qualidade do Matching e identificar padrões de escrituração da auditada: Detalhada (uma linha por item do XML, com código/descrição declarados e ID Único pra rastrear a nota exata) e Agrupada (condensa por código+descrição declarados+descrição do XML, contando ocorrências, ordenada decrescente) | ✅ Implementado (`estagio8_detalhado` + `estagio8_agrupado`; botão de 12º nível próprio no Menu Principal — "📋 ESTÁGIO 8: RESUMO DE ENTRADAS"; expandido em 2026-07-23 com o Estágio 8.1, painel virou 2 abas de nível superior "📥 Entradas"/"📤 Saídas", ver linha 8.1; nomes de coluna em minúsculo/português por Solicitação Técnica explícita — `codproddecl`/`desc_xml`/`descrição_decl`/`idunico`/`qtde_ocorrencias`, diferente da convenção MAIÚSCULA do resto do `loader.py`; `codproddecl`/`descrição_decl` preservam NULL genuíno de item sem correspondência no Matching/BC3 (Regra R07 — nunca vira o literal "None"); `groupby` com `dropna=False` pra não descartar silenciosamente esses itens da contagem; verificação de qualidade embutida na tela — soma de `qtde_ocorrencias` DEVE bater com o total detalhado, `loader.verificar_estagio_8()`; testado na geraldo — 16.407 linhas detalhadas (bate exato com `estoque_entradas`), 5.091 combinações agrupadas, só 11 linhas com NULL real) | sem doc própria ainda; ver `interface.render_pagina_estagio_8()`/`loader.gerar_estagio_8()` |
| 8.1 | Resumo de Saídas — mesma lógica do Estágio 8, sobre `estoque_saidas` (Estágio 4): Detalhada (codproddecl/desc_xml/idunico, sem descrição_decl — não pedida aqui) e Agrupada (codproddecl+desc_xml, contando ocorrências) | ✅ Implementado (`estagio8_saidas_detalhado` + `estagio8_saidas_agrupado`; aba "📤 Saídas" do mesmo painel do Estágio 8; **correção real no mesmo dia**: primeira versão usava `COD_ITEM_DECLARACAO` (via bc3/Matching, só cobre ENTRADAS) pra `codproddecl`, ficando NULL em ~99% das linhas — usuário esclareceu "nas saídas, o código de produto da declaração é o código do xml": na saída a auditada é EMITENTE, então `fatoitemnfe_infnfe_det_prod_cprod` (código do próprio XML) já é o código dela, sem Matching nenhum (mesmo raciocínio já usado em Vendas do Estágio 7.2); cobertura 100% confirmada (0 nulos); testado nas 3 operações reais — geraldo 60.623/4.710, PB2 11.362/106, cometa 204.428/188, `verificar_estagio_8_saidas()` bate nas 3) | sem doc própria ainda; ver `interface.render_estagio_8()`/`loader.gerar_estagio_8_saidas()` |
| 8.2 | Resumo de Estoques — mesma lógica do Estágio 8/8.1, sobre `estoque_anual_consolidado` (Estágio 5) | ✅ Implementado (`estagio8_estoque_detalhado` + `estagio8_estoque_agrupado`; aba "📦 Estoques" do mesmo painel do Estágio 8; desbloqueado em 2026-07-23 com instrução explícita do usuário — `estoque_anual_consolidado` não tem `COD_ITEM`/`DESCR_ITEM`/`ID_UNICO` como a Solicitação Técnica original pedia (schema real: `ANO_REFERENCIA, COD_ITEM_DECLARACAO, DESCR_ITEM_DECLARACAO, UNIDADE, QUANTIDADE_INICIAL, QUANTIDADE_FINAL`, sem chave de item individual — consolidado por ANO+código); `idunico` gerado como hash SINTÉTICO de (ANO_REFERENCIA, COD_ITEM_DECLARACAO, DESCR_ITEM_DECLARACAO, QUANTIDADE_INICIAL, QUANTIDADE_FINAL) via `_gerar_id_unico()` (mesma técnica de `nfe_entradas`/`nfe_saidas`), só dentro do cálculo do 8.2 — Estágio 5 não foi alterado; achado real: (ANO_REFERENCIA, COD_ITEM_DECLARACAO) sozinho já não é chave única (18 pares duplicados na geraldo, a maioria duplicata 100% idêntica — essas continuam colidindo no mesmo hash, limite real do dado, não do cálculo); `.astype(str)` do pandas não converte NaN de float pra string "nan" (fica float), corrigido com `fillna(-1)` nas colunas de quantidade antes do hash; testado nas 3 operações reais — geraldo 31.956/5.975, PB2 223/96, cometa 132/57, `verificar_estagio_8_estoque()` bate nas 3) | sem doc própria ainda; ver `interface.render_estagio_8()`/`loader.gerar_estagio_8_estoque()` |
| 9 | Produtos Alvos Salvos — Botão 9 do Menu Principal (2026-07-23, contagem própria do usuário pra botões novos, não ligado a nenhum Estágio numerado): painel onde o auditor escolhe, dentre os produtos já salvos e ativos no Grupo de Produto Alvo (Estágio 7.3.2), UM produto específico que será o objeto do cruzamento; e (mesmo dia, "VAMOS AVANÇAR") aba "Entradas" com o Critério 1 do cruzamento (similaridade 100% do código de produto contra o Estágio 8) | ✅ Implementado (`produto_cruzamento_escolhido`, tabela de 1 linha só, sempre substituída — diferente de `produto_alvo_fiscalizacao`, que guarda o grupo inteiro com histórico; botão "🎯 PRODUTOS ALVOS SALVOS" na 2ª linha do Menu Principal, ao lado do Estágio 8; mostra a lista salva (mesmo destaque vermelho condicional >30% do 7.3.2) + selectbox + botão "Confirmar produto pra cruzamento"; seção "🔀 Cruzamento" com aba "📥 Entradas" — Critério 1: "100%" é o MESMO código, não string idêntica byte-a-byte — `codproddecl`/`COD_ITEM` normalizados via `_normalizar_cod_item_flexivel()` (remove zero à esquerda só de código puramente numérico) antes de comparar (`loader.cruzar_produto_escolhido_entradas()`); **correção real no mesmo dia**: primeira versão comparava string crua e dava 0 correspondências pra "CERV SKOL LATA 350ML" (código `7891149200504` na Descrição Relevante vs `07891149200504` no Matching de Entradas, mesmo produto/código, só padding diferente) — usuário sinalizou "AINDA COM ERRO", corrigido normalizando os dois lados; confirmado depois: 3 correspondências corretas; tabela inferior adicionada no mesmo dia ("CRIE UMA TABELA INFERIOR COM OS PRODUTOS E RESPECTIVOS IDS ÚNICOS") — mesma comparação normalizada, mas contra `estagio8_detalhado` (uma linha por item do XML, com `idunico`) via `loader.cruzar_produto_escolhido_entradas_detalhado()`, pra localizar a nota fiscal exata (geraldo: 48 itens individuais pro CERV SKOL, batendo com a soma de `qtde_ocorrencias` do agrupado); **Rubrica do Produto Alvo** (mesmo dia, "CRIE CAIXA PARA GRAVAR O PRODUTO QUE FARÁ PARTE DA RUBRICA DO PRODUTO ALVO") — checkbox "Salvar" (renomeado de "Selecionar p/ Rubrica", sempre desmarcado por padrão) + coluna "Observação" (marca "✅ Já salvo na Rubrica") + selectbox "Critério de busca" (movido pra ANTES do cruzamento rodar) + botão "Salvar na Rubrica", persistindo em `cruzamento_confirmado` (agregado, `loader.salvar_cruzamento_confirmado()`, com semântica de SINCRONIZAÇÃO via `universo_chaves` — desmarcar remove de fato) e `cruzamento_confirmado_detalhado` (item-a-item por `idunico`, `loader.salvar_cruzamento_confirmado_detalhado()`, mesma sincronização via `universo_idunicos`) — a tabela "Itens individuais (com ID Único)" lê direto do persistido (`loader.consultar_cruzamento_confirmado_detalhado()`), cumulativa entre critérios; item já confirmado pra um alvo não aparece mais como candidato pra OUTRO alvo (`loader._chaves_ja_atribuidas_a_outro_alvo()`). **Critério 2: Nome de Declaração Igual ao Alvo** (2026-07-23, "o novo critério 2 vai ser o seguinte: nome do alvo igual ao nome de declaração do candidato. mantenha as similaridade entre nome do alvo e descrição xml do candidato.") — filtra por IGUALDADE normalizada (maiúsculas/espaços colapsados) entre `DESCR_ALVO` e `descrição_decl` (nome que a própria auditada usa na declaração), sem exigir nenhuma relação de código; `SIMILARIDADE_DESCRICAO` continua calculada (entre `desc_xml` e `DESCR_ALVO`), só que aqui é informativa/ordenação (`loader.cruzar_produto_escolhido_entradas_criterio2()`/`..._criterio2_detalhado()`, `loader._normalizar_nome_para_igualdade()`). **Critério 3: Código de Produto Divergente** (2026-07-23, era "Critério 2" — renumerado no mesmo dia, "transforme o critério 2 em critério3", quando o Critério 2 de nome-igual foi definido) — cobre o caso oposto do Critério 1: busca candidatos com código DIFERENTE do alvo, usando só `SIMILARIDADE_DESCRICAO` como filtro (≥20%, mesmo piso do app antigo) já que não há mais o código como evidência (`loader.cruzar_produto_escolhido_entradas_criterio3()`/`..._criterio3_detalhado()`); validado com achado real na geraldo — candidato `codproddecl="nd"` (não declarado) com `desc_xml` IDÊNTICO à descrição do alvo (100% de similaridade), que o Critério 1 nunca poderia achar por exigir código igual. Os 3 critérios despachados por `interface._obter_criterios_cruzamento_entradas()`. Testado via `AppTest` em todos os caminhos possíveis (data_editor em si não é inspecionável pelo AppTest nesta versão do Streamlit, mesma limitação já vista no 7.3.2), sem exceção — inclusive trocando entre os 3 critérios no mesmo selectbox; propagado nas 3 operações reais) | sem doc própria ainda; ver `interface.render_produtos_alvo_salvos()`/`interface._render_cruzamento_entradas()`/`loader.escolher_produto_cruzamento()`/`loader.cruzar_produto_escolhido_entradas()`/`loader.cruzar_produto_escolhido_entradas_criterio2()`/`loader.cruzar_produto_escolhido_entradas_criterio3()`/`loader.salvar_cruzamento_confirmado()`/`loader.salvar_cruzamento_confirmado_detalhado()` |
| 15 | Cálculo de divergência RN1 (Estoque Inicial + Compras = Vendas + Estoque Final) — compara o inventário declarado (Estágio 5) com a movimentação real (Estágio 4), com a lógica de PU (preço unitário) e omissão do texto original | ⏳ Planejado — os Estágios 7.2/7.3 já montam a identidade EI+C=V+EF em valor (por código e por Descrição Relevante), mas sem a lógica de PU/omissão do texto original (condições c>0/c=0, tie-break) | sem doc própria ainda; ver `regra de negócios unificadas/regra negocio_pu_rn1_ei+c=v+ef_1.txt` (raiz) e nota em [docs/estagios/04_cronologia_ano_eleito.md](docs/estagios/04_cronologia_ano_eleito.md) |

## Como usar este índice

- Cada estágio novo ganha uma linha aqui e um arquivo próprio em
  `docs/estagios/NN_nome.md`.
- Atualizar o `Status` (⏳ Planejado / 🚧 Em andamento / ✅ Implementado)
  conforme o trabalho avança.
- Estágios que já têm documentação própria e detalhada em outro lugar do
  projeto (ex.: o motor de Matching em `REGRAS_MATCHING.md`) não duplicam o
  conteúdo aqui — o arquivo do estágio só resume e aponta pra fonte
  detalhada.
- Numeração não é estritamente sequencial (ver o salto 6 → 15): o Estágio 6
  original (cálculo de divergência RN1) foi renumerado para 15 em
  2026-07-14 para abrir espaço, no 6, para o menu de navegação — os números
  7-14 ficam reservados para painéis intermediários futuros, sem precisar
  renumerar de novo. **Atualizado 2026-07-18**: o Estágio 7 ("Escolha do
  Produto Alvo") usou esse espaço reservado, com sub-passos numerados
  `7.1`, `7.2`... (primeiro precedente de numeração `N.M` neste índice —
  o Estágio 6, "VAMOS ORGANIZAR", não teve seus grupos internos numerados
  dessa forma). O sub-passo 7.1 ("Fixação da Descrição Relevante") virou
  — por Solicitação Técnica explícita — botão de 6º nível PRÓPRIO no Menu
  Principal, não um sub-painel dentro de "Tabelas Entradas / Saídas /
  Estoques" como a reserva original previa; os números 8-14 continuavam
  livres pra painéis futuros, do mesmo jeito (sub-painel ou botão
  próprio, a decidir caso a caso). **Atualizado 2026-07-23**: o Estágio
  8 ("Resumo de Entradas", com sub-passos `8.1`/`8.2` pro mesmo padrão
  sobre Saídas/Estoques) usou esse espaço reservado — só **9-14**
  continuam livres. O "Botão 9" ("Produtos Alvos Salvos", linha 9 desta
  tabela) NÃO consome essa reserva — é numeração própria do usuário pra
  botões novos do menu (não ligada à numeração de Estágio), só
  compartilha a mesma linha da tabela por conveniência de registro.
