# Estágios do Projeto (AI_HUNTER1.1)

Índice dos grandes estágios do fluxo Hunter, do dado bruto (XML/SPED) até o
cruzamento final. Cada estágio tem seu detalhamento em `docs/estagios/`.
Fonte original da lista: `estágios.txt` (raiz).

| # | Estágio | Status | Detalhe |
|---|---|---|---|
| 1 | Extração | ✅ Implementado (XML: nfe_entradas/saidas + analise/situacao ET-EP; SPED: sped_itens/produtos/unidades; BC2/BC1; Período de Auditoria) | [docs/estagios/01_extracao.md](docs/estagios/01_extracao.md) |
| 2 | Criação BC3 — busca de código de produto do auditado para produtos ET do fornecedor | ✅ Implementado | [docs/estagios/02_criacao_bc3.md](docs/estagios/02_criacao_bc3.md) |
| 3 | Criação das entradas, saídas e estoques (movimentação) para fins de cruzamento | ✅ Implementado (xml_entradas_real/xml_saidas_real; prévia enriquecida com COD_ITEM_DECLARACAO/FATOR_MULTIPLICADOR_SUGERIDO da bc3 desde 2026-07-14) | [docs/estagios/03_fluxos_fisicos.md](docs/estagios/03_fluxos_fisicos.md) |
| 4 | Implantação das regras das datas nas ET e nas EP | ✅ Implementado (DATA_ELEITA/ANO_ELEITO, tabelas estoque_entradas/estoque_saidas — **ainda é movimentação, não estoque**, apesar do nome; ganhou primeiro painel próprio na UI em 2026-07-14, dentro de "Tabelas Entradas / Saídas / Estoques". **Bug real corrigido em 2026-07-25**: notas de autoemissão (emit_cnpj==dest_cnpj==cnpj_auditada — transferência entre estabelecimentos próprios do auditado) às vezes chegam como arquivo fisicamente duplicado nas subpastas "ET" e "EP" — a mesma linha passava 2x em `mask_entrada_real`/`mask_saida_real` (auditada_destinataria/auditada_emitente não dependem de PASTA_ORIGEM), inflando estoque_entradas/estoque_saidas em dobro pra esses itens (achado real na geraldo: CERV SKOL LATA 350ML tinha 173 ocorrências em vez de 171, confirmado pelo controle interno do usuário — 241 pares duplicados no total, ~0,4% de 60 mil linhas). Corrigido em `_classificar_itens_nfe()` com dedupe por ID_UNICO restrito às linhas de autoemissão, ANTES de persistir df_entradas_real/df_saidas_real — reconfirmado 171 após regenerar Estágio 1/3/4/8/8.1 na geraldo; propagado e regenerado em PB_2023_2025 (xml_saidas_real 11.362→11.359) e cometa_2021_2025 (204.428→204.382) em 2026-07-25, `verificar_estagio_8()`/`_saidas()`/`_estoque()` batendo nas 3 operações) | [docs/estagios/04_cronologia_ano_eleito.md](docs/estagios/04_cronologia_ano_eleito.md) |
| 5 | Geração da Tabela de Estoque (consolidação do inventário declarado, Bloco H) | ✅ Implementado (estoque_anual_consolidado — foco só em consolidação; **é aqui que o "estoque" passa a existir de fato**; regra de continuidade corrigida em 2026-07-17, desvio de 1 ano; Auditoria de Divergência de Estoque vs. Excel de referência implementada e fechando 100% nas 3 operações reais desde 2026-07-18 — restrita ao Período de Auditoria configurado, ver AUDITORIA1 no Estágio 6; cálculo de divergência/RN1 propriamente dito — cruzando contra a movimentação do Estágio 4 — fica pra uma etapa futura, Estágio 15) | [docs/estagios/05_tabela_estoque.md](docs/estagios/05_tabela_estoque.md) |
| 6 | VAMOS ORGANIZAR (Menu de Navegação) — reorganiza a tela única em 4 grupos navegáveis: "Extração" (Período/Carga/Entidade Auditada), "Matching (BC3)" (Estágio 2 — motor D1-D6/A1-A5, com BC1 como subcomponente num expander), "Segregados" (CFOPs Não Autorizados/Notas Não Autorizadas — não entram no cômputo do cruzamento) e "Tabelas Entradas / Saídas / Estoques" (Estágios 3-5 + Auditoria; renomeado de "Painéis em Construção" em 2026-07-14) | 🚧 Em andamento | [docs/estagios/06_menu_navegacao.md](docs/estagios/06_menu_navegacao.md) |
| 7 | Escolha do Produto Alvo — seleciona, entre os produtos movimentados/declarados, quais serão efetivamente auditados | 🚧 Em andamento (7.1, 7.2, 7.2.1, 7.3, 7.3.1 e 7.3.2 implementados) | sem doc própria ainda |
| 7.1 | Fixação da Descrição Relevante — elege a descrição mais frequente (moda) por COD_ITEM entre entradas, saídas (Estágio 4) e estoque (Estágio 5), padronizando o nome "oficial" do produto pra relatórios e seleção de itens pra auditoria física | ✅ Implementado (`produto_alvo`; botão de 6º nível próprio no Menu Principal — "🏷️ DESCRIÇÃO RELEVANTE" — em vez de sub-painel dentro de "Tabelas Entradas/Saídas/Estoques" como a nota de numeração original previa; código normalizado ANTES da moda desde 2026-07-19 — achado real: código curto colidindo entre 4 paddings diferentes elegia a descrição errada; testado nas 3 operações reais: geraldo 6.138 produtos, PB2 269, cometa 2.513) | sem doc própria ainda; ver `interface.render_pagina_descricao_relevante()`/`loader.montar_produto_alvo()` |
| 7.2 | Cruzamento por Valor — aplica EI+Compras=Vendas+EF por (Ano, Produto) em R$, identidade pela Descrição Relevante (7.1); Compras/Estoque pela visão da auditada, Vendas pela visão física do XML; indicadores de risco (Divergência absoluta, Infração, % Diverg) ordenados por maior divergência | ✅ Implementado (`cruzamento_valor`; botão de 7º nível próprio no Menu Principal — "📉 7.2: CRUZAMENTO POR VALOR"; Vendas corrigida em 2026-07-18 pra usar `cprod` do XML + excluir autoemissão — ver `docs/estagios/06_menu_navegacao.md`; validado contra referência de produção do usuário — "FRALDA NENE BABY 3" fecha 100% nos 4 anos; direção de `INFRACAO` (2026-07-19) confirmada contra a RN1 já documentada; teto ">1000%" e formatação BR na tela desde 2026-07-19; testado nas 3 operações reais) | sem doc própria ainda; ver `interface.render_pagina_cruzamento_valor()`/`loader.gerar_cruzamento_valor()` |
| 7.2.1 | Cruzamento por Produto — condensa o 7.2 (uma linha por Ano+Produto) numa linha por Descrição Relevante, somando os valores financeiros e recalculando Infração/% Diverg/Divergência sobre os totais acumulados; drill-down pro detalhamento anual de um produto selecionado | ✅ Implementado (`cruzamento_produto`; botão de 8º nível próprio no Menu Principal — "📊 7.2.1: CRUZAMENTO POR PRODUTO"; direção de `INFRACAO` (2026-07-19) mantida igual ao 7.2; **Divergência é o total LÍQUIDO acumulado** (`\|∑TD-∑TC\|`, corrigido 2026-07-20 — antes era soma das divergências anuais, inflava bem além do que TD/TC totais mostravam na mesma linha, achado real no produto "CHOC SONHO DE VALSA 1KG"); testado ao vivo na geraldo — 5.721 produtos condensados) | sem doc própria ainda; ver `interface.render_pagina_cruzamento_produto()`/`loader.gerar_cruzamento_produto()` |
| 7.3 | RN1 — Movimentação Física (XML) — mesma identidade EI+Compras=Vendas+EF do 7.2, mas Compras soma o valor TOTAL do XML de entradas (Estágio 4), inclusive itens sem match no Matching/BC3 (o 7.2 só soma itens COM match) — agregada por (Ano, Descrição Relevante) em vez de (Ano, COD_ITEM), Descrição Relevante (7.1) como elo entre movimentação e inventário | ✅ Implementado (`rn1_fisica`; botão de 9º nível próprio no Menu Principal — "🔥 7.3: RN1 — MOVIMENTAÇÃO FÍSICA (XML)"; Compras via nova `loader._compras_entradas_sem_filtro_bc3()` (sem o filtro `COD_ITEM_DECLARACAO IS NOT NULL` do 7.2); itens sem match nenhum viram uma linha POR (Ano, descrição bruta do XML), prefixada `"(SEM VÍNCULO) "` (`loader.PREFIXO_RN1_SEM_VINCULO`) — não um total cego, achado real na cometa: 52 descrições distintas sem match, R$2,25 milhões; Vendas/EI/EF reaproveitados de `cruzamento_valor` (sem o mesmo problema de cobertura); **não é o Estágio 15** — sem a lógica de PU (preço unitário)/tie-break do texto original da RN1, que continua reservada; testado nas 3 operações reais — geraldo R$1.230.187,38 de divergência acumulada/9 descrições sem vínculo (R$1.307,07), PB2 R$5.412.013,80/12 descrições (R$339.992,00), cometa R$323.652.947,55/50 descrições (R$2.252.713,86)) | sem doc própria ainda; ver `interface.render_pagina_rn1_fisica()`/`loader.gerar_rn1_fisica()` |
| 7.3.1 | RN1 por Produto — condensa o 7.3 (uma linha por Ano+Descrição Relevante) numa linha por Descrição Relevante, somando todos os anos e recalculando Infração/% Diverg/Divergência sobre os totais acumulados; drill-down pro detalhamento anual (mirror do 7.2→7.2.1, agora sobre o 7.3) | ✅ Implementado (`rn1_produto`; botão de 10º nível próprio no Menu Principal — "📊 7.3.1: RN1 POR PRODUTO"; NÃO é redundante com o 7.2.1 — os números divergem sempre que houver Compras sem vínculo no Matching, incluídas normalmente como linhas de produto "(SEM VÍNCULO) ..."; **Divergência é o total LÍQUIDO acumulado** (corrigido 2026-07-20, mesmo dia — mesma correção do 7.2.1); testado nas 3 operações reais, incluindo o caso exato reportado pelo usuário via print da UI) | sem doc própria ainda; ver `interface.render_pagina_rn1_produto()`/`loader.gerar_rn1_produto()` |
| 7.3.2 | Simulação RN1 (+30%) e Grupo de Produto Alvo (Fiscalização) — majora EI/Compras/EF do 7.3.1 em 30% (Vendas como âncora real, sem acréscimo) pra testar se uma subvaloração de 30% explicaria as divergências; o mesmo painel também é a tela de escolha de quais produtos entram no grupo efetivamente fiscalizado (checkbox por produto + botão "Salvar Grupo de Produto Alvo") | ✅ Implementado (`rn1_simulada_30` + `produto_alvo_fiscalizacao`; botão de 11º nível próprio no Menu Principal — "📈 7.3.2: SIMULAÇÃO RN1 (+30%)"; drill-down por ano com clique direto na linha da tabela (`on_select`/`selection_mode`); TD/TC/Divergência/%Diverg destacados em vermelho; grupo de fiscalização é mecânica NOVA — mirror do `ranking.py`/`registrar_produto_eleito()` do app antigo `ANTIGO_geraldo_2020_2024_5`, mas sobre os dados de divergência do 7.3.2 em vez da tabela de ranking bruta; upsert por Descrição Relevante preserva seleções feitas sob outros filtros, cancelamento vira histórico (STATUS) em vez de deletar; testado nas 3 operações reais) | sem doc própria ainda; ver `interface.render_pagina_rn1_simulada_30()`/`loader.gerar_rn1_simulada_30()`/`loader.salvar_grupo_produto_alvo_fiscalizacao()` |
| 8 | Resumo de Entradas — duas visões de referência sobre `estoque_entradas` (Estágio 4) pra conferir a qualidade do Matching e identificar padrões de escrituração da auditada: Detalhada (uma linha por item do XML, com código/descrição declarados e ID Único pra rastrear a nota exata) e Agrupada (condensa por código+descrição declarados+descrição do XML, contando ocorrências, ordenada decrescente) | ✅ Implementado (`estagio8_detalhado` + `estagio8_agrupado`; botão de 12º nível próprio no Menu Principal — "📋 ESTÁGIO 8: RESUMO DE ENTRADAS"; expandido em 2026-07-23 com o Estágio 8.1, painel virou 2 abas de nível superior "📥 Entradas"/"📤 Saídas", ver linha 8.1; nomes de coluna em minúsculo/português por Solicitação Técnica explícita — `codproddecl`/`desc_xml`/`descrição_decl`/`idunico`/`qtde_ocorrencias`, diferente da convenção MAIÚSCULA do resto do `loader.py`; `codproddecl`/`descrição_decl` preservam NULL genuíno de item sem correspondência no Matching/BC3 (Regra R07 — nunca vira o literal "None"); `groupby` com `dropna=False` pra não descartar silenciosamente esses itens da contagem; verificação de qualidade embutida na tela — soma de `qtde_ocorrencias` DEVE bater com o total detalhado, `loader.verificar_estagio_8()`; testado na geraldo — 16.407 linhas detalhadas (bate exato com `estoque_entradas`), 5.091 combinações agrupadas, só 11 linhas com NULL real) | sem doc própria ainda; ver `interface.render_pagina_estagio_8()`/`loader.gerar_estagio_8()` |
| 8.1 | Resumo de Saídas — mesma lógica do Estágio 8, sobre `estoque_saidas` (Estágio 4): Detalhada (codproddecl/desc_xml/idunico, sem descrição_decl — não pedida aqui) e Agrupada (codproddecl+desc_xml, contando ocorrências) | ✅ Implementado (`estagio8_saidas_detalhado` + `estagio8_saidas_agrupado`; aba "📤 Saídas" do mesmo painel do Estágio 8; **correção real no mesmo dia**: primeira versão usava `COD_ITEM_DECLARACAO` (via bc3/Matching, só cobre ENTRADAS) pra `codproddecl`, ficando NULL em ~99% das linhas — usuário esclareceu "nas saídas, o código de produto da declaração é o código do xml": na saída a auditada é EMITENTE, então `fatoitemnfe_infnfe_det_prod_cprod` (código do próprio XML) já é o código dela, sem Matching nenhum (mesmo raciocínio já usado em Vendas do Estágio 7.2); cobertura 100% confirmada (0 nulos); testado nas 3 operações reais — geraldo 60.623/4.710, PB2 11.362/106, cometa 204.428/188, `verificar_estagio_8_saidas()` bate nas 3. **Autoemissão excluída em 2026-07-25**: achado real confirmado com o usuário — "para geraldo, nas saídas, cerveja skol 350 ml, há 170 ocorrências no meu controle já testado e 172 no projeto"; investigado e confirmado: a diferença eram 2 itens de autoemissão (`fatonfe_infnfe_emit_cnpj == fatonfe_infnfe_dest_cnpj` — mesma empresa, CFOP 1411, transferência entre estabelecimentos próprios), contados como "saída"/venda quando na verdade é movimentação INTERNA; usuário confirmou excluir autoemissão de Saídas (Estágio 8.1 e Botão 9) — `172-2=170`, batendo exato. Filtro `WHERE fatonfe_infnfe_emit_cnpj IS DISTINCT FROM fatonfe_infnfe_dest_cnpj` adicionado em `gerar_estagio_8_saidas()` e em `gerar_curadoria_fm_saidas()` (Estágio 9, mesma consistência); Rubrica já confirmada da geraldo corrigida (172→170: removidos os 2 idunicos de autoemissão via `salvar_cruzamento_confirmado_detalhado()` com universo restrito à chave do SKOL, qtde_ocorrencias do agregado atualizada de 171 pra 169); testado — geraldo 60.141/4.687 (era 60.382/4.710, -241 linhas — a autoemissão era um padrão bem mais amplo que só o SKOL), PB2 11.345/105, cometa 203.827/176, `verificar_estagio_8_saidas()` seguiu batendo nas 3; `AppTest` sem exceção; propagado nas 4 operações) | sem doc própria ainda; ver `interface.render_estagio_8()`/`loader.gerar_estagio_8_saidas()` |
| 8.2 | Resumo de Estoques — mesma lógica do Estágio 8/8.1, sobre `estoque_anual_consolidado` (Estágio 5) | ✅ Implementado (`estagio8_estoque_detalhado` + `estagio8_estoque_agrupado`; aba "📦 Estoques" do mesmo painel do Estágio 8; desbloqueado em 2026-07-23 com instrução explícita do usuário — `estoque_anual_consolidado` não tem `COD_ITEM`/`DESCR_ITEM`/`ID_UNICO` como a Solicitação Técnica original pedia (schema real: `ANO_REFERENCIA, COD_ITEM_DECLARACAO, DESCR_ITEM_DECLARACAO, UNIDADE, QUANTIDADE_INICIAL, QUANTIDADE_FINAL`, sem chave de item individual — consolidado por ANO+código); `idunico` gerado como hash SINTÉTICO de (ANO_REFERENCIA, COD_ITEM_DECLARACAO, DESCR_ITEM_DECLARACAO, QUANTIDADE_INICIAL, QUANTIDADE_FINAL) via `_gerar_id_unico()` (mesma técnica de `nfe_entradas`/`nfe_saidas`), só dentro do cálculo do 8.2 — Estágio 5 não foi alterado; achado real: (ANO_REFERENCIA, COD_ITEM_DECLARACAO) sozinho já não é chave única (18 pares duplicados na geraldo, a maioria duplicata 100% idêntica — essas continuam colidindo no mesmo hash, limite real do dado, não do cálculo); `.astype(str)` do pandas não converte NaN de float pra string "nan" (fica float), corrigido com `fillna(-1)` nas colunas de quantidade antes do hash; testado nas 3 operações reais — geraldo 31.956/5.975, PB2 223/96, cometa 132/57, `verificar_estagio_8_estoque()` bate nas 3. **Filtro adicionado em 2026-07-25** (usuário comparando contra `ESTOQUE(...).xlsx` — mesmo Excel de referência do achado de 2026-07-17: "no meu controle vieram 6 cervejas skol 350 ml", contra 7 no sistema): exclui linhas com `QUANTIDADE_FINAL IS NULL` — a regra de continuidade do Estágio 5 sempre materializa uma linha extra de saldo INICIAL pro ano seguinte ao último inventário declarado (ex.: inventário de 31/12/2024 vira EF(2024) E EI(2025), mesmo sem nenhum encerramento de 2025 declarado ainda), e essa linha de ano AINDA NÃO FECHADO não existe no Excel de referência (que só lista anos com fechamento efetivo); mesmo critério prático já usado pela Auditoria de Divergência de Estoque (restrita ao Período de Auditoria). Reconfirmado exato: CERV SKOL LATA 350ML caiu de 7 pra 6 ocorrências, valores de Quantidade Final (56/597/120/19/20/9) batendo linha a linha com o Excel; geraldo recalculado — 25.607/5.975 (agrupado igual, só reduziu ocorrências por item, nenhum item perdeu o grupo inteiro), `verificar_estagio_8_estoque()` seguiu batendo; propagado e regenerado em PB_2023_2025 (127/96) e cometa_2021_2025 (75/57) em 2026-07-25, `verificar_estagio_8_estoque()` batendo nas 3) | sem doc própria ainda; ver `interface.render_estagio_8()`/`loader.gerar_estagio_8_estoque()` |
| — | Produtos Alvos Salvos — "Botão 9" do Menu Principal (2026-07-23, contagem própria do usuário pra botões novos, não ligado a nenhum Estágio numerado — desde 2026-07-24 o "#" desta linha virou "—" pra não colidir com o Estágio 9 formal, ver linha abaixo): painel onde o auditor escolhe, dentre os produtos já salvos e ativos no Grupo de Produto Alvo (Estágio 7.3.2), UM produto específico que será o objeto do cruzamento; e (mesmo dia, "VAMOS AVANÇAR") aba "Entradas" com o Critério 1 do cruzamento (similaridade 100% do código de produto contra o Estágio 8) | ✅ Implementado (`produto_cruzamento_escolhido`, tabela de 1 linha só, sempre substituída — diferente de `produto_alvo_fiscalizacao`, que guarda o grupo inteiro com histórico; botão "🎯 PRODUTOS ALVOS SALVOS" na 2ª linha do Menu Principal, ao lado do Estágio 8; mostra a lista salva (mesmo destaque vermelho condicional >30% do 7.3.2) + selectbox + botão "Confirmar produto pra cruzamento"; seção "🔀 Cruzamento" com aba "📥 Entradas" — Critério 1: "100%" é o MESMO código, não string idêntica byte-a-byte — `codproddecl`/`COD_ITEM` normalizados via `_normalizar_cod_item_flexivel()` (remove zero à esquerda só de código puramente numérico) antes de comparar (`loader.cruzar_produto_escolhido_entradas()`); **correção real no mesmo dia**: primeira versão comparava string crua e dava 0 correspondências pra "CERV SKOL LATA 350ML" (código `7891149200504` na Descrição Relevante vs `07891149200504` no Matching de Entradas, mesmo produto/código, só padding diferente) — usuário sinalizou "AINDA COM ERRO", corrigido normalizando os dois lados; confirmado depois: 3 correspondências corretas; tabela inferior adicionada no mesmo dia ("CRIE UMA TABELA INFERIOR COM OS PRODUTOS E RESPECTIVOS IDS ÚNICOS") — mesma comparação normalizada, mas contra `estagio8_detalhado` (uma linha por item do XML, com `idunico`) via `loader.cruzar_produto_escolhido_entradas_detalhado()`, pra localizar a nota fiscal exata (geraldo: 48 itens individuais pro CERV SKOL, batendo com a soma de `qtde_ocorrencias` do agrupado); **Rubrica do Produto Alvo** (mesmo dia, "CRIE CAIXA PARA GRAVAR O PRODUTO QUE FARÁ PARTE DA RUBRICA DO PRODUTO ALVO") — checkbox "Salvar" (renomeado de "Selecionar p/ Rubrica", sempre desmarcado por padrão) + coluna "Observação" (marca "✅ Já salvo na Rubrica") + selectbox "Critério de busca" (movido pra ANTES do cruzamento rodar) + botão "Salvar na Rubrica", persistindo em `cruzamento_confirmado` (agregado, `loader.salvar_cruzamento_confirmado()`, com semântica de SINCRONIZAÇÃO via `universo_chaves` — desmarcar remove de fato) e `cruzamento_confirmado_detalhado` (item-a-item por `idunico`, `loader.salvar_cruzamento_confirmado_detalhado()`, mesma sincronização via `universo_idunicos`) — a tabela "Itens individuais (com ID Único)" lê direto do persistido (`loader.consultar_cruzamento_confirmado_detalhado()`), cumulativa entre critérios; item já confirmado pra um alvo não aparece mais como candidato pra OUTRO alvo (`loader._chaves_ja_atribuidas_a_outro_alvo()`). **Critério 2: Nome de Declaração Igual ao Alvo** (2026-07-23, "o novo critério 2 vai ser o seguinte: nome do alvo igual ao nome de declaração do candidato. mantenha as similaridade entre nome do alvo e descrição xml do candidato.") — filtra por IGUALDADE normalizada (maiúsculas/espaços colapsados) entre `DESCR_ALVO` e `descrição_decl` (nome que a própria auditada usa na declaração), sem exigir nenhuma relação de código; `SIMILARIDADE_DESCRICAO` continua calculada (entre `desc_xml` e `DESCR_ALVO`), só que aqui é informativa/ordenação (`loader.cruzar_produto_escolhido_entradas_criterio2()`/`..._criterio2_detalhado()`, `loader._normalizar_nome_para_igualdade()`). **Critério 3: Código de Produto Divergente** (2026-07-23, era "Critério 2" — renumerado no mesmo dia, "transforme o critério 2 em critério3", quando o Critério 2 de nome-igual foi definido) — cobre o caso oposto do Critério 1: busca candidatos com código DIFERENTE do alvo, usando só `SIMILARIDADE_DESCRICAO` como filtro (≥20%, mesmo piso do app antigo) já que não há mais o código como evidência (`loader.cruzar_produto_escolhido_entradas_criterio3()`/`..._criterio3_detalhado()`); validado com achado real na geraldo — candidato `codproddecl="nd"` (não declarado) com `desc_xml` IDÊNTICO à descrição do alvo (100% de similaridade), que o Critério 1 nunca poderia achar por exigir código igual. Os 3 critérios despachados por `interface._obter_criterios_cruzamento_entradas()`. Testado via `AppTest` em todos os caminhos possíveis (data_editor em si não é inspecionável pelo AppTest nesta versão do Streamlit, mesma limitação já vista no 7.3.2), sem exceção — inclusive trocando entre os 3 critérios no mesmo selectbox; propagado nas 3 operações reais. **Checkbox "Desfazer" dedicado** (2026-07-24, "aqui abrir a oportunidade de desfazer") — ação EXPLÍCITA de remoção, separada de "Salvar"; universo de sincronização restrito às combinações efetivamente tocadas (Salvar ∪ Desfazer) em vez de todas as linhas da busca, evitando remoção acidental de combinação já salva mas não tocada na rodada. **Enriquecimento fiscal da tabela "Itens individuais"** (2026-07-25) — 6 campos novos buscados ao vivo por `ID_UNICO` em `estoque_entradas` via `loader.consultar_atributos_estoque_por_idunico()` (substituiu `consultar_chv_nfe_por_idunico()`): `ANO_ELEITO`, `ncm4`, `unid_prod` (UCOM), `vl_unit_prod`, `qtde_prod`, `vl_prod` (=`vl_unit_prod`×`qtde_prod`, validado exato contra dado real); bug real corrigido no processo (`PRAGMA table_info` — nome da coluna é índice 1, não 0); ordem final das colunas (mesmo dia, "coloque chave após valor total do item e id único na ultima coluna"): CHV_NFE logo após `vl_prod`, `idunico` por último. **Aba "📤 Saídas"** (2026-07-25, Solicitação Técnica "BUSCA DE CORRESPONDENTES NAS SAÍDAS") — mirror do cruzamento de Entradas sobre `estagio8_saidas_agrupado`, só com Critério 1 e Critério 3 (SEM Critério 2 — regra de negócio: na saída a auditada é emitente, `desc_xml` já é a "declaração" dela, não há nome de declaração separado do candidato pra comparar); `loader.cruzar_produto_escolhido_saidas()`/`_detalhado()`/`_criterio3()`/`_criterio3_detalhado()` (mesma normalização de código/similaridade dos equivalentes de Entradas); `salvar_cruzamento_confirmado()` ganhou tratamento condicional pra "descrição_decl" (ausente em `estagio8_saidas_agrupado`); `interface._render_cruzamento_saidas()` mirror de `_render_cruzamento_entradas()`, todas as keys de widget sufixadas `_saidas` (as 2 abas rodam no mesmo script run do `st.tabs()`); testado com dado real da geraldo (REQUEIJAO ISIS CREMOSO 220GR: Critério 1 achou 525 ocorrências/100% similaridade, Critério 3 achou 3 candidatos divergentes) via `loader.py` direto e depois via `AppTest` navegando as 2 abas sem exceção; propagado nas 3 operações reais. **Aba "📦 Estoque"** (2026-07-25, Solicitação Técnica "BUSCA DE CORRESPONDENTES NO ESTOQUE") — mirror do cruzamento de Entradas sobre `estagio8_estoque_agrupado`/`estagio8_estoque_detalhado` (Estágio 8.2, Bloco H), com Critério 1 (mesmo código) e Critério 2 (nome de declaração igual), SEM Critério 3 (não pedido); diferença estrutural: o Bloco H só tem UMA descrição por item (`descrição_decl`, sem `desc_xml` separado) — `loader.cruzar_produto_escolhido_estoque()`/`_detalhado()`/`_criterio2()`/`_criterio2_detalhado()` preenchem `desc_xml` como ALIAS de `descrição_decl` (mesmo valor nas duas colunas) só pra caber no mesmo esquema de `cruzamento_confirmado`, sem exigir NENHUMA mudança em `salvar_cruzamento_confirmado()`/`_detalhado()`; `SIMILARIDADE_DESCRICAO` calculada entre `descrição_decl` e `DESCR_ALVO` (não `desc_xml`, que não existe de verdade aqui); idunico SINTÉTICO do Estágio 8.2 preservado nas confirmações, pra rastreabilidade da auditoria física futura (Estágio 15); tabela final "Itens individuais" SEM o enriquecimento fiscal usado em Entradas/Saídas (não se aplica — o idunico sintético não existe em `estoque_entradas`/`estoque_saidas`); `interface._render_cruzamento_estoque()` mirror de `_render_cruzamento_entradas()`, keys sufixadas `_estoque` (as 3 abas rodam no mesmo script run do `st.tabs()`); testado com dado real da geraldo (CERV SKOL LATA 350ML: Critério 1 e Critério 2 acharam a mesma combinação, código `07891149200504`, 7 ocorrências, 100% similaridade — ciclo completo de salvar/limpar validado, coexistindo com as confirmações de entradas/saídas do mesmo alvo, distinguidas por ORIGEM) via `loader.py` direto e depois via `AppTest` com as 3 abas sem exceção, incluindo troca de critério no selectbox do Estoque; propagado nas 3 operações reais em 2026-07-25, junto com o fix de duplicação de autoemissão e o filtro de QUANTIDADE_FINAL do Estágio 8.2 — Estágio 1/3/4/8/8.1/8.2 regenerados em PB_2023_2025/cometa_2021_2025, `verificar_estagio_8()`/`_saidas()`/`_estoque()` batendo nas 3, `AppTest` sem exceção nas 3) | sem doc própria ainda; ver `interface.render_produtos_alvo_salvos()`/`interface._render_cruzamento_entradas()`/`interface._render_cruzamento_saidas()`/`interface._render_cruzamento_estoque()`/`loader.escolher_produto_cruzamento()`/`loader.cruzar_produto_escolhido_entradas()`/`loader.cruzar_produto_escolhido_entradas_criterio2()`/`loader.cruzar_produto_escolhido_entradas_criterio3()`/`loader.cruzar_produto_escolhido_saidas()`/`loader.cruzar_produto_escolhido_saidas_criterio3()`/`loader.cruzar_produto_escolhido_estoque()`/`loader.cruzar_produto_escolhido_estoque_criterio2()`/`loader.salvar_cruzamento_confirmado()`/`loader.salvar_cruzamento_confirmado_detalhado()`/`loader.consultar_atributos_estoque_por_idunico()`. **Enriquecimento fiscal da aba Estoque** (2026-07-25, pedido do usuário: "inclua aqui os campos: ncm4\|unid_prod\|vl_unit_prod\|qte_prod\|vl_total_prod\|ano_ef\|ano_ei\|dt_decl", confirmando explicitamente contra `ESTOQUE(...).xlsx`) — `loader.consultar_atributos_estoque_estoque_por_idunico()` recompõe o idunico SINTÉTICO a partir de `estoque_anual_consolidado` e cruza com o H010 bruto (`load_declaracao_estoque()`) + cadastro de produtos (`load_declaracao_produtos()`, registro 0200, `COD_NCM`); `qtde_prod`/`vl_prod` reaproveitam os nomes já usados em Entradas/Saídas; `dt_decl` NÃO é o `DT_INV` bruto — achado real confirmado contra a referência: é o ÚLTIMO DIA DE FEVEREIRO do `ano_ei` (prazo legal do Bloco H), validado nos 6 anos do CERV SKOL LATA 350ML, inclusive os 2 anos bissextos (29/02/2020, 29/02/2024). Testado com dado real batendo linha a linha com o Excel de referência nas 3 operações; propagado nas 4 (`_MODELO_OPERACAO` + geraldo + PB_2023_2025 + cometa_2021_2025), ver `loader.consultar_atributos_estoque_estoque_por_idunico()`. **Sumário de Unidades para Fator Multiplicador** (2026-07-25, Solicitação Técnica) — na aba "📥 Entradas", logo abaixo da tabela "Itens individuais", nova tabela "📊 Diagnóstico de Unidades (Visão XML)" (`loader.gerar_sumario_unidades_alvo()`): agrupa os itens já confirmados por `unid_prod` (UCOM do XML), contando ocorrências e calculando o valor unitário médio (`vl_unit_prod`) — prova estatística pro auditor decidir o Fator Multiplicador no Estágio 9 (ex.: "cx12" com muitas ocorrências e média alta vs. "unid"/"LAT" com poucas e média baixa sugere conversão) e detectar "unidades intrusas" na rubrica. Rótulo de `unid_prod` reaproveita "Unidade do Produto" (dicionário já tinha essa entrada do enriquecimento fiscal — Solicitação Técnica sugeria "Unidade XML", mantido o rótulo existente por consistência, sem duplicar entrada pro mesmo campo técnico). Testado com dado real da geraldo: CERV SKOL LATA 350ML — "cx12" (45 ocorrências, média R$30,49), "LAT" (2, R$2,50), "cx012" (1, R$31,35 — achado real: unidade intrusa, provável variante/erro de "cx12"), batendo com o exemplo da própria Solicitação Técnica; `AppTest` sem exceção. Propagado nas 4 operações. **"FM Sug"/"Nova Unid" editáveis** (2026-07-25, pedido do usuário: "inclua nesta nova tabela o fm_sug e nova unid. ambos devem ser editáveis") — `fm_sug` traz a MODA do `fm_sugerido` JÁ existente na tabela "Itens individuais" (pedido explícito: "trazer da tabela id único", não uma fórmula nova) dentro de cada grupo de unidade; `nova_unid` começa em `NOVA_UP_PADRAO` ("UNID"); `st.data_editor` (não mais `st.dataframe`) com só essas 2 colunas destravadas, sem persistência própria (a decisão final continua no Estágio 9). Testado com dado real — CERV SKOL LATA 350ML: "cx12"→12, "LAT"→1, "cx012"→12 (mesmo fator do "cx12", reforçando o diagnóstico de unidade intrusa); `AppTest` sem exceção; propagado nas 4 operações. **Clusterização por faixa de preço** (2026-07-25, pedido do usuário: "ex: cx012 e cx12 \| 29-33 \| fm 12 / lt \| 0-10 \| fm 1" — decidir o FM por faixa de preço, não só pelo texto exato de `unid_prod`; confirmado via AskUserQuestion: tolerância PERCENTUAL — não faixas fixas — e mesclar variações em 1 linha só) — `_clusterizar_por_preco()`/`TOLERANCIA_CLUSTER_PRECO_FM=0.10` (10%). **Achado real que mudou o desenho original**: a 1ª versão clusterizava item a item (preço bruto de cada linha), o que FRAGMENTOU "cx12" em 5 pedaços artificiais na geraldo — a variação NATURAL de preço entre notas diferentes do mesmo "cx12" (R$24,90 a R$38,76, fornecedor/desconto/época) já passa de 10%, maior que a diferença entre "cx12" e "cx012" que se queria juntar. Corrigido: a clusterização compara a MÉDIA de cada grupo de TEXTO (não o item isolado) — agrega primeiro por `unid_prod` exato (preserva a variação interna de uma unidade real), só then funde grupos de texto diferentes cujas médias fiquem dentro da tolerância. Testado com dado real (mesmo produto): "cx12" (45, R$30,49) e "cx012" (1, R$31,35, diff 2,7%) fundem em 1 linha (46 ocorrências, R$30,51 média, fm_sug=12); "LAT" (2, R$2,50, diff ~92% de "cx12") continua separado (fm_sug=1) — passou de 3 pra 2 linhas na tabela, sem fragmentar "cx12" internamente. `AppTest` sem exceção; propagado nas 4 operações. **Rastreabilidade das grafias fundidas** (2026-07-25, usuário perguntou "kd cx012?" ao ver a linha mesclada sumir — respondeu "vamos tentar rastreabilidade", confirmado via AskUserQuestion: manter 1 linha por cluster + coluna nova, em vez de voltar a linhas separadas) — nova coluna `grafias_cluster` (dicionário: "Grafias no Cluster") lista, dentro da própria linha consolidada, cada grafia de texto que entrou no cluster com sua contagem individual, da mais pra menos frequente (`_formatar_grafias_cluster()`); testado com dado real — linha "cx12" mostra "cx12 (45), cx012 (1)", linha "LAT" mostra "LAT (2)"; coluna travada (só leitura) no `st.data_editor`, mesmo padrão das demais colunas não editáveis. `AppTest` sem exceção; propagado nas 4 operações. **Layout "up\|vl min_max\|qtde\|fm\|nova up"** (2026-07-25, pedido do usuário no mesmo dia, imediatamente após a rastreabilidade — troca `media_vu` por `vl_min_max`, a FAIXA mínimo-máximo de valor unitário dentro do cluster, resposta direta ao exemplo original da Solicitação Técnica "cx012 e cx12 \| 29-33 \| fm 12", que já pedia faixa, não média; confirmado via AskUserQuestion: substituir a tabela inteira pelas 5 colunas, removendo a coluna "Grafias no Cluster" da tela — a própria faixa já sinaliza a dispersão de preço do cluster) — `vl_min_max` formatado em BR direto no `loader.py` ("24,90 - 38,76"; só um valor quando mín=máx, ex.: "LAT" com as 2 ocorrências no mesmo preço). Testado com dado real da geraldo: "cx12" (46 ocorrências, faixa "24,90 - 38,76", fm_sug=12), "LAT" (2, faixa única "2,50", fm_sug=1). `AppTest` sem exceção; propagado nas 4 operações. **Clusterização por preço REMOVIDA** (2026-07-26, pedido do usuário: "não deixe nenhuma up de fora. crie uma faixa para cx012") — a fusão entre grafias diferentes (ex.: "cx12"+"cx012" numa linha só) escondia a UP minoritária quando a UP majoritária tinha grande variação interna de preço entre notas (achado real do dia anterior: "cx12" varia de R$24,90 a R$38,76); voltou a agrupar por `unid_prod` EXATO, sem fusão nenhuma — toda UP garantidamente vira sua própria linha, com sua própria faixa `vl_min_max`. `_clusterizar_por_preco()`/`TOLERANCIA_CLUSTER_PRECO_FM` removidos do código (sem outros usos). Testado com dado real: "cx12" (45, faixa "24,90 - 38,76", fm_sug=12), "LAT" (2, faixa única "2,50", fm_sug=1), "cx012" (1, faixa única "31,35", fm_sug=12) — as 3 grafias visíveis, nenhuma escondida. `AppTest` sem exceção; propagado nas 4 operações. **Campo TRATAMENTO + aplicação real do FM/Nova Unidade** (2026-07-26, pedido do usuário: "crie o campo 'TRATAMENTO' na tabela de id única. agora crie uma caixa no sumáro para aplicar o fm e a nova unidade na tabela id única. o preço unitário deve ser dividido pelo fm e a qtde deve ser multiplicada. caso haja aplicação do FM, deve ser lançando 'T' o campo tratamento") — diferente do "FM Sug"/"Nova Unid" (só decisão em tela), esta é uma AÇÃO REAL: nova tabela `tratamento_fm_entradas` (por `idunico`: `TRATAMENTO`/`FM_APLICADO`/`NOVA_UNIDADE_APLICADA`/`TS`, `loader.aplicar_tratamento_fm_entradas()`/`consultar_tratamento_fm_entradas_por_idunico()`); checkbox "Aplicar" por UP no Sumário (confirmado via AskUserQuestion: linha por linha, não tudo de uma vez) + botão "🔧 Aplicar FM e Nova Unidade"; itens tratados têm `vl_unit_prod`÷FM, `qtde_prod`×FM, `unid_prod`=Nova Unidade — `vl_prod` invariante por construção ((preço÷f)×(qtde×f)=preço×qtde), validado com dado real. Persistência recalcula ao vivo a partir do bruto+FM salvo (confirmado via AskUserQuestion: não grava o número final direto). **Bug real corrigido durante o `AppTest`**: com nenhum tratamento ainda aplicado, o merge da tabela vazia trazia `FM_APLICADO` como dtype `object`, quebrando a divisão mesmo com 0 linhas selecionadas (`TypeError: Invalid value '[]' for dtype 'float64'`) — corrigido com `pd.to_numeric(..., errors="coerce")` logo após o merge. Testado com dado real da geraldo (cx012: FM=12/UNID aplicado, vl_unit_prod 31,35→2,6125, qtde 1→12, vl_prod inalterado 31,35, TRATAMENTO='T'; teste desfeito ao final). `AppTest` sem exceção nas 3 operações reais; propagado nas 4 operações. **Estendido pra Saídas/Estoque** (2026-07-26, pedido do usuário: "FICOU ÓTIMO. ESTENDA PARA SAIDAS E ESTOQUES") — `loader.gerar_sumario_unidades_alvo()` já funcionava out-of-the-box nas 3 origens (função pura, só espera colunas genéricas); as 2 funções de persistência generalizadas com parâmetro `origem` (`consultar_tratamento_fm_por_idunico()`/`aplicar_tratamento_fm()`), 1 tabela por origem (`tratamento_fm_entradas`/`_saidas`/`_estoque`, mesmo padrão do Estágio 9); lógica de cálculo/render do Sumário extraída em 2 funções compartilhadas (`_aplicar_tratamento_fm_detalhado()`/`_render_sumario_unidades_com_aplicar()`) reaproveitadas nas 3 abas, evitando triplicar código. **Achado real corrigido no processo**: um comentário em `_render_cruzamento_saidas()` afirmava (suposição de 2026-07-25, nunca checada) que o enriquecimento fiscal em Saídas "sempre devolve vazio" — testado com a Rubrica real da geraldo (170 itens de CERV SKOL LATA 350ML), o merge veio 100% preenchido; comentário corrigido. Funciona igual no Estoque mesmo com idunico SINTÉTICO (hash) — a persistência não depende de como o idunico foi gerado. Testado com dado real: Saídas (LAT 169/cx12 1, FM=7 aplicado no cx12, vl_prod invariante 469,84) e Estoque (única UP "LA" com 6 ocorrências, FM=7 aplicado nas 6 de uma vez, vl_prod invariante 131,60); testes desfeitos ao final. `AppTest` sem exceção nas 3 operações reais (3 botões "Aplicar FM e Nova Unidade" encontrados e clicados na geraldo); propagado nas 4 operações. **DESCR_ALVO/COD_ITEM gravados junto** (2026-07-26, pedido do usuário: "ISSO DEVE FICAR SALVO NO PRODUTO ALVO") — o tratamento já persistia por idunico, mas sem o nome do Produto Alvo explícito na tabela; `aplicar_tratamento_fm()` ganhou os parâmetros `descr_alvo`/`cod_item` (obrigatórios), gravados junto em cada upsert — mesmo padrão de toda tabela ligada a um produto alvo neste app (`cruzamento_confirmado`/`cruzamento_confirmado_detalhado`/`produto_alvo_fiscalizacao`). Testado com dado real (cx012, FM=12/UNID, DESCR_ALVO="CERV SKOL LATA 350ML"/COD_ITEM="2203" gravados corretamente); `AppTest` sem exceção nas 3 operações; propagado nas 4 operações. **Uso real registrado**: usuário aplicou FM=12/UNID de verdade pela UI (cx12+cx012, 46 itens do CERV SKOL LATA 350ML, Entradas) — confirmado direto no banco, dado real preservado (não é teste). **Checkbox "Desfazer"** (2026-07-26, confirmado pelo usuário) — `loader.desfazer_tratamento_fm(idunicos, origem)` remove o tratamento dos itens informados; Sumário ganhou 2ª coluna checkbox "Desfazer" ao lado de "Aplicar", 1 botão único ("🔧 Aplicar / Desfazer Tratamento") processa as duas juntas, "Desfazer" com precedência (mesmo padrão do "Salvar"/"Desfazer" da Rubrica). Testado com CUIDADO REDOBRADO (dado real de produção já existia) — usando um idunico FAKE (não colidiu com os 46 registros reais), confirmando as 46 linhas reais intactas antes/depois do teste; `AppTest` sem exceção nas 3 operações; propagado nas 4 operações. **Coluna "Observação"** (2026-07-26, pedido do usuário com print anexado da Rubrica: "CRIE UMA OBSERVAÇÃO COMO NA FIG") — `_aplicar_tratamento_fm_detalhado()` devolve um 4º valor, `ups_ja_tratadas` (conjunto de UPs brutas com ≥1 item já tratado); Sumário ganhou coluna "Observação" ("✅ Já aplicado"/"", mesmo padrão exato da Rubrica, não depende do estado dos checkboxes). Testado com dado real (os 46 registros já aplicados hoje): "cx12"/"cx012" mostram "✅ Já aplicado", "LAT" fica em branco; `AppTest` sem exceção nas 3 operações, 46 registros reais reconfirmados intactos; propagado nas 4 operações. **Detecção de valor destoante — só Estoque** (2026-07-26, pergunta do usuário com print de 6 anos de valores muito díspares — "UM VALOR DESTOANDO. QUAL SERIA MELHOR SAIDA PARA ACHAR ESSE VALOR AUTOMATICAMENTE?"; confirmado "SIM", escopo restrito "SOMENTE PARA ESTOQUE, POR ENQUANTO") — `loader.sinalizar_valor_destoante(df, limiar=3.0)` compara `vl_unit_prod` à MEDIANA do grupo (`unid_prod`, via `.transform("median")` — robusta ao próprio outlier), sinalizando "⚠️ Destoante (Nx da mediana)" quando a razão for ≥3x ou ≤1/3x; nova coluna `valor_destoante` só na tabela de Itens Individuais do Estoque (`_COLUNAS_PREVIEW_CRUZAMENTO_CONFIRMADO_DETALHADO_ESTOQUE`), calculada ANTES da formatação BR. Testado com o mesmo dado real do print (CERV SKOL LATA 350ML, mediana R$1,98) — sinalizou 2020 (R$17,67, 8,9x) **e também** 2021 (R$0,53, 0,3x, abaixo de 1/3), achado reportado ao usuário pra confirmar se o teste simétrico (2 lados) é o comportamento desejado. `AppTest` sem exceção nas 3 operações reais; 46 registros de tratamento reais reconfirmados intactos; propagado nas 4 operações. **Sumário do Estoque separa linha por item destoante** (2026-07-26, pedido direto do usuário: "SEPARE AS LINHAS DO SUMÁRIO, DENTRO DA MESMA UNID"; confirmado via AskUserQuestion: 1 linha por item destoante + os normais continuam agrupados) — `loader.gerar_sumario_unidades_alvo_com_destaque()` (mirror de `gerar_sumario_unidades_alvo()`, usando `sinalizar_valor_destoante()` pra separar os grupos antes de agregar); `_aplicar_tratamento_fm_detalhado()` escolhe essa função só quando `origem=="estoque"`. Refatoração acompanhante: `_moda_ou_none()`/`_formatar_numero_br()`/`_formatar_vl_min_max()` viraram funções de módulo (antes eram closures locais) pra reaproveitar sem duplicar. Testado com o mesmo dado real (CERV SKOL LATA 350ML) — Sumário do Estoque passou de 1 linha ("LA", faixa 0,53-17,67, 6 ocorrências) pra 3 (normais agrupados 0,72-4,01/4 ocorrências; 2020 e 2021 cada um na própria linha); `AppTest` sem exceção, 46 registros reais reconfirmados intactos; propagado nas 4 operações. **Estendido pra Entradas/Saídas** (2026-07-26, pedido do usuário logo após explicação de como funciona a separação: "ESTENDA PARA ENTRADAS E SAÍDAS") — `_aplicar_tratamento_fm_detalhado()` passou a usar `gerar_sumario_unidades_alvo_com_destaque()` uniformemente nas 3 origens (sem branch por `origem`), e o cálculo de `valor_destoante` foi centralizado dentro dessa mesma função (antes só existia no bloco do Estoque), eliminando duplicação; coluna adicionada em `_COLUNAS_PREVIEW_CRUZAMENTO_CONFIRMADO_DETALHADO` (Entradas/Saídas). Validado com dado real ANTES de propagar: nenhum item de Entradas/Saídas do CERV SKOL LATA 350ML cruza o limiar de 3x (maior variação interna, "cx12", só ~1,56x) — extensão segura, Sumário de Entradas/Saídas permanece idêntico a antes; Estoque continua sinalizando 2020/2021 como já validado. `AppTest` sem exceção nas 3 operações reais; 46 registros reais reconfirmados intactos; propagado nas 4 operações. **BUG REAL corrigido: "Aplicar" tratava TODOS os itens da UNID, não só a linha marcada** (2026-07-26, reportado pelo usuário com print: "MARQUE SOMENTE O 17,67 E A APLICAÇÃO CONSIDEROU TUDO ERRONEAMENTE") — desde a separação de linhas por destaque, o Sumário pode ter várias linhas com o MESMO texto de `unid_prod` (ex.: "LA" normal + "LA" destoante×2); a lógica de Aplicar/Desfazer ainda localizava os idunicos comparando TEXTO, então marcar qualquer uma das linhas "LA" selecionava os idunicos de TODAS — confirmado no banco real: os 6 itens do SKOL no Estoque foram tratados quando só 1 deveria ter sido. Corrigido com uma coluna interna `_idunicos` (frozenset por linha, não exibida) em `gerar_sumario_unidades_alvo()`/`gerar_sumario_unidades_alvo_com_destaque()`; `_render_sumario_unidades_com_aplicar()` reescrita pra localizar idunicos por ÍNDICE da linha, nunca mais por texto (inclusive a precedência Desfazer/Aplicar). Testado com dado real: as 3 linhas do Estoque agora têm conjuntos de idunico disjuntos (4+1+1=6, sem sobreposição). `AppTest` sem exceção nas 3 operações; propagado nas 4 operações. Dado real corrigido: usuário confirmou reverter os 5 itens tratados por engano no Estoque (`loader.desfazer_tratamento_fm()`), mantendo só o item de 2020 (17,67, FM=12/UNID) — estado final conferido. **Coluna "Valor Destoante" removida da exibição** (2026-07-26, pedido do usuário: "TIRE A COLUNA VALOR DESTOANTE") — removida de `_COLUNAS_PREVIEW_CRUZAMENTO_CONFIRMADO_DETALHADO`/`_ESTOQUE` e do cálculo em `_aplicar_tratamento_fm_detalhado()` (sem mais uso); a separação de linhas do Sumário (`gerar_sumario_unidades_alvo_com_destaque()`) continua funcionando normalmente, independente dessa coluna — testado com dado real, sem exceção, propagado nas 4 operações. **"Itens individuais" editável** (2026-07-26, pedido do usuário: "UNID E FM DEVEM SER EDITÁVEIS, CASO EU NÃO QUEIRA UTILIZAR O SUMÁRIO") — nova `_render_itens_individuais_editavel()` (interface.py, compartilhada nas 3 abas) troca `st.dataframe` por `st.data_editor` com "Unidade do Produto"/"FM Sugerido" destravadas + botão "💾 Salvar edições desta tabela"; edita e aplica tratamento item a item (via `loader.aplicar_tratamento_fm()`), sem precisar do agrupamento por UP do Sumário. Detecta edições reais via `st.session_state[...]["edited_rows"]`, com fallback pro valor atual quando só 1 das 2 colunas foi editada. **"Valor Unitário (Min-Max)" editável + override de preço-base** (2026-07-26, "1-TRANSFORME O VALOR UNIT MIN-MAX EM EDITÁVEL; 2-ATUALIZE A OCORRÊNCIA; 3-VOLTE AO DEFAULT AO DESFAZER") — `_COLUNAS_TRATAMENTO_FM` ganhou `VALOR_UNITARIO_APLICADO` (nullable); `aplicar_tratamento_fm()` ganhou o parâmetro opcional `valor_unitario_aplicado`; `_parsear_valor_unitario_editado()` (interface.py) só ativa o override quando o campo vira um número único (faixa intocada preserva o comportamento padrão); COM override, `vl_unit_prod=VALOR_UNITARIO_APLICADO÷FM` (mesmo valor pra todo o grupo, corrigindo a variação interna de preço) e `vl_prod` é RECALCULADO; pontos 2 e 3 já eram satisfeitos pela arquitetura existente (Aplicar sempre trata a linha inteira de uma vez; Desfazer sempre apaga o registro inteiro), sem código novo necessário. Testado com dado sintético (sem tocar dado real: override 30,00/FM=12 em 3 itens fake, `vl_prod` recalculado corretamente, Desfazer restaura o bruto exato); migração de schema das tabelas reais já existentes (46 registros) testada sem perda. `AppTest` sem exceção nas 3 operações; propagado nas 4 operações, ver `loader.gerar_sumario_unidades_alvo()`/`loader.gerar_sumario_unidades_alvo_com_destaque()`/`loader.aplicar_tratamento_fm()`/`loader.desfazer_tratamento_fm()`/`loader.consultar_tratamento_fm_por_idunico()`/`loader.sinalizar_valor_destoante()`/`interface._render_itens_individuais_editavel()`/`interface._obter_valor_unitario_editado()` | **"Itens individuais" volta a ser só leitura + campos "utilizados"** (2026-07-26, pedido do usuário: "CRIAR ... os campos quant_utiliz|vu_utilizado|unid_prod_utiliz a fim de preservar os originais caso haja aplicação de FM ... Trocar FM sugerido por FM utilizado (1 caso não seja utilizado e não editável) ... Campo de unidade de produto deixa de ser editável") — `unid_prod`/`vl_unit_prod`/`qtde_prod`/`vl_prod` NUNCA mais são sobrescritos (sempre bruto do XML); 4 colunas novas (`unid_prod_utiliz`/`vu_utilizado`/`quant_utiliz`/`fm_utilizado`) mostram o valor EFETIVO (igual ao original + FM=1 quando não há tratamento). `_render_itens_individuais_editavel()` (edição + botão Salvar) virou `_render_itens_individuais()`, `st.dataframe` simples — a decisão de aplicar/desfazer FM passa a ser feita exclusivamente pelo Sumário de Unidades. Testado com dado real (sem tratamento: utilizados = originais) e dado sintético (com/sem override, originais sempre preservados). `AppTest` sem exceção; propagado nas 4 operações, ver `interface._aplicar_tratamento_fm_detalhado()`/`interface._render_itens_individuais()` | **"Valor Minimo"/"Valor Maximo" separadas** (2026-07-26, pedido do usuário: "SEPARE EM DIAS COLUNAS MIN E MAX") — `_COLUNAS_SUMARIO_UNIDADES_ALVO` trocou o texto único "Valor Unitário (Min-Max)" por `vl_min`/`vl_max` numéricos (2 `NumberColumn` editáveis); `_parsear_valor_unitario_editado()` (parsing de texto) virou `_obter_valor_unitario_editado(valor_min, valor_max)` — override só quando os 2 valores forem iguais. Testado com dado real (mostra colunas separadas corretamente) e dado sintético (mesmo resultado de antes). `AppTest` sem exceção; propagado nas 4 operações |
| 9 | Curadoria de Fator Multiplicador (Entradas/Saídas/Estoque) — ferramenta de saneamento focada na descrição declarada: identifica itens onde a unidade de comercialização diverge da unidade de estoque da auditada, agrupando pra edição em massa | ✅ Implementado (`estagio9_fm_entradas_agrupado` + `fm_entradas_curadoria`; botão "⚖️ ESTÁGIO 9: FATOR MULTIPLICADOR (ENTRADAS)" na 2ª linha do Menu Principal — **primeiro Estágio FORMAL a ocupar um dos números 9-14 reservados** (ver nota do índice); distinto do "Botão 9" informal/Produtos Alvos Salvos (linha acima, "#"="—"); agrupamento por Descrição XML + Valor Unitário XML — **achado real investigado com o usuário ANTES de implementar**: agrupar pelo valor EXATO (como a Solicitação Técnica original pedia) gerava 9.036 grupos de 16.407 itens (76,5% com só 1 ocorrência, ruído de arredondamento em cascata entre notas), não atingindo "edição em massa"; usuário escolheu arredondar o valor unitário pro inteiro mais próximo antes de agrupar (`_valor_unit_grupo`, chave INTERNA de agrupamento/upsert, nunca exibida) — reduz pra 6.593 grupos, com grupos de até 125 ocorrências. **"UP XML" corrigido no mesmo dia** ("refaça. up é unidade de produto: cx, uind, fd" — não é valor unitário, como a 1ª versão assumiu por engano): é a MODA do campo UCOM da NFe (`fatoitemnfe_infnfe_det_prod_ucom` — "UN", "CX", "FD", mas também formas com número embutido como "cx12"/"cx06", achado real: 30 valores distintos na geraldo) dentro do grupo; "Partícula" extraída via regex (`C/N`, `CX N`, `FD N`, `N UNID`) da descrição, só um hint informativo; "FM Sugerido" = moda do `FATOR_MULTIPLICADOR_SUGERIDO` (já calculado no Matching, Estágio 2 — 87% de cobertura real) dentro do grupo; **"Nova UP" também corrigido** (era uma fórmula UP÷FM sincronizada bidirecionalmente — errado; resposta do usuário: "deixe como default 'unid'") — valor PADRÃO editável `"UNID"` (`NOVA_UP_PADRAO`), sem fórmula nenhuma ligando a FM Sugerido. **Refinado no mesmo dia** ("ficou otimo. 1-retire a coluna 'gravar' e observaçãao'; 2-transforme fm sugerido 12.000 em 12; 3-tabela deve estar vinckada à tabels com id único"): sem checkbox de seleção nem coluna "Observação" — toda a tabela fica editável e "Salvar" grava TODOS os grupos de uma vez (edição em massa sem passo de marcação); "FM Sugerido" com `format="%g"` (remove zeros à direita: "12.0"→"12"); nova tabela "Itens individuais (com ID Único)" (`loader.consultar_curadoria_fm_entradas_detalhado()`) junta `fm_entradas_curadoria` com `estoque_entradas` pela mesma chave de agrupamento, trazendo o `ID_UNICO` de cada item coberto por um grupo salvo — mesmo espírito da tabela homônima do Botão 9. Persistência em `fm_entradas_curadoria` (`VALOR_UNIT_GRUPO`/`FM_ELEITO`/`NOVA_UP`/`TS`) com a mesma semântica de SINCRONIZAÇÃO (`universo_chaves`) já usada na Rubrica do Botão 9 — desmarcar (ou não reenviar) remove de fato; testado com dados reais nas 3 operações (geraldo 6.593 grupos, PB2 490, cometa 4.009 — soma de `qtde_ocorrencias` bate exato com o total de `estoque_entradas` nas 3; tabela vinculada validada com 350 itens = soma exata de 3 grupos salvos de teste); memória de cálculo prevista pro Estágio 15 (RN1), ainda não consumida lá. **Expandido 2026-07-25** ("crie tb fator para descrição de saídas e estoques nos moldes das entradas") — página virou 3 abas de nível superior ("📥 Entradas"/"📤 Saídas"/"📦 Estoque", mesmo padrão do Estágio 8), botão do Menu mantido com o nome original (mesmo precedente do Estágio 8, cujo botão continua dizendo "RESUMO DE ENTRADAS" apesar de ter as 3 abas). **Saídas** (`gerar_curadoria_fm_saidas()`, mirror exato de Entradas sobre `estoque_saidas`) — achado real confirmado com o usuário ANTES de implementar: `FATOR_MULTIPLICADOR_SUGERIDO` só é preenchido em 0,3% das linhas de `estoque_saidas` (153/60.382, já que o Matching/BC3 só liga Entradas de terceiros) — mantida a coluna "FM Sugerido" mesmo quase sempre vazia (decisão explícita do usuário), o auditor digita o FM_ELEITO manualmente na maioria dos grupos. **Estoque** (`gerar_curadoria_fm_estoque()`, sobre `estoque_anual_consolidado`, Bloco H) — diferença estrutural real: essa tabela não tem valor unitário nem UCOM, só `ANO_REFERENCIA/COD_ITEM_DECLARACAO/DESCR_ITEM_DECLARACAO/UNIDADE/QUANTIDADE_INICIAL/QUANTIDADE_FINAL` — agrupamento simplificado pra só Descrição Declarada (sem `_valor_unit_grupo`), "UP Estoque" = moda do campo UNIDADE, SEM "FM Sugerido" pré-calculado (Bloco H não tem ligação com Matching/BC3 — coluna "FM Eleito" fica em branco por padrão, auditor digita do zero); mesmo filtro `QUANTIDADE_FINAL IS NOT NULL` do Estágio 8.2 aplicado aqui também (achado real: sem o filtro, CERV SKOL LATA 350ML mostrava 7 grupos aqui contra 6 no Estágio 8.2/Botão 9 — inconsistência corrigida antes de propagar); tabela "Itens individuais" reaproveita o idunico sintético de `estagio8_estoque_detalhado` (já filtrado), sem recalcular hash. Testado com dado real da geraldo: Saídas 4.967 grupos (150 com FM Sugerido, CERV SKOL LATA 350ML com 8 itens no subgrupo de valor unitário=2), Estoque 5.974 grupos (CERV SKOL LATA 350ML com 6, batendo com Estágio 8.2); ciclo completo de salvar/limpar validado nas 2 abas novas, dados de teste removidos, estado restaurado; `AppTest` nas 3 abas sem exceção. Propagado nas 3 operações reais em 2026-07-25 — PB_2023_2025 (Entradas 490, Saídas 585, Estoque 96 grupos) e cometa_2021_2025 (Entradas 4.009, Saídas 2.513, Estoque 45 grupos), `AppTest` sem exceção nas 3. **"FM Sugerido" (Entradas/Saídas) corrigido no mesmo dia** — deixou de ser `moda(FATOR_MULTIPLICADOR_SUGERIDO)` (Matching, Estágio 2, comparando XML×SPED): achado real investigado com o usuário — o mesmo produto físico (CERV SKOL LATA 350ML, sempre UCOM "cx12") sugeria FM=12 numa nota e FM=1 noutra, não por mudança no produto, mas porque a AUDITADA nem sempre desmembrava a caixa de 12 no próprio estoque/SPED (confirmado com dado real da bc3: `VALOR_UNITARIO_DECLARACAO` de R$2,50/lata numa nota — desmembrado — contra R$30,19/caixa noutra — não desmembrado); usuário pediu explicitamente "para o fm, nas entradas e saídas não olhe para o sped, olhe apenas para o xml" — nova `_extrair_fator_multiplicador_xml(desc_xml)` reaproveita o regex de "Partícula" (`C/N`, `CX N`, `FD N`, `N UNID`) mas devolve o NÚMERO como fator (fator=1 se nenhum padrão reconhecido), aplicada em `gerar_curadoria_fm_entradas()`/`gerar_curadoria_fm_saidas()` e em `consultar_atributos_estoque_por_idunico()` (tabela "Itens individuais" do Botão 9) — mudança restrita ao Botão 9/Estágio 9, sem alterar o Matching/`REGRAS_MATCHING.md` (que mantém `FATOR_MULTIPLICADOR_SUGERIDO` original pros seus próprios usos); fator passa a ser DETERMINÍSTICO por descrição (não varia mais entre notas do mesmo produto); testado com dado real — todas as 42 ocorrências de "SKOL LATA 350ML SH C/12 NPAL" na Rubrica passaram a mostrar fm_sugerido=12 uniformemente (antes misturava 12/1); Estágio 9 regerado (mesma contagem de grupos — 6.593/4.967 geraldo, 490/585 PB2, 4.009/2.513 cometa — só o valor do fm mudou); `AppTest` sem exceção; propagado nas 4 operações) | sem doc própria ainda; ver `interface.render_pagina_estagio_9()`/`interface.render_curadoria_fm_entradas()`/`interface.render_curadoria_fm_saidas()`/`interface.render_curadoria_fm_estoque()`/`loader.gerar_curadoria_fm_entradas()`/`loader.gerar_curadoria_fm_saidas()`/`loader.gerar_curadoria_fm_estoque()`/`loader.salvar_curadoria_fm()`/`loader.salvar_curadoria_fm_saidas()`/`loader.salvar_curadoria_fm_estoque()`/`loader._extrair_fator_multiplicador_xml()` |
| 15 | Cálculo de divergência RN1 (Estoque Inicial + Compras = Vendas + Estoque Final) — compara o inventário declarado (Estágio 5) com a movimentação real (Estágio 4), com a lógica de PU (preço unitário) e omissão do texto original | ⏳ Planejado — os Estágios 7.2/7.3 já montam a identidade EI+C=V+EF em valor (por código e por Descrição Relevante), mas sem a lógica de PU/omissão do texto original (condições c>0/c=0, tie-break); Fator Multiplicador curado no Estágio 9 (linha acima) ainda não é consumido aqui | sem doc própria ainda; ver `regra de negócios unificadas/regra negocio_pu_rn1_ei+c=v+ef_1.txt` (raiz) e nota em [docs/estagios/04_cronologia_ano_eleito.md](docs/estagios/04_cronologia_ano_eleito.md) |

## Como usar este índice

- Cada estágio novo ganha uma linha aqui e um arquivo próprio em
  `docs/estagios/NN_nome.md`.
- Atualizar o `Status` (⏳ Planejado / 🚧 Em andamento / ✅ Implementado)
  conforme o trabalho avança.
- Estágios que já têm documentação própria e detalhada em outro lugar do
  projeto (ex.: o motor de Matching em `REGRAS_MATCHING.md`) não duplicam o
  conteúdo aqui — o arquivo do estágio só resume e aponta pra fonte
  detalhada.
- Numeração não é estritamente sequencial (ver o salto 6 → 15): o Estágio 6
  original (cálculo de divergência RN1) foi renumerado para 15 em
  2026-07-14 para abrir espaço, no 6, para o menu de navegação — os números
  7-14 ficam reservados para painéis intermediários futuros, sem precisar
  renumerar de novo. **Atualizado 2026-07-18**: o Estágio 7 ("Escolha do
  Produto Alvo") usou esse espaço reservado, com sub-passos numerados
  `7.1`, `7.2`... (primeiro precedente de numeração `N.M` neste índice —
  o Estágio 6, "VAMOS ORGANIZAR", não teve seus grupos internos numerados
  dessa forma). O sub-passo 7.1 ("Fixação da Descrição Relevante") virou
  — por Solicitação Técnica explícita — botão de 6º nível PRÓPRIO no Menu
  Principal, não um sub-painel dentro de "Tabelas Entradas / Saídas /
  Estoques" como a reserva original previa; os números 8-14 continuavam
  livres pra painéis futuros, do mesmo jeito (sub-painel ou botão
  próprio, a decidir caso a caso). **Atualizado 2026-07-23**: o Estágio
  8 ("Resumo de Entradas", com sub-passos `8.1`/`8.2` pro mesmo padrão
  sobre Saídas/Estoques) usou esse espaço reservado — só **9-14**
  continuavam livres. O "Botão 9" ("Produtos Alvos Salvos") NÃO consome
  essa reserva — é numeração própria do usuário pra botões novos do
  menu (não ligada à numeração de Estágio); sua linha na tabela passou
  a usar "#"="—" (em vez de "9") a partir da atualização abaixo, pra
  não colidir visualmente com o Estágio formal que veio a seguir.
  **Atualizado 2026-07-24**: o Estágio 9 ("Curadoria de Fator
  Multiplicador — Entradas") usou o número 9 — primeiro Estágio FORMAL
  a ocupar a reserva desde que ela foi criada; só **10-14** continuam
  livres.
